<!-- Last verified: 2026-05-24 by Claude Code (CH-10-i-phi retro `281cb58d` standards-update batch landed (7 of 7 proposals approved all Apply): **Proposal #1 HIGH (plan-iteration-count cross-cycle pattern: 4th sub-class "gate-1 lock-revision-on-re-surfacing")** codified at outer CLAUDE.md "Plan-iteration-count cross-cycle pattern" section — hypothesis CONFIRMED-5-of-5 at i-phi (CH-05/16a/16b/07a/CH-10 all user-direction-driven NOT Architectural-FAIL); NEW 4th sub-class added (CH-10's iter-3 reverted iter-2 F.b crossterm propagation back to F.a rustyline at user re-surfacing). 4 sub-classes within parent class now: structural § restructure / scope-reduction lock-absorption / scope-EXPANSION lock-absorption / gate-1 lock-revision-on-re-surfacing. **Proposal #2 HIGH (chunk-implementer v22 P-impl-1-v22 per-commit-end LOC boundary)** codified at chunk-implementer v21→v22 — when ≥ 2 plan phases consolidate into single commit, 2× LOC cap pause-discipline fires at commit-end (proactive layer pairing with v15/v16 retroactive fallback). Closes D-5 + D-2/D-3/D-4 retroactive-routing cascade (CH-10's `P-REPL-CORE` commit absorbed 8 phases without firing v15 sub-section trigger). **Proposal #3 MEDIUM (chunk-planner §8 baseline decomposition)** codified at chunk-planner v29→v30 NEW P-plan-9-v30 — plan §8 baseline must decompose into "binary + inline = total" with asymmetric band computed against binary-only count. Closes D-6 plan-narrative classification gap (CH-10 plan §8 cited 245 omitting 50 inline). **Proposal #4 LOW (inline-test cardinality heuristic)** codified at chunk-planner v30 NEW P-plan-10-v30 — enum-with-N-variants × parse-variant matrix predicts N + 4-6 edge cases inline minimum (N=6 → 10-12). Closes D-7 inline-test overshoot (CH-10 6-variant SlashCommand × parse predicted 3-5, shipped 11). **Proposal #5 MEDIUM (code-LOC cap-relaxation framing)** codified at chunk-planner v30 NEW P-plan-11-v30 — per-file LOC caps expressed as "≤ N LOC OR <functional-axis-coverage criteria>" for wrapper / envelope / enum modules. Mirror of v29 P-plan-7 doc-LOC framing for code-LOC symmetry. Closes D-2/D-3/D-4 Band 3 cap-derivation gap (editor.rs cap 100 underestimated rustyline wrapper's 5 functional axes; steer.rs cap 100 underestimated 5 axes; main.rs cap 220 mathematically constrained CH-09 baseline 265). EXTEND'd file caps must express as `≤ baseline + Δ LOC`. **Proposal #6 LOW (gate-3 method-signature 5-axis: arg-shape divergence)** codified at outer CLAUDE.md gate-3 cross-check section — extended from 4-axis to 5-axis (F-token / lock-body / test-name / method-signature / arg-shape-refinement-flag). Closes D-8 within-lock refinement (CH-10 dispatch arg-shape refined from plan `&mut ReplState` to F-steering-input-lane.a-mandated `Arc<Mutex<ReplState>>` for tokio task-safe coordination). **Proposal #7 LOW (cross-cycle observation)** codified at outer CLAUDE.md cross-cycle fork-divergence section — i-phi 2-of-36 (~6%) → 2-of-42 (~5%); 4th-consecutive Direct-approval-clean cycle (CH-08 + CH-07b + CH-09 + CH-10). Version bumps: chunk-planner.md v29 → v30 + chunk-implementer.md v21 → v22 + outer CLAUDE.md (3 sections updated: plan-iteration-count pattern + gate-3 5-axis cross-check + cross-cycle fork-divergence observation). i-phi submodule bumps to capture CH-10 cycle artifacts (forward-scope inline-draft prereq + plan archive + 13 phase commits + cycle-audit PASS + retrospective + retro Status flip + cycle-audit metric fix + standards-related). **CH-10 stats**: cycle hex `281cb58d`; chunk-seal commit range `dfacce8..309afd2` consolidated into 7 logical commits + plan archive + gate-4 + retro; 6 forks all LOCKED at gate-1 at planner-rec (Direct-approval-clean; iter-3 absorbed F-tui-library lock revision back to F.a rustyline at user re-surfacing — net 4th-consecutive i-phi Direct-approval-clean cycle); 1 audit iteration per letter (A iter-1 PASS 15/15 with 1 PASS-with-note dispatch arg-shape + B iter-1 PASS 22/22 + C iter-1 PASS 19/19); 56/56 claims PASS across 3 auditors; 0 auditor re-spawns; 0 orchestrator-applied Trivial-1L; workspace tests 245 → 284 passing + 1 ignored (+39 over baseline; +21 above plan upper band [256, 263] driven by D-7 inline overshoot + D-6 baseline classification gap; Route-A-accepted at gate-2.5); cross-cluster invariant extends 10 → 11 cycles ratified (CH-03/04/05/06/08/16a/16b/07a/07b/09/CH-10); ADR-0012 Accepted with 10 sub-decisions §D12.1-§D12.10; 3 NEW drifts filed at canonical `docs/v0/design/drifts/` (D-CH10-FOLLOWUP-01 persistent-history → CH-14 + D-CH10-FOLLOWUP-02 user-defined-commands → CH-14/CH-17 + D-CH10-FOLLOWUP-03 /branch+/rewind → CH-17); +1 phi-core leverage-site (LS1 `ProgressMessage` consumption in src/repl/render.rs); clippy + fmt GREEN; 4 CI guards green at sub-agent + orchestrator gate-4; 9.6 GiB disk reclaimed (placement-1 4.8 GiB mid-cycle + placement-2 4.8 GiB at close); permissions-audit narrow-mode captured 300 tool calls + 0 hot allow-rule candidates (8-consecutive-cycle 0-hot streak excluding CH-07a structural disruption) + 0 hook false-positives (cleanest cycle to date); 8 deviations in cycle-audit §6 (1 process-orchestrator-re-surfacing D-1 / 3 code-Band-3-LOC Route-A-accepted D-2/D-3/D-4 / 1 process-implementer-pause-discipline-gap D-5 / 1 plan-narrative-baseline D-6 / 1 code-inline-test-overshoot D-7 / 1 code-interface-contract-refinement-within-lock D-8); inline forward-scope authorship at Phase 0 step 7 (CH-10 = 3rd-of-3 i-phi inline-draft; pattern codified at CH-09 retro proposal #3); DaemonClient promoted to NEW `src/client/` shared module per F-daemon-client-shared.a (CH-09's `src/cli/client.rs` collapsed to 14-line `pub use` shim; CH-11a will be 3rd consumer); 3-plan-iteration count from iter-2 → iter-3 lock revision at user re-surfacing (NEW sub-class codified in Proposal #1); net health: 9 GREEN (audit-pass at iter-1 across all 3 auditors + cargo-clean + leverage sites within +1 prediction + test-band-+21-via-D6-D7 + cross-cluster 11-cycle ratification + cross-submodule + zero-hot-candidates + zero-hook-false-positives + 4th Direct-approval-clean streak) + 1 YELLOW-attention (3 Band 3 LOC overruns flagged for retro process improvement) + 0 RED. Standards updates landed across `/root/projects/phi/CLAUDE.md` + `.claude/agents/chunk-planner.md` v29→v30 + `.claude/agents/chunk-implementer.md` v21→v22.) -->

<!-- Last verified: 2026-05-24 by Claude Code (CH-09-i-phi retro `075c07cf` standards-update batch landed (8 of 8 proposals approved all Apply): **Proposal #1 HIGH (gate-1.5 P-orch-5 `cargo check`-grounded plan-narrative literal-reconciliation pass)** codified at outer CLAUDE.md gate-1.5 pre-archival quartet — extended to quintet with P-orch-5 running `cargo check --message-format=json` at gate-1.5 + grepping plan §1/§4 for `phi_core::<TypeName>` patterns + cross-referencing against workspace surface; mismatches surface as Trivial-1L plan-edit BEFORE archive. Closes the highest single-cycle plan-narrative-cluster on i-phi to date (4 deviations D-1+D-2+D-3+D-4 in cycle-audit §6 all preventable). **Proposal #2 HIGH (gate-1.5 P-orch-6 skeleton-vs-fully-behavioural within-lock-body verification)** codified at outer CLAUDE.md gate-1.5 — extended to sextet with P-orch-6 cross-checking each F-LOCKED.a body for within-lock sub-decisions shipping as (a) skeleton/stub OR (b) signal-only OR (c) store-verify-only OR (d) hardcoded default; each marked `[v0 scope-narrowing: <one-line>]` annotation in §1 lock body BEFORE archive. Closes 3rd-cycle pattern across CH-07a + CH-07b + CH-09. **Proposal #3 MEDIUM (chunk-initiate Phase 0 step 7 inline-draft procedure codification)** codified at chunk-initiate/SKILL.md Phase 0 step 7 — explicit 7-step procedure when forward-scope file absent (read adjacent forward-scopes for context → draft 200-500 LOC inline → AskUserQuestion Approve/Edit/Pause/Abort → on Approve write file + chunk-order.md row flip + single prerequisite commit → on Approve resume Phase 0 step 8). 2-of-2 i-phi precedent (CH-01 + CH-09 both Direct-approval-clean). CH-09 reference template added alongside CH-01. **Proposal #4 LOW (cross-cycle observation update i-phi 2-of-29 → 2-of-36)** codified at outer CLAUDE.md "Cross-cycle fork-divergence observation" — i-phi count flips from `2-of-22 (~9%)` across 7 cycles to `2-of-36 (~6%)` across 8 cycles (CH-09 added 7-of-7 planner-rec; 3rd-consecutive Direct-approval-clean cycle CH-08 + CH-07b + CH-09 since CH-07a's USER-DIVERGENT lock; rate dropped ~9% → ~6% as planner-rec dataset broadens). **Proposal #5 MEDIUM (settings.json widen audit-tmp glob to i-phi paths)** codified at `.claude/settings.json` line 49 — 1-line addition `Bash(bash /root/projects/phi/i-phi/scripts/audit-tmp-*.sh*)` sibling to existing baby-phi rule. Enables audit-tmp scripts co-located under target project (removes second-class workaround). **Proposal #6 LOW (doc-LOC threshold authoring with semantic-completeness escape)** codified at chunk-planner v29 NEW P-plan-7-v29 — §"Verification" rows for documentation-tier deliverables (design/{interfaces,...}.md / user-guide/...md / concept docs) MUST be expressed as `≥ N LOC OR <semantic-completeness criteria>` rather than pure-numeric. Closes the cli.md 197 LOC vs ≥ 200 Trivial-1L pattern (D-10 in cycle-audit §6). **Proposal #7 LOW (wire-types-vs-handler-bodies sub-phase split heuristic for IPC-route phases)** codified at chunk-planner v29 NEW P-plan-8-v29 — when a phase ships a `POST /<route>` with BOTH (a) wire-type structs (Request/Response) AND (b) handler body AND (c) daemon-side method implementations, EITHER split into 3 sub-phases (P-WIRE-TYPES → P-HANDLERS → P-DAEMON-METHODS) with cargo-check between OR annotate expected internal ordering. Closes D-5 (implementer moved pause/resume/harvest wire types from P-PAUSE-RESUME-HARVEST-WIRING to P-CLIENT for compile cleanness; RED-window-neutral). **Proposal #8 LOW (chunk-implementer verified-header annotation-length cap)** codified at chunk-implementer v21 NEW P-impl-1-v21 — `[ANSWERED at CH-NN: ...]` open-question annotations capped at 2 lines; longer rationale hoisted to a sibling `### §X.Y — <topic>` section with 1-line pointer. Closes sessions.md Q-pause-time-estimate 4-line annotation pattern (Audit B Claim 10 PASS-with-caveat). **CH-09 stats**: cycle hex `075c07cf`; chunk-seal commit range `dfacce8..e80a35a` (13 phase commits + 1 plan archive `e745d12` + 1 cycle-audit `c86f596`); 7 forks all LOCKED at gate-1 at planner-rec (Direct-approval-clean; zero divergence; 3rd consecutive i-phi Direct-approval-clean cycle); 1 audit iteration per letter (A iter-1 PASS 15/15 + B iter-1 PASS 22/22 with 1 PASS-with-caveat orchestrator-applied Trivial-1L + C iter-1 PASS 19/19); 56/56 claims PASS total at iter-1; 0 auditor re-spawns; 1 Trivial-1L orchestrator-applied (cli.md design 197→202 LOC + §"Notes" addendum); workspace tests 219 → 245 passing + 1 ignored (within band [240, 247]; +5 over lower-bound from inline test absorption); cross-cluster invariant extends 9 → 10 cycles ratified (CH-03/04/05/06/08/16a/16b/07a/07b/CH-09); ADR-0011 Accepted with 11 sub-decisions §D11.1-§D11.11; 3 NEW drifts filed at canonical `docs/v0/design/drifts/` (D-CH09-FOLLOWUP-01 multi-model-provider-override → CH-14 + D-CH09-FOLLOWUP-02 cli-auth-cookie-file → CH-15 + D-CH09-FOLLOWUP-04 tool-timeout-plumbing → M5-DEFERRED-04); +4 phi-core leverage-sites within ±3 of plan +3 prediction; clippy + fmt GREEN; 4 CI guards green at sub-agent + orchestrator gate-4; 4.5 GiB disk reclaimed (placement-1 within-cycle); permissions-audit narrow-mode captured 469 tool calls + 0 hot allow-rule candidates (7-consecutive-cycle 0-hot streak excluding CH-07a structural disruption) + 0 hook false-positives + 1 widening opportunity rolled into Proposal #5; 10 deviations in cycle-audit §6 (4 plan-narrative D-1/D-2/D-3/D-4 / 1 phase-ordering D-5 / 1 Band-2 LOC D-6 / 1 defensive omit D-7 / 2 v0-scope-narrowing D-8/D-9 / 1 orchestrator Trivial-1L D-10); inline forward-scope authorship at Phase 0 step 7 (CH-09 = 2nd-of-2 i-phi inline-draft); net health: 9 GREEN (audit-pass at iter-1 across all 3 auditors + cargo-clean + leverage sites within band + test-band-lower-bound +5 + cross-cluster 10-cycle ratification + cross-submodule + zero-hot-candidates + zero-hook-false-positives + Direct-approval-clean lock-set) + 0 YELLOW + 0 RED. Standards updates landed across `/root/projects/phi/CLAUDE.md` + `.claude/agents/chunk-planner.md` v28→v29 + `.claude/agents/chunk-implementer.md` v20→v21 + `.claude/skills/chunk-initiate/SKILL.md` + `.claude/settings.json`.) -->

<!-- Last verified: 2026-05-21 by Claude Code (CH-16b-i-phi retro `634ce263` standards-update batch landed (6 of 6 proposals approved all Apply): **Proposal #1 HIGH (cross-cycle fork-divergence streak END)** codified at outer CLAUDE.md "Cross-cycle fork-divergence observation" — i-phi count flips from "0-of-12 (~0%) across CH-05/CH-06/CH-08" to "1-of-14 (~7%) across CH-05/CH-06/CH-08/CH-16a/CH-16b" with explicit "**4-cycle unanimous-planner-rec streak ENDED at CH-16b gate-1.5** with F-async-bridge.b user-DIVERGENT — cause class is `block_on`-safety architectural-divergence" framing + cross-reference back to CH-16b cycle hex. **Proposal #2 MEDIUM (audit-prompt-authoring cross-check extended to non-F-token paraphrases)** codified at outer CLAUDE.md gate-3 "Audit review" §3 — cross-check extends from F<X>.<letter> token grep to non-F-token claim paraphrases citing plan §1 Locked-fork-details lock-body wording. CH-16b D-2 evidence cited (super-episode parent_episode_id paraphrase divergence). Closes orchestrator-side process miss class. **Proposal #3 MEDIUM (forward-scope drift-ID-substitution belt-and-suspenders)** codified BOTH layers per user-selection: (a) outer CLAUDE.md gate-2 widened sweep regex extended from `\bFOLLOWUP(-[A-Z]+)?-NN\b` to `\b(D-CH[0-9A-Za-z]+-)?FOLLOWUP(-[A-Z]+)?-NN\b` (catches `D-CH<NN>(b|c)?-FOLLOWUP-NN-*` planning-time placeholders); (b) chunk-implementer v17→v18 NEW P-impl-1-v18 forward-scope drift-ID-substitution P-SEAL step (greps forward-scope file for placeholders + substitutes actual filed drift IDs in same P-SEAL commit batch as drift filings). Belt-and-suspenders mirrors CH-04 v3 P14 + v22 P13 pattern. CH-16b 2-line orchestrator-applied Trivial-1L cited as evidence. **Proposal #4 LOW (plan-iteration-count pattern CONFIRMED at 2-of-2 i-phi data points)** codified at outer CLAUDE.md "Plan-iteration-count cross-cycle pattern" section — CH-16b's 2-iter user-direction-driven cause class CONFIRMS the CH-16a hypothesis; both data points are "decision refinements" not "technical defect corrections". Continue tracking at CH-07. **Proposal #5 LOW (drift-bundling heuristic for shared-root-close-criterion deferrals)** codified at chunk-implementer v18 NEW P-impl-2-v18 — when ≥2 deferred surfaces share a single root close-criterion (same upstream dependency + same code-shape resolution + same future cycle closes all N), file ONE coherent drift instead of N separate drifts. CH-16b D-CH16b-FOLLOWUP-01-phi-core-async-strategy.md cited as canonical precedent (3 LLM bodies under one drift). Decision criteria + drift-body shape documented. **Proposal #6 LOW (cycle-audit §6 deviation-class row template for audit-prompt-authoring miss)** codified at outer CLAUDE.md gate-4 §"Final cycle re-audit" — NEW prose paragraph added describing standard row shape for `audit-prompt-authoring miss` deviation-class. CH-16b D-2 cycle-audit row cited as canonical precedent. Deviation-class doesn't increment iteration counters. **CH-16b stats**: cycle hex `634ce263`; 2 plan iterations (iter-1 → iter-2 post-gate-1.5-lock-set; user-direction-driven NOT Architectural-FAIL — CH-16a pattern hypothesis CONFIRMED at 2-of-2 i-phi data points); 4 forks all locked (F2.b + F4.b PRE-LOCKED DIVERGENT from CH-16 gate-1; F-async-bridge.b LOCKED DIVERGENT at CH-16b gate-1.5 — first within-cycle DIVERGENT lock on i-phi since the unanimous-planner-rec streak CH-05/CH-06/CH-08/CH-16a; F3.a LOCKED planner-rec); workspace tests 167 → 176 passing + 1 ignored (upper bound of plan band [171, 176]); clippy + fmt GREEN; cross-cluster invariant extends 6→7 cycles ratified (CH-03/04/05/06/08/16a/16b); 3 ALWAYS drifts filed at canonical `docs/v0/design/drifts/` (D-CH16b-FOLLOWUP-01-phi-core-async-strategy MEDIUM bundles 3 LLM-emitting body deferrals under shared close-criterion + D-CH16b-FOLLOWUP-02-per-agent-compactor LOW + D-CH16b-FOLLOWUP-03-recursive-depth-bound LOW); ADR-0017 Accepted with 5 sub-decisions §D17.1-§D17.5 + ADR-0016 §D16.4 fully closed + §D16.6/§D16.7 partial-closure-with-drift-cite + ADR-0007 §D7.11 addendum cite + compaction.md §8 line 138 fully ANSWERED + line 142 PARTIAL with drift cite; 3.0 GiB disk reclaimed (placement-1 post-Phase-2; placement-2 at gate-5 retro close pending); 1 Trivial-1L orchestrator-applied patch (forward-scope FOLLOWUP-NN → actual drift IDs); 0 audit re-spawns (A PASS at iter-1 + B PASS-with-caveat at iter-1); 4 sub-pause deviations all properly logged (strategy.rs/extraction.rs doc-comment over-delta + recursive.rs Band 2 132% + mod.rs Band 2 114% — all below 1.5× pause threshold); 1 audit-prompt-authoring miss D-2 (orchestrator-side process; closed by Proposal #2 extension + Proposal #6 template); net health: 8 GREEN (audit-pass at iter-1 + cargo-clean + leverage sites within +1 to +3 band + test-band-upper-edge + cross-cluster 7-cycle ratification + cross-submodule + zero-hot-candidates + zero-hook-false-positives) + 1 YELLOW informational (FOLLOWUP-NN placeholders surface that the belt-and-suspenders extensions close) + 0 RED. Standards updates landed across `/root/projects/phi/CLAUDE.md` + `.claude/agents/chunk-implementer.md` v17→v18.) -->
<!-- Last verified: 2026-05-21 by Claude Code (CH-16a-i-phi retro `066799f3` standards-update batch landed (6 of 6 proposals approved all Apply): **Proposal #1 HIGH (Locked-fork-details at §1 front-of-plan)** codified at chunk-planner.md v27→v28 NEW P-plan-3-v28 §1-position rule + chunk-initiate/SKILL.md Phase 1.5 Step A updated to reference `## §1 — Locked fork details` at front-of-plan (was `### Locked fork details` appendix at end-of-plan); user-direction at CH-16a iter-3 2026-05-21 codified — locked outcomes provide essential context for §2-§13 reading. Third artifact `baby-phi/docs/specs/v0/implementation/m5_1/process/per-chunk-planning-template.md` full restructure DEFERRED to a future baby-phi cycle (chunk-planner v28 prompt enforces regardless of template default). **Proposal #2 MEDIUM (Split-decision-as-routing pattern)** codified at chunk-initiate Phase 1 step 6 Approve-split branch with 4-step protocol: (a) orchestrator drafts NEW chunk's forward-scope file inline; (b) updates chunk-order.md; (c) commits as prerequisite; (d) iter-2 planner re-spawn. Precedents CH-16a + CH-02a cited. **Proposal #3 MEDIUM (Closure-side state-machine pattern guidance)** codified at chunk-planner v28 NEW P-plan-4-v28: when phi-core lifecycle Fn signature lacks data closures need across invocations, propose `Arc<Mutex<<NewState>>>`-shared-state with caller-side population + add ~20-40% LOC budget to closure file §4.B cap. ADR-0016 §D16.5 canonical precedent. **Proposal #4 MEDIUM (Same-name cross-cluster-trait disambiguation convention)** codified at i-phi/CLAUDE.md NEW "Same-name cross-cluster types convention" bullet + chunk-planner v28 NEW P-plan-5-v28 cross-cluster naming-conflict grep step at §3 forbidden-duplication enumeration. Requires (a) mod-doc note in NEW cluster's `mod.rs`; (b) ADR sub-decision; (c) consumer-facing cross-reference. CH-16a EpisodeStore precedent. **Proposal #5 LOW (Plan-iteration-count cross-cycle observation + inline-test allowance narrowing)** codified at outer CLAUDE.md NEW "Plan-iteration-count cross-cycle pattern" section (CH-16a 3-iter user-direction-driven NOT Architectural-FAIL; different cause class from baby-phi CH-28) + chunk-planner v28 NEW P-plan-6-v28 inline-test allowance narrowing rule (~0.25N reduction when MUST-SHIP narrows by N). **Proposal #6 LOW (Lapsed-deadline tracking carry-forward)** codified at i-phi/CLAUDE.md "i-phi CI guards story" with CH-16a-i-phi `066799f3` checkpoint 2026-05-21 annotation (deadline still tracked + not yet lapsed; CH-07 close is the gate; if pre-CH-07 also lapses i.e. 3rd consecutive lapse, escalate to option (c) dedicated CI chunk per chunk-retrospector v8). **CH-16a stats**: cycle hex `066799f3`; chunk-seal commit i-phi `bd41896` (24 files / +3,514 / -10); first i-phi cycle with 3 plan iterations (iter-1 full CH-16 → iter-2 narrowed via Split A USER-DIVERGENT → iter-3 §13→§1 structural restructure; all user-direction-driven); first USER-DIRECTION DIVERGENT Split-decision on i-phi (CH-16b forward-scope filed inline at i-phi `238b143`); 2 forks pre-locked DIVERGENT for CH-16b (F2.b dedicated compactor + F4.b recursive summarisation); 5 forks locked planner-rec at CH-16a (F1.a + F5.a + F6.a + F-cross-session-boundary.a + F-extraction-step.a); 167/0/1 workspace tests (+2 over plan band at pause-trigger boundary; no pause fired); clippy + fmt GREEN; cross-cluster invariant extends 5→6 cycles ratified; novel implementation pattern Arc<Mutex<EpisodeBuildState>> closure-side state-machine ratified at ADR-0016 §D16.5; same-name cross-cluster-trait ambiguity discovered at P0 (compaction::store::EpisodeStore vs sessions::resume::EpisodeStore; intentional coexistence ratified at ADR-0016 §D16.7); 4.46 GiB disk reclaimed (placement-1 3.5 GiB mid-cycle + placement-2 960 MiB at close); 0 NEW drifts at CH-16a (CH-16b forward-scope routes deferred LLM + recursive work); 0 audit re-spawns (A PASS-with-caveat + B PASS at iter-1); 0 hot allow-rule candidates (4th consecutive); 6 compaction.md OPEN closures (4 ANSWERED + 2 PARTIALLY ANSWERED with CH-16b cross-refs); ADR-0016 Accepted with 8 sub-decisions §D16.1-§D16.8 (incl. §D16.8 NEW Split-decision ratification); ADR-0007 §D7.11 partial-closure + ADR-0008 §D8.12 closure footers; net health: 6 GREEN + 2 YELLOW informational + 0 RED. Standards updates landed across `.claude/agents/chunk-planner.md` v27→v28 + `.claude/skills/chunk-initiate/SKILL.md` + `/root/projects/phi/CLAUDE.md` + `/root/projects/phi/i-phi/CLAUDE.md`.) -->
<!-- Last verified: 2026-05-20 by Claude Code (CH-08-i-phi retro `2a786a5b` standards-update batch landed (9 of 10 proposals approved across 3 user-presented batches; #10 paperwork _changelog.md entries auto-applied): **Proposal #1 HIGH (chunk-planner v26→v27 P-plan-2 cross-cluster-struct field cascade pre-flight)** — when a fork-lock body cites a cross-cluster-struct field (e.g., `HookSpec.timeoutMs`, `MergedPermissions.X`, `Session.Y`), planner MUST `grep -A 20 'pub struct <StructName>' <cross-cluster-file>` at fork-lock-draft time + verify the field exists; mismatch surfaces as planner-time gap routing via (a) widen fork-lock body to acknowledge SCOPE-NARROWING + file IMPL-DISCOVERED-style drift in plan §4 OR (b) re-scope cross-cluster touch via AskUserQuestion to user. Closes CH-08 §3 row 5 (HookSpec.timeoutMs SCOPE-NARROWING discovery-at-impl gap; auto-resolved by implementer Route-A-like inline + D-CH08-FOLLOWUP-06 filed but should have surfaced at planner-time). **Proposal #2 MEDIUM (chunk-planner v27 P-plan-1 functional-scope-derivation refinement)** — adds enum-multiplicity multiplier (+30 LOC variance for ≥9-variant enums with parse + as_str + is_* derived-method coverage) + inline-test tolerance derivation rule (≤3 inline ONLY when total enum-variants-in-file ≤6; ≥9-variant enums warrant 5-8 inline tolerance = 1 classification + 1 roundtrip + (variants/3) edge cases; per-file inline tolerance cited in plan §3.C alongside §3.B LOC cap). Closes CH-08 §3 rows 3+4 (types.rs Band 2 +16.7% deviation + inline-test count breach +5 over ceiling). **Proposal #3 MEDIUM (outer CLAUDE.md gate-2 widened-sweep regex generalization)** — phrase set generalizes from `FOLLOWUP-NN` to `\bFOLLOWUP(-[A-Z]+)?-NN\b` (captures `FOLLOWUP-NN`, `FOLLOWUP-PHICORE-NN`, future `FOLLOWUP-WASM-NN`, etc.). CH-08 cited as codification evidence. Closes §3 rows 1+2 (PHICORE-NN placeholder cite-drift that initial gate-2 widened sweep missed; Audit-B Claim 9/12 caught the residuals). **Proposal #4 MEDIUM (chunk-implementer v17→v18 R-P-SEAL drift-filing discipline)** — at the moment of authoring any cross-reference to a drift in any deliverable (ADR / phi-core-usage / user-guide / plan-archive), use the FINAL drift filename (`-01-`, `-05-`, `-06-`, `-PHICORE-01-`), NEVER the `-NN-` placeholder. Placeholder is a planner-archival staging artefact only; implementer-tier deliverables resolve at filing-time. Eliminates the placeholder class entirely from implementer-tier deliverables; pairs with Proposal #3 outer regex generalization (defense-in-depth). **Proposal #5 MEDIUM (chunk-retrospector v7→v8 lapsed-deadline detection)** — at Procedure step 1, grep `<project>/CLAUDE.md` open-question entries for deadline candidates citing the just-closed chunk (e.g., `pre-CH-08`); for each match where the cited action did NOT ship in the just-closed cycle, emit §3.5-style §"Lapsed deadlines" subsection + a §5 standards-update row escalating the lapse to user. Closes CH-08 §3 row 7 (CI-guards deadline lapse pattern; now 2-consecutive: pre-CH-05 lapsed at CH-05 + pre-CH-08 lapsed at CH-08). **Proposal #6 MEDIUM (i-phi/CLAUDE.md CI guards deadline update)** — open question "i-phi CI guards story" deadline updates from "pre-CH-08 (lapsed at CH-08 close 2026-05-20)" to **"pre-CH-07"** (CH-07 = agent factory; consumes hooks + check_permission). NEW "Lapsed deadlines history" log added tracking the pattern (pre-CH-05 lapsed at CH-05 close 2026-05-19, pre-CH-08 lapsed at CH-08 close 2026-05-20). Alternative routing (option (c) — dedicated CI chunk after M0) explicitly noted as the fallback if pre-CH-07 also lapses. Pairs with #5 to enforce non-silent lapse going forward. **Proposal #7 LOW (outer CLAUDE.md cross-cycle fork-divergence observation per-project split)** — existing aggregate callout splits into **baby-phi 14-of-19 (~74%) divergent at CH-28 close vs i-phi 0-of-12 across CH-05/CH-06/CH-08 (3 consecutive unanimous-planner-rec cycles; ~0% divergence)**. The per-project split helps chunk-planner v9+ framing: i-phi's divergence pattern is materially different from baby-phi's; treating divergence as modal across both projects is misleading for i-phi-specific planner work. **Proposal #8 LOW (permissions-audit skill v4.2→v4.3 window-mode guidance)** — adds brief guidance: "All-planner-rec single-iter-archive cycles can stay narrow-mode (default); cycles with planner re-spawn OR ≥1 gate-1 fork-divergence SHOULD use wide-mode for completeness (capture pre-archival work)." Cites CH-08 as canonical all-planner-rec narrow-mode-OK precedent + CH-05/CH-06 as wide-mode precedent for planner-re-spawn cycles. **Proposal #9 LOW (chunk-planner v27 P-plan-3 CONDITIONAL drift activation discipline codification)** — codifies the working pattern from CH-08 D-CH08-FOLLOWUP-05 (R8 event-emission) canonical first activation: (a) plan §3.E "Anticipated gate-2.5 candidates" row carries the CONDITIONAL drift placeholder; (b) §10 close criteria notes "CONDITIONAL drift filed iff P0 source-walk shows X"; (c) P-SEAL workflow files OR skips per verified branch. Positive process pattern — codify the working mechanism for future cycles touching unverified phi-core surfaces or other risk-acknowledged branches. **Proposal #10 paperwork** — this _changelog.md entry + 4 version bumps applied to chunk-planner.md (v26→v27) + chunk-implementer.md (v17→v18) + chunk-retrospector.md (v7→v8) + permissions-audit/SKILL.md (v4.2→v4.3). **CH-08 stats**: cycle hex `2a786a5b`; chunk-seal commit i-phi `e044e43` (28 files / +4,347 / -12); 9 forks all locked at gate-1 at planner-rec (zero divergence; 3rd consecutive all-planner-rec on i-phi after CH-05 + CH-06); 1 audit iteration per letter (A iter-1 PASS, B iter-1 PARTIAL → Trivial-1L resolved at gate-3 without iter-2); 10 Trivial patches across pre+post-audit sweeps (no auditor re-spawn); 152 / 0 / 1 tests (vs plan band [143, 146] — +6 over driven by 8 inline tests); 5-cycle cross-cluster invariant durability (CH-04/05/06/08 all honored; src/{daemon,identity,permissions,memory,sessions}/ + phi-core/ untouched); zero Cargo.toml diff; +5 phi-core leverage-sites within plan +3 to +5 band; 7 NEW drifts filed (5 ALWAYS + 1 CONDITIONAL ACTIVATED D-CH08-FOLLOWUP-05 event-emission + 1 IMPL-DISCOVERED D-CH08-FOLLOWUP-06 hookspec-timeout-field); ADR-0009 Accepted with 9 sub-decisions §D9.1-§D9.9 + closes ADR-0006 §D6.7; 7-of-7 hooks.md open-questions ANSWERED; ~3.85 GiB disk reclaimed cumulative (placement-1 2.9 GiB mid-cycle + placement-2 949 MiB at close); permissions-audit narrow-mode captured 238 tool calls + 0 actionable hot allow-rule candidates (4th consecutive zero-actionable; system convergent) + 0 hook false-positives + 0 cross-cycle bleed; cycle-cumulative i-phi unanimous-planner-rec locks now 19-of-19 across CH-05/CH-06/CH-08 (per-project split codified at #7); P0 source-walk discipline canonical first activation surfaced R8 (D-CH08-FOLLOWUP-05) BEFORE implementation locked in (proposal #9 codifies); 5 deviations logged at cycle-audit §6 (D-1 force-proceed split + D-2 types.rs Band 2 + D-3 inline test count +5 + D-4 HookSpec.timeoutMs SCOPE-NARROWING + D-5 R8 P0 outcome); net health: 8 GREEN (audit-pass at iter-1 + cargo-clean + leverage sites + test-band-near-target + cross-cluster invariant + cross-submodule invariant + zero-hot-candidates + zero-hook-false-positives) + 1 YELLOW-lite (10 standards-update count expected given fork-count + cross-cluster-discovery + 2-consecutive deadline lapse + retrospective regex gap) + 0 RED.) -->
<!-- Last verified: 2026-05-20 by Claude Code (CH-28 retro plan archive `chunk-decomposition-and-fork-framing-76e04080.md` user-driven process-improvement batch landed (all 7 deliverables shipped; addresses CH-28-observed gaps in chunk-decomposition + fork-framing + deferred-feature-visibility): **Deliverable 1 (per-chunk-planning-template.md)** — NEW Pre-§1 "Forks for orchestrator" section documenting fork-row format (User-visible column + Product trajectory line + TECHNICAL FORK release label); NEW §2.5 "Functional outcome" mandatory section declaring FUNCTIONAL vs TECHNICAL-PREREQUISITE chunk-type + user-visible delivery paragraph (or Unblocks line); NEW §4.A "Deferred functionality" sub-section with mandatory User-visible feature deferred + Product impact during deferral columns. **Deliverable 2 (chunk-planner v25 → v26)** — NEW P-plan-1-v26 mandatory user-facing fork framing rule (`**User-visible:**` in pros + `**Product trajectory:**` in cons; TECHNICAL FORK release label for engineering-only forks; ALWAYS-FIRE self-check loop mirroring v23 P13 pattern; rule TEXT project-agnostic — CH-28 cited as evidence in rationale only). **Deliverable 3 (phase-planner v1 → v2)** — Step 5 expanded with FUNCTIONAL / TECHNICAL-PREREQUISITE chunk-type tagging discipline (mandatory) + 30%-supporting-infra split heuristic + max-2-consecutive-TECHNICAL-PREREQUISITE chain cap; Step 7 forward-scope summary table expanded with Chunk-type + User-visible delivery columns; per-chunk §1 narrative blocks expanded with Functional outcome: + Defers (with product impact): lines. **Deliverable 4 (forward-scope row shape)** — documented in phase-planner v2 output schema (no separate file). **Deliverable 5 (chunk-initiate skill 4-line gate-1 template)** — REPLACES v23 2-line `(a) user-impact + (b) pros/cons` template with STRICT 4-line template per fork option's `description` field: **User-visible:** / **Product trajectory:** / **Cycle scope:** / **Defers (if chosen):**; 2-line release for TECHNICAL FORK-labeled forks (Cycle scope + Defers only); project-agnostic — applies uniformly to baby-phi AND i-phi. **Deliverable 6 (NEW feature-inventory.md)** — NEW canonical cross-chunk product-trajectory tracker at `baby-phi/docs/specs/v0/feature-inventory.md` enumerating every user-facing capability in v0.1 (~45 features across M0..M8) with v0 scope + closing chunk(s) + deferred sub-aspects + v0 vs final state delta; §3 deferred catalogue indexed by feature (M6-DEFERRED-04 / M6-or-M7-DEFERRED / M7-DEFERRED-01/02 / M7b-DEFERRED-01/02 / D-CH28-FOLLOWUP-01 / D-CH27-FOLLOWUP-01); §4 cross-chunk dependency graph at feature-tier granularity; §5 revisit triggers. **Deliverable 7 (m6-forward-scope retroactive re-frame)** — §5 per-chunk scope summary table extended with Chunk-type + User-visible delivery columns for all 11 chunks CH-28..CH-38; §1 per-chunk narrative blocks extended with Chunk-type: + Functional outcome: + Defers (with product impact): lines; NEW §6 Q-CHAIN-1 open question on messaging-axis 3-consecutive-TECHNICAL-PREREQUISITE chain cap deviation with phase-planner-rec (a) accept-with-parallel-band-rationale; distribution 6 FUNCTIONAL + 5 TECHNICAL-PREREQUISITE. **Project-agnostic discipline (user-reinforced 2026-05-20)**: agents + skills shared across baby-phi + i-phi + future projects; rule TEXT in chunk-planner v26 + phase-planner v2 + chunk-initiate 4-line template MUST NOT hard-code baby-phi paths; CH-28 cited only as evidence in rationale paragraphs. Self-verification grep ran at Phase 7 close: ZERO baby-phi-specific path literals in NEW rule text (pre-existing baseline hits in v15..v24 grandfathered). i-phi follow-up: i-phi will need its own equivalent feature-inventory.md at `i-phi/docs/v0/feature-inventory.md` authored at a future i-phi planning session — out-of-scope here. Plan archive landed at `baby-phi/docs/specs/plan/review-and-docs/chunk-decomposition-and-fork-framing-76e04080.md`.) -->
<!-- Last verified: 2026-05-20 by Claude Code (CH-28 retro `0412eb06` standards-update batch landed (13 of 13 proposals approved: 3 HIGH + 6 MEDIUM + 4 LOW, all user-Apply-all): **Bundle HIGH (P-plan-1 + P-plan-2 + P-orch-1)** — chunk-planner v24→v25 with HIGH-tier P-plan-1 NEW §3.F SurrealDB SCHEMAFULL Semantic Checklist (mandatory when migration ships REMOVE FIELD / ALTER TABLE narrowing / new SCHEMAFULL table / narrowing-UNIQUE-index; enumerates write-path call-site inventory + per-call-site wire-strip mitigation + phase placement + UPDATE-vs-UPSERT keyword discipline + traversal-projection `_rid` LET-VALUE refactor + half-migrated-state tolerance; cites ADR-0063 §D63.14+§D63.15 canonical pattern; closes iter-4 Architectural-FAIL #2 root cause) + HIGH-tier P-plan-2 in-process projection preservation rule codified as generalizable concept-vs-implementation reconciliation device (§D63.13 canonical: persistence-of-record moves to NEW table; in-process struct preserved with `#[serde(default)]`; read-path synthesizes via helper; write-path strips at SurrealDB boundary via WireRow intermediate; N-site cascade dissolves to ~0 sites; future M7 cleanup may flatten); CLAUDE.md outer P-orch-1 HIGH NEW SurrealDB SCHEMAFULL semantic spot-check at gate-4 final cycle re-audit (verify §3.F checklist authored + every checklist row's mitigation landed + ≥1 compound-tx site spot-check + UPDATE-vs-UPSERT grep returns zero). **Bundle MEDIUM (P-plan-3 + P-plan-4 + P-impl-1 + P-impl-2 + P-orch-2 + P-orch-3)** — chunk-planner v25 P-plan-3 latent-defect-discovery cushion in cascade-band methodology (NEW Repository methods × NEW SurrealDB table × compound-tx → budget ~2-4× cushion on predicted production LOC; CH-28 P1.5 actual 340 LOC vs predicted 80 = 4.25× overrun empirical) + P-plan-4 §7.0 phase-order stress-test pass at iter-2 plan-draft (when ≥2 of: >5 phases / ≥1 user-locked DIVERGENT fork / cascade-band overlap; walks each boundary against compile-time + runtime-test invariants + workspace-RED window length + audit envelope diff-readability); chunk-implementer v16→v17 with P-impl-1 P-SEAL verified-header sweep extension to ADRs receiving inline amendment blocks (`> Amended at CH-NN / ADR-NNNN §DN.M` triggers top-of-file CH-NN prepend verification; closes CH-28 Audit-B Claim 9/12 PASS-with-caveat on ADR-0057 verified-header miss) + P-impl-2 wire-row pattern cascade propagation check at P-FIXTURES actuals snapshot (when §D63.14-class WireRow ships AND compound-tx sites exist, grep ALL compound-tx sites for wire-row substitution propagation; CH-28 caught 4 ADDITIONAL apply_org_creation + apply_agent_creation sites beyond §D63.14's stated 3 method bodies); CLAUDE.md outer P-orch-2 MEDIUM ADR-inline-amendment verified-header cross-check at gate-3 (defensive layer paired with chunk-implementer v17 P-impl-1) + P-orch-3 MEDIUM NEW gate-1.7 cross-lock interaction stress-test for 3-of-3 DIVERGENT lock-sets (pair-wise interaction evaluation against compile-time + runtime-test + phase-ordering constraints; CH-28 F1.c × F3.b × in-process-struct interaction would have surfaced at iter-2 with this gate). **Bundle LOW (P-skill-1 + P-doc-1 + P-rename-1 + P-meta-1)** — granular-bash-discipline doc §2.8 NEW empirical-quirk record for trailing `; echo "EXIT=$?"` chained-command shape breaking cargo-test rule prefix-glob (5-prompt cluster at CH-28; recommended workaround = wrapper script `scripts/run-cargo-test-with-exit.sh`); CLAUDE.md outer + baby-phi mirror cargo-clean Empirical durability line bumped to "8+ consecutive cycles" with CH-28 8th-cycle ratification (~990+ GiB cumulative reclaimed; CH-28 alone ~390 GiB across 6 invocations); acceptance_m6_agent_profile_cardinality.rs test 4 renamed (`test_upsert_agent_profile_no_longer_deletes_sibling_rows_for_same_agent_id` → `test_upsert_agent_profile_does_not_delete_other_agents_rows`) for naming clarity (P2-ACCEPTANCE NOTE block updated inline); CLAUDE.md outer cross-cycle fork-divergence observation NEW (14-of-19 ~74% cumulative cross-cycle divergent-fork count across 10+ cycles; high-iteration-count correlation: 3-of-3 DIVERGENT lock-sets correlate with elevated plan-iteration counts; CH-28 ran 5 plan iterations + 2 Architectural-FAIL re-spawns; future cycles with 3-of-3 DIVERGENT should anticipate iteration-count tail; gate-1.7 P-orch-3 is the proactive mitigation). **CH-28 stats**: cycle hex `0412eb06`; 5 plan iterations (highest count to date; iter-1→2 3-of-3 forks user-locked DIVERGENT + iter-2→3 gate-2.5 phase swap + iter-3→4 Architectural-FAIL #1 P1-vs-P2 internal-inconsistency + iter-4→5 Architectural-FAIL #2 ADDITIVE-green-claim falsified); 0 audit-fix-loop iterations (all 3 letters A/B/C PASSed at iter-1; planner-side iteration absorbed implementation risk); 1 Trivial-1L orchestrator-applied patch (ADR-0057 verified-header prepend at gate-3 close); 2 PASS-with-caveat findings (Audit B test 4 naming + Audit C composite-write defensive extension); 26 files changed (+2607/-176); ADR-0063 Accepted with 16 sub-decisions §D63.1-§D63.16 (NEW iter-4 §D63.13 in-process projection + NEW iter-5 §D63.14 WireRow + §D63.15 read-synthesis + §D63.16 listener template-tier deferral); D-CH28-FOLLOWUP-01 filed → M6-DEFERRED-04 (CH-36); 3 latent P1 defects fixed in-flight at P1.5 (UPDATE-vs-UPSERT keyword bug + traversal-projection `_rid` alias defect + compound-tx wire-strip cascade miss across 4 sites apply_org_creation + apply_agent_creation); 2 cascade-band OVERRUNs (P1.5 actual 340 vs predicted 80 = 4.25× + P2-ACCEPTANCE actual 643 vs predicted 150-200 = 3×); cumulative cross-cycle divergent-fork count 14-of-19 (~74%); workspace 1599/0 GREEN within plan §8 band [1590, 1604]; clippy `-Dwarnings` GREEN; 4 CI guards 4/4 GREEN; phi-core leverage 56 imports (was 57 baseline; -1 variance unrelated to CH-28; no parallel implementations); EDGE_KIND_NAMES.len() 72→74 per F1.c; ~390 GiB disk reclaimed across 6 placement-1 invocations; permissions-audit skill v4.2 captured 1665 tool calls + 1 actionable hot cluster (P-skill-1 chained-command 5-prompt cluster) + 2 cross-cycle-bleed (CH-06-i-phi parallel-cycle Edit::.claude/agents/* attribution); net health: 3 GREEN (audit-pass at iter-1 + no hook false-positives + hot candidates count) + 2 YELLOW (Architectural-FAIL count + 13 standards-update count expected given 5-iteration cycle scope) + 0 RED.) -->
<!-- Last verified: 2026-05-20 by Claude Code (CH-06-i-phi retro `da221147` standards-update batch landed (12 of 13 proposals approved across 6 user-presented decisions; 1 confirmation-only): **Decision 1 (chunk-planner v23→v24)** — P-plan-1-v24 §3.B LOC-cap derivation refinement for cascade-plumbing scenarios with per-axis weights (BFS/DFS walk body ~70 LOC + Arc::new_cyclic pattern ~30 LOC + parent-child linkage refactor ~25 LOC + inter-task command-enqueue-and-reply ~40 LOC; trigger refinement when weighted-est exceeds precedent-mirror baseline by > 1.5× rather than v23 > 2×; Route B planner-level escape when ≥ 3× cap); P-plan-2-v24 ADR-label-strict-form loosening for never-shipped-yet axes (narrative paragraph stating "no prior behaviour to preserve — net-new surface" replaces strict `**Pre-existing-behaviour:**` label when fork surface is net-new at this chunk; existing surfaces still require strict label). **Decision 2 (chunk-implementer v15→v16 five-update P-SEAL self-check bundle)** — P-impl-1-v16 ADR placeholder grep (`grep -nE '§D[0-9]+\.<N>|<TODO>|<TBD>'`) closes CH-06 D5 §D8.4 literal placeholder; P-impl-2-v16 ADR Pre-existing-behaviour label-or-narrative grep validates either form present per sub-decision count; P-impl-3-v16 3-band cap-deviation lifecycle (≤1.1× silent / 1.1×-1.5× log / >1.5× pause) closes CH-06 D7 error.rs +8% framing gap; P-impl-4-v16 concept-doc annotation-form grep (`grep -nE '\[(ANSWERED|PARTIALLY ANSWERED|DEFERRED|CLARIFIED) [0-9]{4}-'`) closes CH-06 D8 sessions.md date-stamp drift; P-impl-5-v16 drift-directory canonical-path enforcement (i-phi = `docs/v0/design/drifts/`; baby-phi = `docs/specs/plan/drifts/`). **Decision 3 (outer /root/projects/phi/CLAUDE.md)** — P-orch-1 user-directed mid-cycle in-plan amendment exception class codified (plan archives normally immutable; EXCEPTION when user-direction with stated rationale; amendment block format with `> **EXCEPTION to plan-archive immutability**` header + tabular change-diff + bidirectional cross-references; CH-06 plan §3.B-A is canonical first activation); P-orch-2 post-amendment re-citation cross-check at gate-3 verifies bidirectional cite freshness (ADR ↔ amendment ↔ cycle-audit ↔ P-SEAL deviation log); Trivial-1L pre-audit patch if any cite missing or stale. **Decision 4 (chunk-initiate skill Phase 2)** — P-skill-1 Route A (deviation-acceptance) / Route B (module-split / plumbing-extraction-on-LOC-pressure) / Route C (fork-relaxation / re-vote) named routing classes at mid-implementation v15 P-impl-1 pause; orchestrator surfaces 3 named routes via AskUserQuestion with v23 fork-template (user-impact + pros/cons); selected route lands in cycle-audit §6 + ADR §DN.M + plan §X.Y-A amendment (Route B/C); CH-05 parser.rs canonical Route A precedent; CH-06 cascade.rs canonical Route B precedent. **Decision 5 (i-phi/CLAUDE.md + 5-drift migration)** — P-iphi-1 CI-guards story carry-forward ratified at CH-06 close (deadline candidate remains pre-CH-08; CH-06 closed without guards); P-iphi-2 canonical drift directory locked at `docs/v0/design/drifts/` + 5 historical stragglers from `docs/v0/proposal/drifts/` migrated to `docs/v0/design/drifts/` (D-CH04-FOLLOWUP-PHICORE-01 + D-CH05-FOLLOWUP-01/02/03 + D-CH16-PHICORE-COMPRESS-RELOAD) + `docs/v0/proposal/drifts/` directory removed + 18 docs bulk-updated for cross-ref freshness (concept-docs + design-docs + forward-scope + chunk-graph + cycle artifacts pre-CH-06). **Decision 6 (permissions-audit skill v4.2)** — confirmation-only; no change; 2nd consecutive 0-actionable-hot-candidate cycle on i-phi validates v4.2 + workflow-discipline convergence (CH-05 first, CH-06 second). **CH-06 stats**: cycle hex `da221147`; first v15 P-impl-1 activation (rule from CH-05 retro fired correctly at >2× LOC cap on handle.rs + registry.rs; pause-AT-breach worked); first Route B (module-split) precedent on i-phi (cascade.rs extraction; ratified in ADR-0008 §D8.14 + plan §3.B-A user-directed amendment); first user-directed mid-cycle in-plan deviation amendment (plan §3.B-A; new exception class to plan-archive immutability with explicit user-direction citation; codified as outer CLAUDE.md P-orch-1 + P-orch-2); 4th i-phi cycle all-PASS-iter-1 streak (CH-03 + CH-04 + CH-05 + CH-06); 2nd all-planner-rec lock-set (9 forks all planner-rec; cumulative cross-cycle divergence rate now ~21/43 = ~49%, was ~62% pre-CH-06); pre-cycle session-feature doc updates (11 files, +143 lines at commit `f38040c`) as NEW precedent for capturing architectural axes BEFORE forward-scope (3 requirements: sub-agent SessionMode + cross-agent memory harvest + interrupt-and-checkpoint flow distributed across CH-06 + CH-07 + CH-09+ + CH-16 chunk boundaries); 4-cycle ratification of chunk-planner v20 P7 cross-cluster invariant (CH-03 + CH-04 + CH-05 + CH-06 extending across Cluster A → Cluster B boundary); 128/128 workspace tests pass (97 baseline + 31 NEW MUST-SHIP within band [31, 38]; per-Tier breakdown A=4/B=7/C=4/D=6/E=5/F=5 matches plan §8 exactly); clippy `-Dwarnings` clean; fmt --check clean; cross-cluster invariant honored (zero touch of `src/identity/` + `src/permissions/` + `src/memory/`); cross-submodule invariant honored (zero phi-core diff); ADR-0008 Accepted with 14 sub-decisions §D8.1-§D8.14 (§D8.13 + §D8.14 ratify Route B cascade.rs extraction); 2 NEW drift placeholders (D-CH06-FOLLOWUP-02 eval-parallelism post-v0 + D-CH06-FOLLOWUP-03 rewind-semantics CH-09a); 8 deviations documented at cycle-audit §6 (D1 file-count axis cascade.rs NEW + D2 LOC residuals + D3 user-directed amendment + D4 drift-path ambiguity closed via Decision 5 + D5-D8 retro-surface items closed via Decision 2); 1 Trivial-1L orchestrator-applied (cycle-index sub-decision count 12→14); 0 audit re-spawns; 0 Tactical/Architectural FAIL; permissions-audit captured 746 tool calls (wide-mode; rate 4.24/min driven by mid-cycle activity + CH-28 baby-phi parallel-cycle bleed; 2 cargo:test bleed subtracted via v4.2 attribution); CH-06 → CH-07 cross-chunk API contract preservation pattern now 3-cycle (CH-04 → CH-16; CH-05 → CH-16; CH-06 → CH-07 + CH-09+ + CH-16); net health: 6 GREEN (audit-pipeline + cargo-clean + test-band + leverage + cross-cluster + cross-submodule) + 1 YELLOW (13 standards proposal density driven by 5-update v15→v16 P-SEAL bundle + 4-update first-activation feedback to v23→v24; benign per clustering analysis) + 0 RED.) -->
<!-- Last verified: 2026-05-19 by Claude Code (CH-05-i-phi retro `f7a354b6` standards-update batch landed (15 of 15 proposals approved across 5 user-presented clusters; all Recommended): **Cluster 1 (locked-fork-details always-fire bundle, 3-of-3-cycle codification)** — chunk-planner v22→v23 P-plan-3 P13 ALWAYS-FIRE upgrade with explicit retry mechanics (`grep -c "^### Locked fork details" <draft-path>` self-check + `grep -c "^#### F" <draft-path>` sub-section count match + per-subsection sentence-count check; planner retries appendix emission until all 3 grep conditions hold BEFORE returning draft); chunk-initiate skill SKILL.md Phase 1.5 fork-lock decision flow re-shaped to Step A (mandatory iter-2 planner re-spawn after fork-locks regardless of divergence — closes 3-cycle CH-03 + CH-04 + CH-05 pattern under v22; codifies standing memory `feedback_locked_fork_details_appendix.md`) + Step B (conditional material-scope-expansion gate) + Step C (architectural-refinement-at-approval-gate iter-3 re-spawn precedent — CH-05 iter-2→iter-3 asymmetric tier layout canonical); chunk-initiate skill AskUserQuestion fork-template requirement (each option's `description` field MUST include (a) high-level user-impact summary + (b) pros/cons; codifies user's standing rule at CH-05 mid-cycle); **Cluster 2 (chunk-implementer v14→v15 LOC-discipline triad)** — P-impl-1 sharpened pause-discipline at >2× LOC cap (must `AskUserQuestion` BEFORE proceeding with next sub-section; cite current LOC + cap + functional driver + propose deviation-acceptance OR module-split; closes CH-05 parser.rs 5× overrun pass-through gap); P-impl-2 deviation-log discipline at cap-to-1.5×-ceiling overruns (no pause; emit `## Deviation log` entry at P-SEAL with functional driver cited; closes file_store.rs +10.75% silent absorption); P-impl-3 P-SEAL test-count reconciliation per Tier (report actual breakdown by Tier against plan §8 predicted breakdown; flag >1.5× predicted as deviation; mirrors chunk-planner v22 P1 per-Tier §8 mandate implementer-side); **Cluster 3 (chunk-planner v22→v23 LOC-derivation + dep-cascade)** — P-plan-1 §3.B per-file LOC cap derivation from functional scope size NOT precedent-mirror baseline (~5 LOC/field + ~3 LOC/match-arm + ~15 LOC/helper + ~50-150 LOC/inverse-renderer + ~10 LOC/inline-test + 30% slack; when weighted-est > 2× precedent baseline use weighted as cap with 1-2 sentence §3.B justification; CH-05 retroactive: parser.rs cap should have been ~350-400 not 80); P-plan-2 §3 cascade-vector-B dependency-feature prediction (sub-rule (a) predict `serde` feature for third-party newtypes used in `#[derive(Serialize, Deserialize)]` blocks — e.g., `pub struct MemoryRecordId(uuid::Uuid)` + downstream derive → predict `uuid features = [..., "serde"]`; sub-rule (b) predict direct-dep promotion for non-re-exported transitive deps used in plan-listed derives — e.g., `chrono` via phi-core + `DateTime<Utc>` field → predict `chrono = "0.4"` direct add; literal `cargo tree -p <crate> | grep <dep>` pre-flight at plan §3 vector B for gate-1.5 orchestrator review); **Cluster 4 (orchestrator gate-1.5 pre-archival quartet — outer CLAUDE.md)** — P-orch-1 LOC-cap functional-scope-derivation cross-check (mirror of P-plan-1; verify plan §3.B caps diverging > 2× precedent baseline carry justification citing concrete functional axes; flag thin justification as Trivial-1L plan-edit BEFORE archive); P-orch-2 `cargo tree` dependency-cascade verification (mirror of P-plan-2; run grep against current submodule manifest for each newly-predicted direct-dep + feature; confirm transitively-exists + not-re-exported + listed in Cargo.toml diff; flag mismatches as Trivial-1L); P-orch-3 pre-archival numeric-citation cross-check (grep plan §6 carry-forward numeric claims `\d+ (permissions|identity|daemon|smoke) tests` + cross-reference against `cargo test --workspace --no-run` snapshot; flag mismatches as Trivial-1L plan-edit BEFORE archive; closes CH-05 plan §6 stale "36" → 34 permissions test count); P-orch-4 architectural-refinement-at-approval-gate routing precedent codified (when user surfaces architectural insight at gate-1.5 that materially refines LOCKED variant body — NOT fork re-vote — orchestrator routes via second planner re-spawn iter-3 scoped to affected fork bodies only; other locks keep iter-2 status; CH-05 iter-2 → iter-3 asymmetric tier layout canonical: `F-storage-layout` + `F-retrieval` + `F-write-atomicity` refined; `F-tier-types` + `F-rotation` + `F-incognito` + `F-record-id` kept iter-2; zero downstream rework, all PASS-iter-1); **Cluster 5 (cleanup + carry-forwards)** — chunk-retrospector v6→v7 P-skill-3 v6 bootstrap pre-seed retired (CH-08 baby-phi `7cbe74a4` bash-check rule broadening — 13 zero-fire cycles + CH-01 i-phi `95c96df7` `git -C` rule add — 5 zero-fire cycles BOTH past terminal-state-drop threshold; retired to "Historical retired rows" one-line note in CH-05 retro §3.5; starting CH-06 table begins empty + grows organically); settings.json P-settings-1 (CH-04 P15+P16 carry-forward — added `Edit(//root/projects/phi/.claude/agents/**)` + `Edit(//root/projects/phi/.claude/skills/**)` + MultiEdit equivalents; closes the previously-12-prompt CH-04 cluster); i-phi/CLAUDE.md P-iphi-1 (CH-04 P4 carry-forward — CI guards deadline updated `Pre-CH-05` → `Pre-CH-08` since CH-05 closed without guards via chunk-auditor v9 i-phi override). **CH-05 stats**: first all-planner-rec lock-set on i-phi (7 forks, zero divergence vs ~59% historical baseline); first zero-audit-re-spawn cycle on i-phi (all 3 auditors PASS at iter-1; LARGE envelope A+B+C parallel); 2 plan re-spawns NOT counted as audit-iter (iter-1→iter-2 appendix per chunk-initiate Step A + iter-2→iter-3 asymmetric refinement per Step C; both ratify new standing rules); 26 deliverables + 20 MUST-SHIP tests + 12 ADR sub-decisions §D7.1-§D7.12 + 3 NEW drifts (D-CH05-FOLLOWUP-01 retrieval-ranking M0-post + D-CH05-FOLLOWUP-02 rotation-summarisation post-CH-17 + D-CH05-FOLLOWUP-03 vector-store-migration-target post-v0 — NEW drift category "post-v0 architectural-evolution targets"); 1382 source LOC + 450 test LOC; 97/97 workspace tests pass; clippy `-Dwarnings` clean; cross-cluster invariant honored (zero touch of `src/daemon/` + `src/identity/` + `src/permissions/` — 3rd-cycle ratification CH-03 + CH-04 + CH-05); cross-submodule invariant honored (zero phi-core diff); 7 PASS-with-note deviations documented at cycle-audit §6 (parser.rs 5× LOC + file_store.rs +10.75% + uuid serde feature + chrono direct-dep + plan §6 stale count + inline test overcount + iter-3 architectural refinement); 335.8 MiB disk reclaimed at gate-5; permissions-audit captured 518 tool calls across wide-mode window (planner re-spawn × 2 + iter-3) with 0 actionable hot allow-rule candidates breaking the 1-per-cycle 6-cycle streak — signal: settings.json + workflow discipline converging; ~23% cross-cycle-bleed from CH-28 baby-phi parallel orchestration (3 PR singletons) — bleed signal decreasing; CH-05 → CH-16 cross-chunk API contract preservation pattern now 2-cycle (CH-04 + CH-05 both lock Cluster A primitives CH-16 consumes); chunk-planner v20 P7 cross-cluster invariant elevated from guideline to ratified empirical norm at 3-cycle ratification; CH-05 is the strongest cycle on i-phi process-maturation side — 3 framework-maturation signals concurrent: first all-planner-rec + first zero-audit-re-spawn + first zero-actionable-hot-candidate; net health: 1 RED (15-proposal density driven by 3-cycle v22 P2 codification reaching mandatory point) + 1 YELLOW (LOC pause-discipline gap closed by Cluster 2) + 7 GREEN.) -->
<!-- Last verified: 2026-05-18 by Claude Code (CH-04-i-phi retro `8a9c50ea` standards-update batch landed (14 of 16 proposals approved + 2 rejected: P15 settings.json + P8 inline-ADR-routing; user chose P9 drift-routing for security-adjacent v0 limitations): chunk-planner v21→v22 with 5 sub-updates packaged as "v22 bundle" (P13 v20-P2 locked-fork-details appendix self-check loop closes 2-of-2-cycle compliance regression — planner self-greps own draft for `## Locked fork details` heading + sub-section count = lock count before returning; P1 §8 per-Tier test-cardinality breakdown — emit Tier A=N1, Tier B=N2 ... instead of single number; closes 2-of-2-cycle test-count overshoot CH-03 14→15 + CH-04 22→38; P2 §3 proc-macro decorator prediction — grep upstream trait for #[async_trait] etc. + predict implied dev-dep set; closes CH-04 async-trait dev-dep miss at P4; P7 §5 ADR-location lookup discipline — grep prior ADR file-naming convention before proposing path; closes CH-04 plan §5 proposal/architecture/ vs design/decisions/ drift; P12 v20 P3c filesystem-event-coalescing assertion form clarification — rule is brittleness avoidance NOT specific positive-form check); chunk-implementer v13→v14 with 3 sub-updates (P6 P-SEAL typo-cascade grep — when correcting definition-site typo, grep cross-cutting docs for stale-typo sites BEFORE P-SEAL complete; closes CH-04 4-site donAsk cascade implementer-side; P9 security-adjacent v0 limitations route as NEW drifts not inline ADR notes — D-CH<NN>-FOLLOWUP-<SURFACE>-<NN>.md; user-locked routing choice over P8 inline-note alternative; P11 ADR-template codification — mental checklist for security-adjacent paths audit-trail/PII/path-handling/secret-handling/permission-decision/identity-source); chunk-auditor v11→v12 (P10 §A lock-compliance matrix per-claim granularity — each fork-lock row expands to component semantic claims; partial-implementations show as PASS(a)✓+(b)✗+(c)✓ surfacing at Audit-A not Audit-C); chunk-archive-plan skill (P14 NEW step 8 hard-assertion — refuses archive when ≥1 user-lock + no `### Locked fork details` appendix; belt-and-suspenders to chunk-planner v22 P13); outer CLAUDE.md (P3 gate-3 audit-prompt-authoring cross-check — grep each F<X>.<letter> token in audit prompt + cross-check wording matches LOCKED variant before dispatch; P5 gate-2 dynamic-pattern widened-sweep derivation — when chunk closes typo/rename/corrective-amendment, sweep extends with pre-correction literal; P16 cd-compound reminder in granular Bash discipline — even when natural to cd into submodule, use -C/-manifest-path/bash<abs> forms); i-phi/CLAUDE.md NEW `## Open questions for upcoming chunks` section (P4 — surfaces CI guards story, additionalDirectories canonicalize semantic, check_permission CH-07 wiring, PII-redaction CH-11a, D-CH04-FOLLOWUP-PHICORE-01 window, watcher-pattern codification at CH-05/CH-08); rejected: P15 settings.json Edit allow rules for /root/projects/phi/.claude/ tree + P8 inline ADR "Known v0 limitations" sentence (user picked P9 drift-routing instead); CH-04 is the strongest cycle on i-phi audit-discovery side — 0 audit re-spawns iter-1 + 1 Audit-C iter-2 confirmation re-spawn under Trivial-multi protocol; 14 fork-locks (10 top-level + 4 sub-fork) with 3 divergent (F4.b notify-rs watcher AT CH-04 + F-decision-type.c rich 5-variant + F-tool-kind.b cross-submodule resolved via §D6.PCK.c vendor-locally extension trait); user's clarifying question "cant the trait extention be done on the i-phi code base without modifying phi-core?" drove §D6.PCK.c resolution; 38 NEW tests (34 integration + 4 inline matcher unit) vs 22 plan §8 prediction (+16 overshoot); 72 workspace tests total; phi-core leverage +3 sites with +2 raw imports within ±3 tolerance; 1293 source LOC + 818 test LOC = 2111 cycle LOC; 2 LOC overruns at soft-cap (types.rs 242/180 driven by F2.a parens-grammar parser + watcher.rs 238/230) within 1.5× pause thresholds; ADR-0006 Accepted with 17 sub-decisions §D6.1-§D6.13 + §D6.W1/W2/W3 + §D6.PCK; 1 NEW drift D-CH04-FOLLOWUP-PHICORE-01 explicit-allocated "phi-core 0.8.x window OR i-phi cleanup post-M0"; cross-cluster invariant honored (0 lines diff in src/daemon/ + src/identity/); cross-submodule invariant honored (0 lines diff in phi-core under §D6.PCK.c vendor-locally lock); spec.md line 13 `donAsk` → `dontAsk` typo correction landed + cross-cutting sweep applied at gate-3 across 4 sites; ADR §D6.5 + §D6.8 inline notes for path-normalization + PII-leakage stay in place but P9 drift-routing applies from CH-05 onwards; cumulative cross-cycle divergent forks now 19-of-32 (~59%); 6th consecutive cargo-clean v9 placement-1 discipline validation; permissions-audit skill v4.1+v4.2 captured 667 tool calls + 1 actionable hot cluster (P15 deferred by user) + 1 cross-cycle-bleed (6 prompts baby-phi M6 forward-scoping bleed); CH-04 retro Row 9 watcher-pattern stabilization (8 symmetry axes CH-03 ↔ CH-04 watchers) flagged for CH-05/CH-08 codification consideration; CH-04 is the 2nd i-phi cycle where chunk-planner v20+ P2 appendix was NOT natively emitted at iter-2 draft — orchestrator added 14-subsection appendix manually post-draft — closes the 2-of-2-cycle regression at planner-tier via P13 + archive-tier via P14 belt-and-suspenders.) -->
<!-- Last verified: 2026-05-18 by Claude Code (CH-03-i-phi retro `c542648f` standards-update batch landed (11 decisions: 9 retro proposals P1-P9 + 2 user-derived P5b + P5c; user chose Apply for 10 + Defer P9 to CH-04 close): chunk-planner v19 → v20 with 4 sub-updates packaged as "v20 bundle" (P2 locked-fork-details appendix MANDATORY when ≥1 fork locked — 3-6 sentences plain-English semantics per `#### F<N> = F<N>.<letter>` subsection; P3 plan precision triad — error-variant Source-preservation policy column + LOC alt-form column when "implementer's choice" applies + filesystem-event-coalesced count assertions default to `(1..=N).contains`; P4 phi-core leverage prediction methodology — count leverage-sites not import-line counts per file, ±3 tolerance; P7 cross-cluster invariant template entry — `**CH-NN MUST NOT touch src/<other-cluster>/**` directive + audit claim wiring in Audit C / B); chunk-initiate skill 3 additions (P1 Phase 1.5 gate-1 fork-lock decision flow — re-spawn-planner-as-default when divergent locks materially expand scope ≥+5 deliverables OR envelope tier bump; P5c NEW Phase 8 temp-folder cleanup with backup-only-if-deemed-necessary clause; P8 Phase 5→6 transition gate MANDATORY before retrospector dispatch); phi-core-leverage-check skill P4 methodology propagation; permissions-audit skill v4 → v4.1 with `window_mode = narrow | wide` parameter (resolves window-truncation when planner re-spawn workflow fires); settings.json 3 allow rules (P5 cargo-tree 2 lines + P5b `Read(//tmp/**)` 1 line); CH-03 is the strongest audit-side cycle on i-phi to date — 0 audit re-spawns + 35/35 first-iter PASS across LARGE envelope (A 14/14 + B 12/12 + C 9/9); 10 top-level forks user-locked at gate-1 with 3 divergent (F3.b full 3-tier merge + F4.b notify-rs watcher + F-empty-dir-fallback.b strict NoLayersFound) — FIRST cycle where planner re-spawned at gate-1 vs force-proceed; 4 sub-forks all locked at planner-rec at gate-1.5 (§D5.14.b REPLACE + §D5.15.a 250ms-debounce + §D5.16.a Linux-only-noop-warn + §D5.17.a callback-closure); 22 deliverables across 6 phases; 15 MUST-SHIP tests + 0 MAY-COVER = 34 workspace tests total (band [31, 38]); ADR-0005 with 17 sub-decisions §D5.1-§D5.17 (largest i-phi ADR; Divergent-cycle Forks-header form); 7/7 identity-composition.md open-questions ANSWERED (no DEFERRED); 0 follow-up drift files filed under iter-2 eager-closure lock; `src/daemon/` untouched (F4.b watcher pure-library invariant); 2.8 GiB disk reclaimed at gate-5; cross-cycle divergence tracker advances combined window 8-of-11 → 9-of-12 cycles + cumulative forks 14-of-22 → 16-of-22 (73%); F4.b watcher implemented Linux-only #[cfg]-gated with 250ms hand-rolled debounce + mpsc + Drop joins thread + zero shared mutable state; new feedback memory `feedback_pause_before_retrospector.md` saved mid-cycle codifies P8; sets Cluster A precedent for parallel CH-04 + CH-05 + CH-08; P9 deferred to CH-04 close.) -->
<!-- Last verified: 2026-05-17 by Claude Code (CH-26 retro `d1cb9e1f` standards-update batch landed (4 bundles / 8 rows; user chose Apply Bundle A + B + C + D): Bundle A chunk-planner v17→v18 with 2 sub-updates (R1 handler-refactor cascade CheckContext-build cost addendum to §3 cascade-discipline — ~1.5-2 ed per handler with 4-component decomposition; R2 composite-resource extension fork-pattern reference table — 3 paths a/b/c with effort + pros/cons matrix); Bundle B chunk-implementer v11→v12 with 2 sub-updates (R3 cardinality-reference cascade grep at P-SEAL — catches `[N] Composite/Fundamental/EdgeKind/Action variants` references in `_concept-audit-matrix.md` post-cardinality flip; R4 section-anchor sync cascade grep at P-SEAL — catches `#composite-classes-N` cross-refs post-anchor flip; both close CH-26 Audit-B Side Observations 1+2); Bundle C CLAUDE.md gate policy (R5 session-interrupt mid-audit kill detection + safe re-dispatch in Audit-fix loop section — re-dispatch NOT counted as iteration per CH-26 Audit-A re-dispatch precedent; R6 in-M5 carve-out vs M6-DEFERRED routing decision criteria in gate-5 retrospective sub-section — codifies CH-25 F-D59.2/F-D59.3 M6-DEFERRED vs CH-26 → CH-27 in-M5 carve-out routing precedents; both mirrored to baby-phi/CLAUDE.md); Bundle D settings.json (R7 + R8 already-applied — `Bash(jq *)` line 57 + `Bash(tail *)` line 73 covered pre-cycle; no new edits); CH-26 closes drift D-philosophy-02 (HIGH-A at load-bearing semantic axis; advisory-only F1.b refactor + CH-27 carve-out for blocking-gate tightening per user routing); cargo test 1568/0/2 within plan §8 v2 band [1568, 1574] at lower bound; phi-core baseline 57 preserved (Δ +0); 2 within-cycle divergent forks F1.b + F2.b (first cycle with BOTH gate-1 forks divergent); cumulative cross-cycle divergent forks now 10-of-12 (~83%); 2 orchestrator-applied trivial patches at gate-3 (Trivial-1L on `_concept-audit-matrix.md:25` Composite cardinality + Trivial-multi on `permissions/01-resource-ontology.md:189` enumeration + anchor); ADR-0061 Accepted with 7 sub-decisions §D61.1-§D61.7 (§D61.5 + §D61.7 amendments document advisory-only revision + CH-27 carve-out); NEW drift D-CH26-FOLLOWUP-01 filed (LOW Bucket-B; Closing chunk CH-27 NOT M6-DEFERRED per user routing); 6th consecutive cargo-clean v9 placement-1 discipline validation (~400+ GiB cumulative reclaimed across cycle invocations with NO disk-pressure incident); permissions-audit skill v4 2nd-cycle re-validation PASSED (1863 valid entries vs 2302 naive grep, 19.1% delta explained by parallel i-phi cycle telemetry + denser heredoc body rejection); session-interrupt mid-audit kill on Audit-A re-dispatched cleanly per new CLAUDE.md Row 5 protocol; M5.3 carve-out scope: 3-chunk {CH-25, CH-26, CH-27}; M6 plan-open waits on CH-27 close.) -->
<!-- Last verified: 2026-05-16 by Claude Code (CH-25 retro `1e01618e` standards-update batch landed (3 entries below; user chose Apply Bundle A R1+R3 + Bundle B R2+R4 + Row 6 + Defer Row 5 to CH-26 with phi-core 0.7.0 crates.io migration context noted): chunk-planner v13→v14 with 2 sub-updates (R1 handler-gating verification at plan-draft — when plan claims scenario exercises check_permission via handler Y, planner MUST grep -n check_permission in Y at plan-draft time; if absent, re-frame as engine-level test BEFORE plan-locking; closes CH-25 Audit-C #6 PARTIAL on ADR-0060 §D60.4 load-bearing-form re-interpretation; R3 P0 pre-flight phi-core HEAD delta enumeration — at chunk-open run `git -C phi-core log --oneline <last-close>..HEAD` to enumerate phi-core API surface changes since last cycle, classify each as API-additive/breaking/internal, plan §7 P0 deliverable adds explicit carrier-fix task for each impact; NEW pattern from CH-25's `response_format: ResponseFormat::default()` carrier-fix at launch.rs:567 — phi-core HEAD `d6f6998` 0.7.0 structured-output feature); chunk-implementer v8→v9 with 2 sub-updates (R2 P-SEAL paperwork checklist explicit item: prepend NEW verified-header line at TOP of _cycle-index.md describing chunk-seal — two-step verification at chunk-close to prevent recurrence; closes Audit-B Trivial-1L; R4 canonical-script-reuse mandate strengthening — before authoring any new audit-tmp-*.sh script, FIRST `ls audit-tmp-*.sh` + adapt invocation if existing script covers need; canonical script set documented at scripts/audit-tmp-cargo-counts.sh); chunk-auditor v7→v8 (R4 mirror — same canonical-script-reuse strengthening discipline for auditors); settings.json (Row 6 — NEW allow rule `Bash(RUSTFLAGS="-Dwarnings" /root/rust-env/cargo/bin/cargo build *)` closes 7-prompt hot cluster from permissions-audit skill v4 first-cycle empirical report); CH-25 closes drift D-philosophy-01 (HIGH-A; first M5.3 carve-out chunk; Agent-as-creator-and-owner of Org/Project via NEW Edge::Owns variant + OwnedResourceId enum + synth-owner-grant rule in step_2_resolve_grants + ADR-0060 Accepted with 6 sub-decisions §D60.1-§D60.6 ratified including F1.b USER-DIVERGENT lock); 1 within-cycle divergent fork (F1.b NEW Edge::Owns on closed-set Edge enum); cumulative cross-cycle divergent forks now 8-of-10 (80%); 2 orchestrator-applied trivial patches (1 Trivial-1L on cycle-index verified-header per Audit-B + 1 Trivial-multi on ADR-0060 §D60.4 load-bearing-form re-interpretation per Audit-C); R5 carry-forward from CH-24 cleanly resolved via P-R5-INVESTIGATE — permissions-audit skill v3→v4 single-`select(...)` form at line 31 folds `.version == 1` into time-window predicate; skill v4 first-cycle empirical validation PASSED (1689 valid entries for CH-25 window vs 1813 raw lines — 124-line delta accounted for by `fromjson?` rejecting malformed entries + cycle-window exact-cutoff); workspace-health carrier-fix at launch.rs:567 for phi-core 0.7.0 API integration; cargo test 1556/0/2 within plan §8 v2 band [1545, 1555] ceiling +1 within ×1.30 buffer (+19 net new tests vs CH-24 baseline 1537); phi-core import baseline preserved at 57 (Δ +0); Audit-A 24/24 PASS clean; Audit-B 14/15 PASS (1 Trivial-1L); Audit-C PARTIAL 7/8 (1 Trivial-multi); v8 placement-1 cargo-clean discipline validated 5th consecutive cycle (~370+ GiB cumulative reclaimed across cycle invocations with NO disk-pressure incident); Row 5 concurrent-cargo-test contention policy DEFERRED to CH-26 per user routing — phi-core 0.7.0 breaking change context provided + crates.io migration suggestion forwarded as architectural decision for M6 plan-open or dedicated M5.3 chunk; first cycle to ship workspace-health carrier-fix for phi-core API evolution; first cycle to validate permissions-audit skill v4 empirically.) -->
<!-- Last verified: 2026-05-11 by Claude Code (CH-24 retro `5778bb77` standards-update batch landed (1 omnibus chunk-planner v12→v13 entry below; user chose Apply R1+R2+R3+R4+R6 + Defer R5 to CH-25 + R4-housekeeping inline patch): chunk-planner v12→v13 with 5 sub-updates (R1 closed-set audit-prompt verification; R2 source-map PRODUCTION-vs-TEST-FIXTURE classification + enum-string verification; R3 struct-placement dependency-direction verification; R4 drift M*-DEFERRED-NN allocation requirement; R6 gate-2.5 mid-cycle scope-expansion lane formalization + v9 surfacing-not-suppressing re-evaluation at 78% cumulative divergence); per-chunk-planning-template §3.E NEW section "Anticipated gate-2.5 candidates" + §4 non-terminal drift M*-DEFERRED-NN rule; D-new-28 R4-housekeeping 1-line patch (`CH-19 (+ M6 review)` → `M6-DEFERRED-01`); forward-scope §2.5 CH-25 carry-forward investigation for permissions-audit skill bug (R5 deferred — log is healthy with 1290 entries from 2026-05-11; skill query mechanism returns 0 for cycle-window window-filter; needs separate investigation at CH-25 retro); CH-24 closes drift D-CH24-recent-sessions-api-flip (MEDIUM; in-cycle scope expansion: C-M5-3 API-surface flip closed via P-FLIP-RECENT-SESSIONS + ADR-0059 with LIMIT 10 / dedicated repo method `list_recent_sessions_for_project` / `RecentSessionEntry` 6-field shape; 7th field `started_by_display_name` deferred to follow-up chunk per ADR-0059 §D59.3-FOLLOWUP); 3 within-cycle divergent forks (F1.B + F-D59.2.b + F-D59.3.b — first cycle to multiply diverge); cumulative cross-cycle divergent forks now 7-of-9 (78%); 4 successive plan re-spawns v1→v2→v3→v4 (most plan revisions in single chunk); first cycle to scope-expand mid-flight via P-NEW-TESTS authoring finding (gate-2.5 user-lock to close in-chunk vs file follow-up drift); milestone tag `v0.1-m5` staged at `milestone-tag-staging.md` for user-managed execution; Audit-A 23/25 PASS (2 sandbox-blocked + 1 cargo-test-in-flight closed at gate-4 = 25/25); Audit-B 12/12 PASS clean; Audit-C PARTIAL 4 PASS + 4 PARTIAL of 8 (PARTIALs are orchestrator-audit-prompt-language overreach NOT impl defects; closed by R1); 0 Trivial-1L/Trivial-multi orchestrator inline patches; v8 placement-1 cargo-clean discipline empirically validated 4th consecutive cycle (~360 GiB cumulative reclaimed across 6 within-cycle invocations with NO disk-pressure incident); permissions-audit pipeline opaque this cycle due to skill query bug — telemetry log itself healthy.) -->
<!-- Last verified: 2026-05-10 by Claude Code (CH-20 retro `240616a4` standards-update batch landed (2 entries below; user chose Apply both Rows 1 + 2): chunk-planner v11→v12 (Row 1 — forward-scope drift-count pre-flight verification rule for ratification chunks; Row 2 — cross-cycle divergence note ELEVATED from v10 inline-prepend to prominent gate-1 callout at top of `## Forks for orchestrator` as fenced admonition block); CH-20 closed as 2nd consecutive clean cycle close (with CH-19): 0 sub-agent FAILs (Audit A 8/8 + Audit B 14/14 first-iter PASS), 0 Trivial-1L/Trivial-multi orchestrator inline patches, 0 new follow-up drifts; 16 Bucket C convention drifts ratified `discovered → accepted-as-is` via consolidated ADR-0058 + 5 NEW convention-docs at NEW `v0/conventions/` peer tier per F1.B user-lock at gate-1 (DIVERGENT from planner v1 F1.A single-consolidated-file recommendation); F1.B user-lock divergence pattern now 4-of-6 cycles (CH-15 + CH-17 + CH-18 + CH-20 diverged; CH-19 lone non-diverger) — pattern is the modal outcome; CH-19's "doc-only chunks reliably avoid divergence" hypothesis FALSIFIED; cargo-clean v8 placement-1 discipline empirically validated for 2nd consecutive cycle (~316 GiB reclaimed across 5+ within-cycle invocations with NO disk-pressure incident); auditor canonical-script-reuse mandate v7 validated for 2nd consecutive cycle (zero orphan scripts); pre-existing-behaviour preservation v11 formula relaxation exercised at ADR-0058 (6 strict + 2 (b) multi-milestone-pattern + 2 (c) never-shipped-yet); permissions-audit fully stable for 5th consecutive cycle (336 events; 0 hot allow-rule candidates; lowest count since CH-15); NEW pattern surfaced: forward-scope row mid-cycle amendment via META sub-decision §D58.10 (codified as Row 1).) -->
<!-- Last verified: 2026-05-10 by Claude Code (CH-19 retro `2c520ba7` standards-update batch landed (1 entry below; user chose Apply Row 1 — pre-existing-behaviour preservation note formula relaxation for deferral-ratification + multi-milestone-pattern sub-decisions); chunk-planner v10→v11 (3 documented variations now permitted: deferred-scope / multi-milestone-pattern / never-shipped-yet); per-chunk-planning-template §5 deliverable 3 relaxed (canonical rule text updated); CH-19 closed cleanest cycle since CH-15: 0 user-locked forks at gate-1 (Direct-approval-clean), 0 sub-agent FAILs (Audit A 8/8 + Audit B 14/14 PASS clean first iteration), 0 Trivial-1L/Trivial-multi orchestrator inline patches, 0 new follow-up drifts; 10 Bucket B drifts ratified `discovered → accepted-as-is` via consolidated ADR-0057 (D5.2/D5.3/D6.3/D7.2/D7.4/D7.5/D-new-21/D-new-25/D-new-27/D-new-30); F3.B-style user-lock divergence pattern broken at 1-of-1 doc-only cycle (validates planner v10 cross-cycle divergence rule applies primarily to code-touching chunks); cargo-clean v8 placement-1 discipline empirically validated: ~324 GiB reclaimed across 5 within-cycle invocations (P1+P3+Audit A+Audit B+gate-4) with NO disk-pressure incident (refines CH-18 retro Row 1 USER DIRECTIVE 2026-05-10); auditor canonical-script-reuse mandate (v7) validated (zero orphan scripts created); permissions-audit fully stable for 4th consecutive cycle (349 events; 1 hot candidate classified informational chunk-specific not workflow-routine; bash-check + cargo-test 4-stage clusters at resolved-via-workaround for 4th consecutive cycle).) -->
<!-- Last verified: 2026-05-10 by Claude Code (CH-18 retro `c77937bc` standards-update batch landed (5 entries below; user chose Apply-all-5 + Option B for Row 2); chunk-implementer v7→v8 (immediate-post-test cargo-clean discipline added per Row 1 USER DIRECTIVE 2026-05-10); chunk-auditor v6→v7 (canonical-script-reuse mandate + immediate-post-test cargo-clean per Rows 1+5); chunk-retrospector v3→v4 (immediate-post-script cargo-clean after permissions-audit per Row 1); chunk-planner v9→v10 (cross-cycle user-lock-divergence cross-reference rule per Row 2 + pre-flight grep precision for "definitionally redundant" claims per Row 3 + cascade-grep extension to wire-mapping functions per Row 4); CLAUDE.md root + baby-phi mirror gate-5 cargo-clean refined to TWO-placement discipline; per-chunk-planning-template §"Chunk-close paperwork" cargo-clean subsection refined to TWO-placement; orphan `scripts/audit-tmp-cardinality.sh` deleted; CH-18 closes drift D-new-12 (MEDIUM; AuthRequest per-state ACL enforcement shipped via 17 production callsites + Alerted-class audit-event); 2 new follow-up drifts filed (D-CH18-FOLLOWUP-01 admin/auditor role-discrimination M6+ per F3.B.role.b; D-CH18-FOLLOWUP-02 adopt.rs submit-side wiring M6+ per F3.B.create-side.a structural mismatch); F3.B DIVERGENT user-lock pattern now 3-of-4 cycles (CH-15 + CH-17 + CH-18 all diverged from planner-recommendation at gate-1); mid-cycle disk-pressure incident at gate-4 self-recovered via user-directed kill + cargo clean (151 GiB reclaimed); permissions-audit fully stable for 3rd consecutive cycle (820 events; 0 hot allow-rule candidates).) -->
<!-- Last verified: 2026-05-09 by Claude Code (CH-17 retro `40c4d759` standards-update batch landed (7 entries below); USER REQUESTED chunk-seal cargo-clean codification; chunk-implementer v6→v7 (cycle-index enumeration MANDATORY + cargo-clean at chunk-seal + MUST-SHIP-tests-blocking rule); chunk-planner v8→v9 (§3.D rule strengthened with mandatory pre-claim grep + concept-doc verbatim cite — 2-cycle pattern with CH-15; phi-core multi-site Δ ≥ N anticipation); per-chunk-planning-template §3.C amend-don't-add precedence + §8 buffer ceiling ×1.20 → ×1.30 on Artifact-C cycles + §8 MUST-SHIP/MAY-COVER split + P-seal cycle-index row + P-seal disk-reclamation; CLAUDE.md root + baby-phi mirror chunk-seal disk-reclamation step; CH-17 closes drift D7.1 (HIGH; SSE tail endpoint shipped, advisory-only deferred-tail retired); 0 new follow-up drifts (3rd in 6); CHK8S-D-10 filed (HIGH; M7b cross-pod live-event fan-out via Redis pub/sub); cargo-clean reclaimed 107 GB at chunk-seal; bash-check + cargo-test 4-stage clusters: 0 prompts (2nd consecutive 0-prompt cycle — both clusters dropped from cross-cycle tracking).) -->
<!-- Last verified: 2026-05-08 by Claude Code (CH-15 retro `c3f46f17` standards-update batch landed (8 entries below); chunk-implementer v5→v6 (mirror tool-specific absolute-path forms verbatim from CLAUDE.md); chunk-auditor v5→v6 (same mirror); chunk-planner v7→v8 (forward-scope-vs-concept-doc precedence detection + critical-fork escalation rule); permissions-audit skill v2→v3 (resolved-via-workaround terminal state for matcher-bug-confirmed clusters); per-chunk-planning-template §3 (typed-multi-value cascade pause-threshold sizing + canonical phi-core grep) + new §3.D (forward-scope-vs-concept-doc precedence rule); CLAUDE.md root + baby-phi mirror (widened gate-2 doc-sync sweep grep set to ALL m*/architecture + m*/operations + m*/user-guide files with canonical stale-narrative phrase set); granular-bash-discipline §2.7 added with CH-15 5th-cycle validation (bash-check cluster downgrades matcher-bug-confirmed → resolved-via-workaround empirically — 0 prompts validated); 0 new follow-up drifts; CH-15 closes drift D4.1 (HIGH; advisory-only Permission Check retired); 5/5 forks user-locked; F3.A first-ever forward-scope-vs-concept-doc re-interpretation precedent.) -->
<!-- Last verified: 2026-05-08 by Claude Code (CH-14 retro `5803bb94` standards-update batch landed (12 entries below); chunk-implementer v4→v5 (chunk-seal ADR↔drift cross-check + scope-narrowing-must-escalate); chunk-planner v6→v7 (PartialEq/Eq derive pre-check); permissions-audit skill v1→v2 (matcher-bug-confirmed tier at ≥ 3 cycles); per-chunk-planning-template §3 (migration-number reservation + CascadeResult typed-multi-value precedent) + §5 ADR-body checklist (pre-existing-behaviour preservation note); CLAUDE.md root + baby-phi mirror (gate-2 doc-sync sweep + 4-stage cardinality-extraction antipattern); settings.json bash-check cluster replaced 2 broadened-glob rules with 5 literal-script-name rules (PENDING empirical validation per matcher-bug-confirmed escalation protocol); granular-bash-discipline doc §2.6 added (4-cycle bash-check data series + matcher-bug-confirmed escalation procedure); NEW `scripts/audit-tmp-cargo-counts.sh` for gate-4 cargo-test cardinality-extraction; D-CH14-FOLLOWUP-02 closed in-cycle at gate-2 inline correction.) -->
<!-- Last verified: 2026-05-08 by Claude Code (CH-08 retro `7cbe74a4` standards-update batch landed; chunk-planner v5→v6; chunk-auditor v4→v5; chunk-retrospector v2→v3; per-chunk-planning-template §3 cascade-discipline extended; settings.json bash-check rule broadened mid-cycle by user; granular-bash-discipline doc §2.5 added (3-cycle bash-check cluster history + matcher-semantics hypothesis + literal-script-name workaround); permissions-audit skill §B regression-protection escalated to matcher-semantics-investigation at ≥ 2 cycles; 8 entries below). -->
<!-- Last verified: 2026-05-07 by Claude Code (CH-07 retro `cc912d07` standards-update batch landed; chunk-planner v4→v5; per-chunk-planning-template + forward-scope row CH-07 + settings.json + granular-bash-discipline doc + permissions-audit skill all amended; 7 entries below). -->
<!-- Last verified: 2026-05-02 by Claude Code -->

# Agent + skill changelog

Records every prompt or procedure change to files under `/root/projects/phi/.claude/agents/` and `/root/projects/phi/.claude/skills/`. Each entry cites the source retrospective + the gap it closes.

Format: `YYYY-MM-DD | target | from-version → to-version | source retrospective hex | summary`.

## Entries

| 2026-05-24 | `chunk-implementer.md` (v19 → v20) + `chunk-auditor.md` (v12 → v13) + `chunk-retrospector.md` (v7 → v8) + `/root/projects/phi/CLAUDE.md` (chunk-order.md early-flip codification + gate-3 method-signature paraphrase cross-check extension) + `/root/projects/phi/i-phi/CLAUDE.md` (CH-07b open-questions entry RESOLVED → ARCHIVED) | 6 Apply / 4 Defer / 0 Reject standards-update batch | CH-07b-i-phi retro `283d3949` proposals #2 + #3 + #4 + #6 + #8 + #9 (Apply per retrospector + user-approval); #1 + #5 + #7 + #10 (Defer per retrospector + user-approval) | **CH-07b-i-phi standards-update batch (joint-convergence second half: Cluster C compaction wiring + sub-agent mode-selection + harvest API + interrupt receiver + resume-from-checkpoint — 219 workspace tests / +25 vs baseline, ADR-0010b shipped with 9 sub-decisions, 2 NEW drifts filed, LARGE envelope 3 auditors, 1 Trivial-multi orchestrator-applied patch at gate-3 closing Audit C iter-1 Claim 5 FAIL, 2nd consecutive unanimous-planner-rec i-phi cycle).** **(Proposal #2 — outer CLAUDE.md chunk-order.md early-flip codification)**: amended "chunk-order.md update at retro-close (i-phi only)" paragraph to ratify P-ADR/P-DOCS early-flip as canonical-with-Phase-7-fallback. 4-cycle empirical precedent: CH-26 + CH-27 (baby-phi) + CH-07a + CH-07b (i-phi). Closes cycle-audit §6 D-3 + Audit B Claim 17 PASS-with-paperwork-timing. **(Proposal #3 — chunk-implementer v19 → v20 P-impl-1-v20 method-form-deliverable self-check)**: when plan §8 phase deliverable cites `<TypeName>::<method_name>(...)`, implementer at phase boundary MUST verify both `fn <method_name>` grep AND `impl <TypeName>` grep return ≥ 1 hit AND function body lives INSIDE one of those impl blocks. Failure surfaces as phase-commit blocker. CH-07b evidence: forward-scope item 10 + plan §8 P-HARVEST deliverable 2 + ADR-0010a all called for `AgentHandle::harvest_from_subagent_session(...)` method; implementer shipped only free function; orchestrator applied 37-LOC Trivial-multi delegate at gate-3 commit `56e53fa`. **(Proposal #4 — chunk-auditor v12 → v13 Audit-A interface-contract claim)**: when audit prompt cites `<TypeName>::<method_name>(...)`, Audit A's claim list MUST include dedicated interface-contract claim verifying (a) fn def exists + (b) impl block exists + (c) fn lives INSIDE impl block (NOT just shipped as similar-named free function) + (d) signature matches plan literal. Closes the method-vs-free-function interface-drift class at the code-correctness audit lane (was surfacing only at cross-cluster Audit C). **(Proposal #6 — outer CLAUDE.md gate-3 method-signature paraphrase cross-check extension)**: 4th axis on i-phi (after F-token wording + lock-body paraphrases + test-name allocation). When audit prompt cites method signature with arg list, cross-check arg names + return type against plan §X / forward-scope literal. CH-07b evidence cycle-audit §6 D-8: Audit C iter-1 prompt cited 2-arg form; actual delegate landed 4-arg. **3-layer defense** for the method-form drift class via proposals #3 (implementer-side P-IMPL) + #4 (auditor-side Audit A scaffold) + #6 (orchestrator-side gate-3 cross-check). 5th-is-the-skill threshold remains active for consolidation proposal #7 (Deferred). **(Proposal #8 — chunk-retrospector v7 → v8 regression-protection precedent citation)**: 1-line note codifying empirical confirmation that CH-07a 6-rule settings.json update closed 72→5 PermissionRequest collapse + restored 0-hot-candidate posture within 1 cycle. Reference precedent for future hot-cluster proposal narrative — structural workflow shifts can disrupt 0-hot-streak BUT next-cycle rule-fix application restores cleanly. Also folds CH-08-i-phi proposal #5 lapsed-deadline detection (already in-procedure but missing from header) into v8 trail. **(Proposal #9 — i-phi CLAUDE.md CH-07b open-questions entry RESOLVED → ARCHIVED)**: flipped entry header from "ALL CARRY-FORWARD QUESTIONS RESOLVED" to "ARCHIVED"; added codification of the 3 within-lock scope-narrowings codified at ADR-0010b §D10b.2/§D10b.4/§D10b.6. Cleanup paperwork to clear active open-questions for CH-09 first-consumer carry-forward. **(Deferred proposals)**: #1 chunk-planner §4 leverage-site naming precision (apply at next chunk-planner pass); #5 gate-3 per-file change-description paraphrase extension (subsumed by #7 consolidation); #7 audit-prompt-cross-check.sh consolidation (wait for 5th axis trigger); #10 chunk-planner §3.E within-lock-scope-narrowing template entry (apply at next chunk-planner pass). **Cycle context**: 12 phase commits + 1 gate-3 Trivial-multi + 1 gate-4 cycle-audit + 1 retro standards-update + 1 plan-archive prereq + outer phi submodule bump = 16 commits on i-phi/dev + 1 on outer/dev. Audit pipeline: A iter-1 PASS (15/15) + B iter-1 PASS (22/22) + C iter-1 PARTIAL (18/19; Claim 5 FAIL) → orchestrator Trivial-multi `56e53fa` → C iter-2 PASS (19/19). 6 forks ALL LOCKED at planner-rec (Direct-approval-clean; zero divergence — 2nd consecutive i-phi unanimous-planner-rec cycle; cumulative divergence 2-of-28 (~7%) post-CH-07b close). Cross-cluster invariant extends 8→9 cycles. phi-core untouched. CH-07b unblocks Surface-1 group (CH-09 / CH-10 / CH-11a / CH-17 — parallel from this point per chunk-order.md §3 G-post-CH-07b active group). Cargo-clean placement-1 reclaimed 3.3 + 3.8 GiB; placement-2 at gate-5 reclaimed 3.8 GiB; ~10.9 GiB cumulative reclaim. permissions-audit captured 583 tool calls (4.32/min within historic band); 0 hot allow-rule candidates (regression-protection success empirically confirmed; codified at proposal #8). 8 deviations logged in cycle-audit §6 (D-1 Force-proceed split + D-2 Trivial-multi closure + D-3 chunk-order.md early-flip-codification-trigger + D-4/D-5/D-6 within-lock scope-narrowings codified at ADR-0010b sub-decisions + D-7 leverage-site paraphrase precision + D-8 audit-prompt-authoring miss method-signature paraphrase — surface for proposal #6). 0 Tactical/Architectural FAILs; 1 Trivial-multi closed cleanly at iter-2; 0 Trivial-1L patches. Audit re-spawn count: A=0, B=0, C=1 (Trivial-multi-driven). |
| 2026-05-23 | `chunk-implementer.md` (v18 → v19) + `chunk-auditor.md` (v12 → v13) + `chunk-initiate/SKILL.md` (Three-update bundle: P-orch-3 mandatory + gate-3 audit-prompt-test-allocation cross-check + gate-5 paperwork-sweep step) + `/root/projects/phi/CLAUDE.md` (cross-cycle fork-divergence observation second-axis update + plan-iteration-count cross-cycle pattern 3-of-3-confirmed update) + `/root/projects/phi/.claude/settings.json` (4 NEW i-phi CI guard rules + 2 NEW phi-core cargo-registry rules) | omnibus 10-decision standards-update batch (10 Apply + 1 Defer) | CH-07a-i-phi retro `5384684d` proposals #1+#2+#3+#5+#6a+#6b+#7+#8+#9+#10 (all Apply per retrospector recommendation; #4 chunk-planner phi-core surface signature spot-check Deferred per retro rec) | **CH-07a-i-phi standards-update batch (first joint-convergence cycle on i-phi; agent factory Cluster A wiring + daemon runtime + Tier P USER-DIVERGENT CI guards carve-out shipped — 194 workspace tests, 4 drifts filed, 4 CI guards green in sub-agent audit sandbox for first time at i-phi, 0 audit re-spawns).** **(Proposals #1 + #5 — chunk-implementer v18 → v19 two-update bundle)**: **(P-impl-1-v19 Lock-body wire-consumption self-check at P-SEAL)** — for EACH fork in plan §1 Locked-fork-details with a Code-level binding sentence citing a struct field / helper fn / wire call-site, implementer at P-SEAL MUST grep + verify (a) wire body invoked locked helper/method at cited call-site; (b) build path instantiated locked field; (c) if either "no" → surface as P-SEAL scope-narrowing with ADR §D<N>.<M> cite. Closes CH-07a §3 D-5 (`AgentHandle.identity_watcher: None` at build despite F-watcher-scope.a lock body citing per-session instantiation) + D-6 (`canonicalize_for_check` helper shipped per F-additionalDirectories-canonicalize.a but NOT wire-consumed in `build_before_tool_execution_with_permissions`). Both audit-discovered scope-narrowings escaped the implementer's 4-deviation P-SEAL list at audit-a-iter1 v12 sub-claim granularity. **(P-impl-2-v19 ADR sub-decision partial-application carve-out at P-DOCS)** — when a sub-decision lock body says "implementation realises X" but shipped behaviour partially-applies X (helper exposed but no wire-time consumer / field declared but always-None at build / Pre-existing-behaviour preserved instead of invoked), sub-decision body MUST explicitly carve out partial-application as a Pre-existing-behaviour-preservation-note variant. CH-07a evidence: ADR-0010a §D10.4 documents `canonicalize_for_check` helper + rationale but does NOT explicitly carve out wire-non-consumption (D-6); stricter reading would FAIL sub-claim. Auditor A recommended exactly this remediation inline. **(Proposal #9 — chunk-auditor v12 → v13)**: **EXECUTE for i-phi post-CH-07a CI guards** — removed the stale "skip for i-phi" permanently-skip-CI-guards project-context-override; replaced with "EXECUTE for i-phi post-CH-07a per F-iphi-ci-guards-deadline.b USER-DIVERGENT lock at CH-07a cycle `5384684d`; ADR-0010a §D10.13. Run the 4 `bash /root/projects/phi/i-phi/scripts/check-{doc-links,verified-headers,phi-core-reuse,spec-drift}.sh` guards in your audit sandbox. Each must exit 0 for the chunk to seal. Mark `NOT-EXECUTED-IN-AUDIT` only if your specific sandbox blocks bash execution (sandbox limitation, not project policy)". Closes CH-07a Pattern 5 (first i-phi cycle without `NOT-EXECUTED-IN-AUDIT` markers for guard-shaped claims) + Audit B claim 12 + cycle-audit.md §3 MUST-RUN evidence. **(Proposals #2 + #3 + #10 — chunk-initiate/SKILL.md three-update bundle)**: **(Update #1 P-orch-3 mandatory)** — mandatory P-orch-3 pre-archival numeric-citation cross-check at Phase 1.5 plan-archive (step 7 chunk-archive-plan invocation). Closes 5-cycle non-application gap (CH-05 + CH-06 + CH-08 + CH-16a + CH-16b + CH-07a all specified P-orch-3 but none applied; CH-07a evidence D-7: plan §6 cited baseline 173 vs actual 176). Companion script `numeric-citation-check.sh` body TBD-authored at follow-up commit; minimum-viable shape spec'd in SKILL.md. **(Update #2 gate-3 audit-prompt-test-allocation cross-check)** — extends gate-3 audit-prompt-authoring cross-check to ALSO cross-reference each `test_<name>` token in audit prompt against plan §8 per-Tier MUST-SHIP test cardinality + split-decision allocation when chunk is one half of a split. CH-07a evidence: orchestrator's Audit A prompt cited 2 CH-07b territory tests (`test_factory_sub_agent_default_mode_incognito` + `test_factory_assembles_initial_context_per_compaction_md_section_4`) as required-PASS — auditor handled gracefully but orchestrator-side miss is the deviation class (D-8). **(Update #3 gate-5 paperwork-sweep step)** — at Phase 5 close, orchestrator paperwork-sweep gains NEW question: "Did this cycle ship NEW recurring executables (scripts under `<root>/scripts/`, binaries under `<root>/src/bin/`, hook scripts under `<root>/hooks/`)? If yes, propose corresponding `.claude/settings.json` Bash allow-list rules as part of Phase 6 standards updates." CH-07a evidence: Tier P shipped 4 NEW `scripts/check-*.sh` that orchestrator invoked 35× in same cycle → 35 PermissionRequest prompts. Gate-5 sweep would have caught at chunk-close. **(Proposals #6a + #6b — settings.json 6 NEW rules)**: **(#6a 4 i-phi CI guard absolute-path rules)** — `Bash(bash /root/projects/phi/i-phi/scripts/check-doc-links.sh*)` + `Bash(bash /root/projects/phi/i-phi/scripts/check-verified-headers.sh*)` + `Bash(bash /root/projects/phi/i-phi/scripts/check-phi-core-reuse.sh*)` + `Bash(bash /root/projects/phi/i-phi/scripts/check-spec-drift.sh*)` mirroring baby-phi precedent (zero-prompt for 8+ cycles). Closes hot-candidate cluster #1 (35 prompts in CH-07a). **(#6b 2 phi-core cargo-registry rules)** — `Read(//root/rust-env/cargo/registry/src/index.crates.io-*/phi-core-*/**)` + `Bash(grep:/root/rust-env/cargo/registry/src/index.crates.io-*/phi-core-*/**)` covering phi-core source-walking workflow post-`7c7ab4b3` 0.8.0 absorption (chunk-implementer + chunk-auditor + retrospector skills all touch this surface). Closes hot-candidate cluster #2 (11 prompts in CH-07a). **(Proposals #7 + #8 — outer `/root/projects/phi/CLAUDE.md` cross-cycle observation updates)**: **(#7 fork-divergence second-axis update)** — i-phi cumulative count updated "1-of-14 (~7%) divergent" → "2-of-22 (~9%) divergent across CH-05/06/08/16a/16b/CH-07a". CH-07a F-iphi-ci-guards-deadline.b USER-DIVERGENT is SECOND within-cycle DIVERGENT lock on i-phi. Cause class differs from CH-16b: CH-16b = architectural-bridge surface (`block_on` safety); CH-07a = **process-velocity decision** (ship-the-carve-out-NOW vs defer 3rd lapse). Hypothesis from CH-16b retro proposal #1 gains second axis class: process-velocity / deadline-lapse axis attracts divergence when story is on 3rd-cycle-lapse. **(#8 plan-iteration-count 3-of-3-confirmed)** — section updated CH-07a iter-2 data point (2 plan iterations; cause class user-direction structural refinement). Sub-class refinement: CH-16a iter-3 = structural § restructure; CH-16b iter-2 = scope-reduction lock-absorption; CH-07a iter-2 = scope-EXPANSION lock-absorption (Tier P USER-DIVERGENT added +8 deliverables + 1 new phase). 3 sub-classes within 1 parent class. All three i-phi cycles audits PASSed at iter-1 (0 re-spawns) despite multi-iteration plans. **(Proposal #4 — DEFERRED per retro rec)**: chunk-planner phi-core surface signature spot-check at P-plan-2 — refinement not defect-fix (D-4 was correctly handled at ADR §D10.3 hot-swap scope-narrowing). Re-evaluate at next cycle showing similar partial-application pattern. **Cycle context**: CH-07a closes joint-convergence first half (Cluster A wiring + daemon runtime + Tier P CI guards carve-out per F-iphi-ci-guards-deadline.b USER-DIVERGENT lock). Sibling CH-07b (Cluster C wiring + sub-agent mode-selection + harvest API + interrupt + resume) carved out at gate-1 split-decision routing (2nd i-phi split after CH-16 → CH-16a/16b 2026-05-21). 8 forks locked at gate-1 (7 planner-rec + 1 USER-DIVERGENT — F-iphi-ci-guards-deadline.b). Plan iter-2 archived at `b3d8dde`; implementer landed 10 commits b3d8dde→`879d95e` shipping 9 phases (P0/P1/P2/P3/P4/P5/P-NEW-TESTS/P-CI-GUARDS/P-DOCS/P-SEAL). 194 workspace tests pass (baseline 176 → +18); cargo fmt + clippy `-Dwarnings` exit 0; 4 CI guards exit 0 in sub-agent + orchestrator gate-4 sandboxes. 4 drifts filed at `docs/v0/design/drifts/`: D-CH07a-FOLLOWUP-01-multi-model-override-surface + D-CH07a-FOLLOWUP-02-input-filter-vs-hook-bridge + D-CH07a-FOLLOWUP-03-additionalDirectories-canonicalize-policy + D-CH07a-FOLLOWUP-04-ci-guards-baseline-state (CONDITIONAL-ACTIVATED gate-2.5 paperwork-cleanup record). ADR-0010a with 13 sub-decisions (incl. §D10.13 USER-DIVERGENT Tier P ratification). Cross-submodule + cross-cluster invariants extend 7→8 cycle streak (phi-core untouched + existing Cluster A/B/C trees untouched). 8 deviations logged in cycle-audit §6 (4 implementer-surfaced + 3 audit-discovered + 1 orchestrator-side audit-prompt miss). 0 Tactical/Architectural/Trivial-multi FAILs; 0 implementer/planner re-spawn; 0 orchestrator-applied Trivial-1L patches at gate-3/4. Cargo-clean placement-1 reclaimed 3.7 GiB; placement-2 reclaimed 264.2 MiB / 290 files; 262 GB free post-clean. |

| 2026-05-18 | NEW `phase-planner.md` v1 + `_changelog.md` (this row) + `/root/projects/phi/CLAUDE.md` + `/root/projects/phi/baby-phi/CLAUDE.md` (Multi-agent chunk pipeline section: 4 agents → 5 agents) + `baby-phi/docs/specs/agentic-workflow/multi-agent-chunk-pipeline-0853574c.md` (phase-planner from "anticipated future" → "shipped v1") | NEW agent (v1 initial ship) | M6 forward-scoping plan archived at `baby-phi/docs/specs/plan/review-and-docs/m6-forward-scoping-and-rename-cleanup-af8aed16.md` (user-locked decision 3 — draft phase-planner FIRST, then use the agent to author the forward-scope rather than hand-author) | **NEW 5th agent in the multi-agent chunk pipeline — milestone-decomposition tier.** `phase-planner` (opus, ~190 lines) sits one tier above `chunk-planner`: takes a milestone narrative (from the base build plan) + a pre-scoping alignment audit + prior-milestone deferral markers (`M*-DEFERRED-NN` from prior forward-scope §3) and emits the per-milestone forward-scope document (chunk-level decomposition + dep graph + open questions). Its output is the forward-scope file that `chunk-planner` later consumes (forward-scope row → chunk plan). 11-step procedure includes: pre-flight verification of input file paths; read in order (base plan + prior forward-scope + audit + concept docs); cross-reference deferral markers vs current code state via `[PLANNED]` + `M<N>-DEFERRED-` greps; enumerate carryovers (`C-M<N>-NN`); decompose into chunks with contiguous numbering from prior milestone's last CH-NN (e.g., post-CH-27 → CH-28 first); build dep graph; per-chunk scope summary table; open questions section; preserve prior forward-scope's §3 markers (only remove markers brought IN-MILESTONE); single Write call. Quality bar parallels chunk-planner v21 — no fabricated drift IDs; all concept-doc citations grep-verifiable; effort estimates carry explicit upper-band; per-chunk severity column populated (HIGH/MEDIUM/LOW rubric); forward-scope §3 preservation verified. PROJECT_ROOT-aware path resolution (baby-phi default; i-phi conventions noted). Granular Bash discipline adopted from chunk-planner v21 / chunk-implementer v13 / chunk-auditor v11 lineage. Output handoff: 9-line return block (milestone tag + path + chunk count + total effort + dep-graph summary + open question count + recommended chunk-zero + 5-line summary). Standing template: most-recent forward-scope file in `docs/specs/plan/forward-scope/` (post-M5.3: `remaining-scope-post-m5-p7-22035b2a.md`). **Origin**: anticipated in `multi-agent-chunk-pipeline-0853574c.md` design discussion as a future M6+ milestone-decomposition slot; CH-27 close (`0edcaba9`, 2026-05-18) is the trigger per CH-27 retrospective's "M6 plan-mode opens at retro-complete" hand-off. Plan archive: `baby-phi/docs/specs/plan/review-and-docs/m6-forward-scoping-and-rename-cleanup-af8aed16.md` (Phase 1 deliverable per the user-approved plan; M6 forward-scoping plan covering Phase 0 file-rename cleanup + Phase 1 phase-planner + Phase 2 alignment audit + Phase 3 M6 forward-scope authorship). **Cycle context**: phase-planner v1 ships post-M5.3-carve-out close. M5 fully sealed; M5.3 carve-out 3-chunk arc {CH-25 `1e01618e`, CH-26 `d1cb9e1f`, CH-27 `0edcaba9`} all CLOSED. Companion file-rename cleanup landed in same Phase 0: 5 plan archives renamed from hex-prefix → hex-suffix per `feedback_plan_archive_naming.md` (525d2085-m5-3-announcement-plan, 22035b2a-remaining-scope-post-m5-p7, ab49f22b-k8s-microservices-readiness-plan, e2781622-fresh-install-admin-journey, a7c31e54-m0-audit-and-docs); ~82 cross-reference updates across plan archives + ADRs + drifts + concept docs; 4 CI guards green post-rename. |

| 2026-05-18 | `chunk-planner.md` (v20 → v21) + `chunk-implementer.md` (v12 → v13) + `chunk-auditor.md` (v10 → v11) + `/root/projects/phi/CLAUDE.md` (gate-2.5 PAUSE + CH-26 Row 6 ratification + cargo-clean v9 7th-cycle ratification) + `/root/projects/phi/baby-phi/CLAUDE.md` (mirror) + `permissions-audit/SKILL.md` (v4.1 → v4.2 cross-cycle-bleed attribution methodology) | omnibus 11-decision update (Bundle A 4 rows + Bundle B 3 rows + Bundle C 3 rows + Standalone R8) | CH-27 baby-phi retro `0edcaba9` §5 Rows R1+R2+R3+R4+R5+R6+R7+R8+R9+R10+R11 (all 4 bundles approved) | **CH-27 baby-phi standards-update batch (M5.3 carve-out 3-chunk arc closure cycle; first cycle with planning-precision quad + ADR-body-strict-reading discipline + cross-cycle-bleed attribution methodology).** **(Bundle A — chunk-planner v21 / R1+R5+R6+R7 planning-precision quad)**: **(R1) Cascade-collapse-cardinality-banding when implicit-emission rules apply** — when plan §3 cascade-enumeration predicts ≥ N test sites that "use production path X" (e.g., `apply_org_creation`, `spawn_claimed_with_org`), planner MUST cross-reference whether X carries an implicit-emission rule from a prior ADR (e.g., CH-25 ADR-0060 §D60.1's Edge::Owns emission). If yes: plan §3 cascade-band widens to `[N - implicit-covered-subset, N]` with cascade-collapse rationale ready (NOT single point estimate). Closes CH-27 Audit-A claim 7 FAIL (planning band 12-18 actual 9 cascade-collapse). **(R5) Plan §8 band-derivation for top-level HTTP scenarios** — band derivation MUST be `[base + MUST-SHIP-count, base + MUST-SHIP-count + MAY-COVER-count]` with NO "partial overlap" subtraction when MUST-SHIP includes NEW HTTP-tier scenarios authored at test-file top level (NOT in sub-mods). Closes CH-27 Gate-4 deviation #1 (test count +2 above band 1574; 4 NEW HTTP 403-block scenarios are top-level #[tokio::test] with no overlap). **(R6) Helper-API trait-grep verification at plan-draft pre-archive** — for any code-block citing a Rust API call (`repo.X(...)`, `trait::method(...)`), planner MUST grep the actual trait definition at the cited file:line to verify method existence. If not exist: (a) substitute actual API OR (b) mark "helper-shape illustration" with SCOPE-NARROWING contingency. Closes CH-27 Gate-4 deviation #2 (F4.b helper-body `repo.insert_edge(Edge::Owns)` non-existent API; implementer substituted `Repository::create_grant`). **(R7) P3 scenario-naming source-grep** — P3-scenario-naming MUST match actual handler operation name. Planner MUST grep actual handler operation name from source body at plan-draft P3 deliverable enumeration. Closes CH-27 Gate-4 deviation #4 (`..._list_agent_supervisors_returns_403` vs actual `set_agent_supervisor` handler at `agent_supervisor.rs:194`). **(Bundle B — chunk-implementer v13 / R2+R3 + chunk-auditor v11 / R4)**: **(R2) ADR-body-strict-reading deliverable-interpretation** — when RESUME-NOTE or plan §X deliverable cites "in ADR-NNNN §Y body", implementer MUST treat this as post-§Y-header content, NOT verified-header. Closes CH-27 Audit-B claim 5 PARTIAL (§D62.4 SCOPE-NARROWING note documented at 3 sites but missed inline in §D62.4 body itself; orchestrator applied Trivial-multi inline patch at gate-3). **(R3) P-FIXTURES → P-DOCS actuals snapshot** — at P-FIXTURES close (BEFORE P-DOCS opens), implementer MUST run "P-FIXTURES actuals snapshot" (call-site count, file count, LOC added, cascade-band marker COLLAPSE/WITHIN/OVERRUN). P-DOCS MUST cite snapshot, NOT plan §X bands. Pairs with R9 gate-2.5 PAUSE rule. Closes CH-27 Audit-B side observation (19 fixture-extension sites documented across 4 docs while P-FIXTURES landed 9; orchestrator applied Trivial-multi cardinality cascade patch at gate-3 across 7 doc locations). **(R4) Audit-C allowed-edit envelope cross-check** — when auditing carry-forward regression, if M3/M4/M5 fixture/acceptance test shows wire-canonical/error-code/API-signature adjustment, cross-check plan §7 P2 (or any phase deliverable) for explicit call-out. If plan-aligned + ADR-cited + invariant-preserving: PASS-with-caveat (load-bearing-correctness-following), NOT regression flag. Closes CH-27 Audit-C claim 10 PASS-with-caveat scope ambiguity (`ORG_ACCESS_DENIED` → `NO_GRANTS_HELD` flip at `acceptance_m5_orgs.rs:140-160`; documented inline with ADR-0062 §D62.1 cite; 403-isolation invariant preserved). **(Bundle C — CLAUDE.md / R9+R10+R11)**: **(R9) Gate-2.5 PAUSE between P-FIXTURES + P-DOCS** — added to both `/root/projects/phi/CLAUDE.md` + `/root/projects/phi/baby-phi/CLAUDE.md` under cargo-clean discipline section. Orchestrator inserts gate-2.5 PAUSE between cascade-emitting phases + P-DOCS; implementer reports actuals snapshot; orchestrator confirms before P-DOCS opens. Pairs with chunk-implementer v13 R3. **(R10) CH-26 Row 6 success-case data-point ratification** — added single sentence to existing In-M5-vs-M6-DEFERRED routing paragraph in both CLAUDE.md files. CH-27 ratifies the criteria via first successful application: within single chunk, F3.a resolvers wiring routed M6-DEFERRED (architectural design > ~10 ed; not load-bearing for M5 wire-tier closure) while wire-tier blocking + synth-grant widening + F4.b helper routed in-M5 (load-bearing; user-routed). **(R11) Cargo-clean v9 7th-cycle empirical durability narrative** — strengthened narrative in both CLAUDE.md cargo-clean paragraph: validated across 7+ consecutive cycles (CH-18 codified → CH-27 7th-cycle re-validation; ~600+ GiB cumulative reclaimed; 0 mid-cycle disk-pressure incidents post-codification). **(Standalone — permissions-audit SKILL.md v4.1 → v4.2 / R8)**: **(R8) Cross-cycle-bleed attribution methodology** — added to §B Hot allow-rule candidates discipline. When cycle window captures BOTH CH-NN direct work AND parallel-cycle work in shared telemetry stream, each ≥3-prompt cluster row MUST classify as "CH-NN-direct" OR "cross-cycle-bleed (cite parallel cycle hex via sample-input-anchor)". Use sample-input introspection to discriminate. Subtract cross-cycle-bleed clusters from CH-NN actionable Hot-candidate count in §G trend reporting. Closes CH-27 telemetry observation (51 of 98 PermissionRequests = 52% were CH-03-i-phi parallel-cycle bleed; without attribution apparent actionable count was 4-5; with attribution only 1 actionable). **Cycle context**: CH-27 closes M5.3 carve-out 3-chunk arc {CH-25 `1e01618e`, CH-26 `d1cb9e1f`, CH-27 `0edcaba9`}. M6 plan-mode unblocks at retro-complete. F4.b USER-DIVERGENT (opt-in helper `seed_owner_grants` over default-extension F4.a); 3-of-4 forks aligned with planner-rec (F1.a + F3.a + count-amend); cross-cycle divergence pattern 11-of-16 = ~69% (softening from prior ~83%). Audit-fix-loop iterations: 1 (Audit B iter 1 PARTIAL → iter 2 PASS via 2 orchestrator-Trivial-multi patches: SCOPE-NARROWING note inline in §D62.4 + cardinality cascade "19" → "9" across 7 doc locations). 0 Implementer re-spawn; 0 Planner re-spawn. Gate-4 MUST-RUN GREEN (clippy 0 warnings 9m 02s + cargo test exit 0 [1576/0/2; +2 above plan §8 v2 band 1574 = planning-precision deviation routed retro] + fmt + 4 CI guards + phi-core baseline 57 Δ +0). Cargo-clean placement-1 reclaimed 92.1 GiB (7th-cycle empirical durability validation). NEW drift D-CH27-FOLLOWUP-01 filed with explicit M6-DEFERRED-RESOLVERS-WIRING allocation. Permissions-audit skill v4.1 3rd-cycle re-validation PASSED. CH-26 Row 7 (`Bash(jq *)`) + Row 8 (`Bash(tail *)`) BOTH graduate to `resolved-via-workaround` empirically (0 CH-27-direct prompts). |

| 2026-05-18 | `chunk-planner.md` (v19 → v20) + `chunk-initiate/SKILL.md` (P1 gate-1 fork-lock decision flow + P5c NEW Phase 8 temp-folder cleanup + P8 Phase 5→6 transition gate) + `phi-core-leverage-check/SKILL.md` (P4 leverage-sites methodology) + `permissions-audit/SKILL.md` (v4 → v4.1 window-mode flag) + `.claude/settings.json` (P5 cargo-tree allow + P5b `Read(//tmp/**)` allow) | omnibus 11-decision update (9 retro proposals + 2 user-derived) | CH-03-i-phi retro `c542648f` §5 Rows P1+P2+P3+P4+P5+P5b+P5c+P6+P7+P8 (P9 deferred to CH-04 close) | **CH-03-i-phi standards-update batch (first Cluster A cycle on i-phi; identity composition shipped — strongest audit-side cycle on i-phi yet with 0 audit re-spawns + 35/35 first-iter PASS across LARGE envelope A+B+C).** **(P1 — chunk-initiate/SKILL.md Phase 1.5 gate-1 fork-lock decision flow)**: codifies re-spawn-planner-with-locked-forks as the DEFAULT when divergent locks materially expand scope (≥ +5 deliverables OR audit-envelope tier bump). Force-proceed remains an explicit escape hatch. CH-02c (force-proceed) vs CH-03 (re-spawn) precedent — re-spawn produced 0 audit re-spawns + 0 Trivial-multi patches vs CH-02c's 4 deviations at gate-2 audit. Below-threshold divergence proceeds with original plan + deviation note. **(P2 — chunk-planner v20 § "v20 bundle" P2)**: locked-fork-details appendix MANDATORY when ≥ 1 fork is locked. After §"Forks for orchestrator" header table, plan MUST add `### Locked fork details — what each lock actually means` section with `#### F<N> = F<N>.<letter> — <headline>` subsection per locked fork carrying 3-6 sentences of plain-English semantics (implementer-readable; downstream-consumer-readable; cite open-questions closed). Header-table-only documentation insufficient. CH-03 precedent: user requested this mid-gate-1.5; 130+ line appendix added in-place; v20 bakes into planner template so artifact ships at iter-1 archive. The 4 sub-fork open-form option tables STAY for traceability. **(P3 — chunk-planner v20 § "v20 bundle" P3 — plan precision triad)**: 3 refinements in plan §3/§7/§8: (a) error-variant `Source-preservation policy` column (preserve / coerce); default preserve for human-facing variants. (b) `LOC estimate (alt forms)` column when "implementer's choice" applies (hand-roll vs library); both estimates stated. (c) Tests asserting filesystem-event-coalesced counts default to `(1..=N).contains(&count)` not `assert_eq!(actual, 1)` — inotify fires MODIFY+CLOSE_WRITE per write; debounce gates may leak 2 callbacks. CH-03 precedent: `test_watcher_debounces_rapid_changes` shipped with `(1..=2).contains`. Closes 3 PASS-with-note deviations sharing common plan-precision theme. **(P4 — chunk-planner v20 § "v20 bundle" P4 + phi-core-leverage-check/SKILL.md "Prediction methodology")**: phi-core leverage prediction MUST count leverage-sites (semantically distinct uses), NOT `use phi_core` line counts per file. Tolerance ±3 leverage-sites at chunk-close. CH-03 evidence: predicted 15-16 lines; actual 13 (-2 to -3 deviation). Under leverage-site methodology, predicted 2 (compose + tests); actual 2 — exact match. Forbidden-duplication greps stay unchanged. Methodology propagates to phi-core-leverage-check skill's predict/self-check/verify modes. **(P5 — settings.json `Bash(.../cargo tree *)` + `Bash(cargo tree *)`)**: 2 new allow lines after `cargo run *` block. Closes HOT cluster from CH-03 permissions-audit (3 fires, first-encounter). **(P5b — settings.json `Read(//tmp/**)`)**: NEW allow rule (user-derived from P5 amendment 2026-05-18). Sub-agents can read `/tmp` without PermissionRequest. Unblocks common pattern of caching long agent outputs in `/tmp/<slug>-*-output*.{md,json}` and re-reading from sub-agents. **(P5c — chunk-initiate/SKILL.md NEW Phase 8)**: NEW Phase 8 — Temp-folder cleanup with backup-only-if-deemed-necessary clause (user-derived from P5 amendment). Scan `/tmp/<chunk-slug>-*` + `/tmp/<cycle-hex>-*` + `/tmp/claude-*/.../tasks/<id>.output` + cycle-window-modified files. Triage: backup to `<cycle folder>/scratch/<filename>` ONLY IF content carries unique signal NOT already captured in cycle-folder artifacts; default is NO backup; then `rm` selected. Skip in dry_run. Runs AFTER Phase 7 final summary printed. **(P6 — permissions-audit v4 → v4.1)**: NEW `window_mode` parameter — `narrow` (default; plan-archive-mtime → cycle-audit-mtime; back-compat) | `wide` (implementer-dispatch-time → cycle-audit-mtime; captures pre-archival work including forward-scope drafts, gate-1 fork-locks, planner re-spawn iterations, mid-gate-1.5 plan edits). Use `wide` when planner re-spawn workflow fires; narrow mode truncates the iter-1 → iter-2 transition. Per-minute rate cells in §G MUST annotate which mode produced them. **(P7 — chunk-planner v20 § "v20 bundle" P7 — cross-cluster invariant)**: when a locked fork's surface area sits in a different cluster than the chunk's primary cluster, plan §4 MUST carry: (1) explicit `**CH-NN MUST NOT touch src/<other-cluster>/**` directive at the locked fork's row; (2) audit claim wiring (`git diff HEAD -- <other-cluster-path> | wc -l = 0` in Audit C for LARGE OR Audit B for MEDIUM); (3) rationale (1-2 sentences why surface seems related + which downstream chunk inherits wire-up). CH-03 precedent: F4.b watcher pure-library at CH-03; CH-07 agent-factory wires later; audit C verified `git diff src/daemon/ = 0`. **(P8 — chunk-initiate/SKILL.md Phase 6 preamble)**: MANDATORY Phase 5 → Phase 6 transition gate before retrospector dispatch. Orchestrator presents transition summary via AskUserQuestion: (a) proceed with retrospector dispatch (Recommended), (b) skip retrospective, (c) provide context for retrospector first, (d) pause longer / abort. Treats retrospector as last expensive sub-agent spawn (produces standards-update proposals the user reviews one-by-one) and gives user a checkpoint. Codifies user feedback memory `feedback_pause_before_retrospector.md` saved mid-cycle. **(P9 — DEFERRED to CH-04 close)**: drop CH-08 `7cbe74a4` + CH-01 `95c96df7` rows from mid-cycle settings.json edits table at CH-04 retrospective (both rows now ≥ 3 consecutive zero-fire cycles). No action this cycle. **Cycle context**: CH-03 is the first Cluster A chunk on i-phi (identity composition — markdown layers → SystemPromptStrategy). Depends only on CH-01; independent of CH-02a/b/c daemon runtime. 10 top-level forks user-locked at gate-1 (3 divergent: F3.b full 3-tier merge AT CH-03 + F4.b notify-rs watcher AT CH-03 + F-empty-dir-fallback.b strict NoLayersFound); 4 sub-forks locked at gate-1.5 (all planner-rec: §D5.14.b REPLACE + §D5.15.a 250ms-debounce + §D5.16.a Linux-only-noop-warn + §D5.17.a callback-closure). FIRST cycle where planner re-spawned at gate-1 (vs CH-02c force-proceed) — produced cleaner audit pipeline (35/35 PASS first-iter). 22 deliverables, 6 phases (P0/P1/P2/P3/P4/P5), 15 MUST-SHIP tests; LARGE envelope (3 auditors A+B+C). ADR-0005 with 17 sub-decisions §D5.1-§D5.17 (largest ADR on i-phi to date; Divergent-cycle Forks-header). 7/7 open-questions in identity-composition.md ANSWERED at CH-03 (Q1-Q7 all). 0 drift files filed under iter-2 eager-closure lock. 0 cross-cluster contamination (`git diff src/daemon/ = 0`). 34 workspace tests pass (band [31, 38]); 2.8 GiB disk reclaimed at gate-5. Cross-cycle divergence tracker advances: combined window 8-of-11 → **9-of-12** cycles; cumulative cross-cycle divergent forks 14-of-22 → **16-of-22 (73%)**. F4.b watcher implemented as pure-library primitive (Linux-only #[cfg]; 250ms hand-rolled debounce via `Instant` gate; mpsc + Drop joins thread); zero shared mutable state with composer. Sets Cluster A precedent for parallel chunks CH-04/05/08. |

| 2026-05-18 | `chunk-planner.md` (v18 → v19) + `granular-bash-discipline-ab19399b.md` (§2.2 `jq + 3+-stage pipeline` example added) + `baby-phi/scripts/audit-jq-extract.sh` (NEW helper) | omnibus 7-proposal update | CH-02c-i-phi retro `81f0c24e` §5 Rows P1+P2+P3+P4+P5+P6+P7 (P-D1 deferred) | **CH-02c-i-phi standards-update batch (fourth i-phi cycle retrospective; 7 of 7 active proposals applied — 5 LOW-to-MEDIUM hygiene + 1 MEDIUM mid-cycle-confirmed + 1 LOW informational; P-D1 deferred).** **(P1-P5 — chunk-planner v18 → v19 hygiene bundle)**: 5 small additive refinements to v17 P1+P2+P3 disciplines, all in a single new "Hygiene bundle (v19)" sub-section. **(P1)** §3.B per-fork pause-threshold table now accounts for co-located inline `#[cfg(test)] mod tests { ... }` blocks (CH-02c registry.rs 312 LOC vs cap 280 — RFC3339 helper + 2 unit tests; well under pause-trigger 420). **(P2)** §6 carry-forward back-compat preservation decision-prompt template — when divergent fork-lock structurally removes a load-bearing scaffold that prior-cycle tests depend on (CH-02c F-broadcast-scope.b removed daemon-wide broadcast; preserved narrow `fallback_event_tx` for CH-02b smoke back-compat), planner surfaces (a) amend-test / (b) preserve-narrow-scaffold / (c) defer-test at plan-draft. Expected to fire at CH-04 + CH-06. **(P3)** §3.E `AgentEvent::ProgressMessage` substitution catalog entry — when project has no direct chrono/time dep but needs phi-core event emission (CH-02c per-session task substituted ProgressMessage for AgentStart since chrono not directly pulled). **(P4)** §3 baseline-import-count grep invocation — explicit `grep -rn "use phi_core" ... | wc -l` at plan-draft + predicted-delta accounts for new test file's incidental imports (CH-02c plan predicted 6→8 but actual was 8→11). **(P5)** §3.E hand-rolled helper centralization decision-prompt — when ≥ 2 files use the same hand-rolled helper (`format_rfc3339_seconds` across sessions/registry.rs + ipc/handlers.rs at CH-02c), planner pre-allocates centralization site at plan-draft OR flags v0-posture inlining. Also updates chunk-planner.md `## Cross-cycle user-lock-divergence prominent gate-1 callout` "Current data" line to reflect post-CH-02c state (combined 8-of-11 cycles / cumulative forks 14-of-22 / 64% / F<X>.b durable across 9 cycles). **(P6 — granular-bash-discipline.md §2.2 + `baby-phi/scripts/audit-jq-extract.sh`, MEDIUM + mid-cycle confirmed)**: NEW `jq + 3+-stage pipeline` worked example documenting the matcher-quirk (`jq -r '<filter>' file | sort | uniq -c | sort -rn | head -N` 4-stage pipeline; 20 PermissionRequest fires from CH-02c retrospector's cross-cycle aggregation). Resolutions: (a) wrap in helper script — ship `audit-jq-extract.sh <jsonl-file> <jq-filter> [head_N=30]` as canonical reference (parallel to CH-02b's `wait-for-pid.sh`); (b) do NOT add `Bash(jq * | sort | uniq -c | sort -rn *)` allow-rule — would encourage discouraged shape. **(P7 — informational ledger; this row body)**: CH-02c is the FIRST 4-divergence i-phi cycle (F2.b per-agent isolated pools + F4.b RwLock + F6.c file crash-recovery drift + F-broadcast-scope.b per-session broadcast channels). Cross-cycle divergence tracker advances: combined window 7-of-10 → **8-of-11** cycles (baby-phi 5-of-7 + i-phi 3-of-4); cumulative cross-cycle divergent forks 10-of-12 → **14-of-22 (64%)**. F<X>.b expansion-divergence pattern durable across 9 cycles (baby-phi CH-15/17/18/20/24/25 + i-phi CH-02a F5.b + CH-02b F4.b/F-error.b + CH-02c F4.b/F-broadcast-scope.b). **chunk-planner v17 P1+P2+P3 empirically validated at CH-02c (first applicable cycle)** — P1 fired correctly (per-fork pause-threshold table at plan §3.B); P2 fired correctly (ADR-0004 all 7 canonical sections present at iter 1; **0 Trivial-multi paperwork patches** vs CH-02b's 2 — empirical win); P3 fired correctly (carry-forward test names matched repo state). **First cycle to validate 3 prior-retro standards updates simultaneously since CH-02a validated 3 of CH-01's.** **Cycle context**: CH-02c is i-phi's fourth pipeline cycle. F0.A 3-way split's concurrency slice — the FINAL piece; F0.A 3-way split is now structurally complete (CH-02a lifecycle + CH-02b IPC + CH-02c concurrency). 10 forks user-locked at gate-1; **4 divergent** (most-divergent i-phi cycle to date). **Cleanest audit pipeline on i-phi to date**: Audit A 25/25 PASS first iter + Audit B 20/20 PASS first iter; 0 re-spawns; 0 patches; 0 FAIL. All MUST-RUN green (clippy 0 warnings; 19/19 tests pass — 2 unit + 6 concurrency + 7 IPC + 3 lifecycle + 1 smoke; fmt clean). 4.6 GiB disk reclaimed across gate-2 + gate-4. ADR-0004 with 13 sub-decisions §D4.1-§D4.13 (Divergent-cycle Forks-header; 6 `### For CH-NN` consequences subsections; 7 Revisit triggers bullets). 1 drift remediated (D-CH02B-FOLLOWUP-01 multi-attach-locking → remediated) + 1 NEW drift filed (D-CH02C-FOLLOWUP-01 crash-recovery → post-CH-08 dedicated crash-recovery chunk per F6.c lock; NO TBD). **i-phi daemon runtime is now structurally complete + end-to-end functional**: tokio runtime + lifecycle + IPC server + 7 handlers + session registry + per-session task scheduling + multi-attach RwLock locking + broadcast fan-in. Unblocks: CH-06 (session integration) + CH-11a (full HTTP/daemon API). |
| 2026-05-17 | `chunk-planner.md` (v17 → v18) + `chunk-implementer.md` (v11 → v12) + `/root/projects/phi/CLAUDE.md` (Audit-fix loop + gate-5 sub-section) + `/root/projects/phi/baby-phi/CLAUDE.md` (mirror) | chunk-planner v17 → v18; chunk-implementer v11 → v12 | CH-26 retro `ch-26-org-project-as-composite-d1cb9e1f/retrospective.md` §4 Rows 1-8 (Bundles A+B+C+D) | **Omnibus standards-update batch landing 6 new content rows (R7+R8 already-applied pre-cycle).** **(Bundle A — chunk-planner v18 / R1+R2)**: **(R1) Handler-refactor cascade CheckContext-build cost addendum** — when chunk's deliverables refactor ≥ 2 handlers to invoke `check_permission` from bespoke-gated baseline, planner MUST enumerate per-handler CheckContext-build cost at plan-time. Default ~1.5-2 ed/handler decomposed: Manifest construction 0.5-1 ed + bespoke-gate threading 0.5-1 ed + metric injection 0.1-0.2 ed + acceptance blast-radius 0.3-0.5 ed. If WIDE-handler-refactor scope ≥ 7 handlers, cycle SHOULD be split BEFORE plan-locking (e.g., advisory-only invocations in N + blocking-gate tightening in N+1). Closes CH-26 mid-cycle scope-revision (F1.b v2 estimate ~5 ed actual ~12-15 ed). **(R2) Composite-resource extension fork-pattern reference table** — when chunk introduces NEW Composite variant needing instance-identity backing, planner surfaces 3-path matrix at gate-1: (a) catalogue-entry-only ~0.5-0.8 ed; (b) tag-field-on-struct + backfill migration ~1.5-2 ed; (c) hybrid ~1.5-2.5 ed. Default framework: (a) for ≤ 2 cross-cutting concerns; (b) for wire-format-explicit needs; (c) for load-bearing-philosophy resources. Honors 83% cumulative divergence pattern while surfacing all 3 paths. **(Bundle B — chunk-implementer v12 / R3+R4)**: **(R3) Cardinality-reference cascade grep at P-SEAL** — when chunk flips enum/struct cardinality (Composite::ALL / EDGE_KIND_NAMES / Action::CANONICAL), implementer MUST `git grep -nE '[0-9]+ (Composite\|Fundamental\|EdgeKind\|Action) (variants\|kinds)' modules/crates/ docs/` at P-SEAL + verify all matches reflect new cardinality. Closes CH-26 Audit-B Side Observation 1. **(R4) Section-anchor sync cascade grep at P-SEAL** — when concept-doc section anchor changes (e.g., `#composite-classes-8` → `#composite-classes-10`), implementer MUST `git grep -nE '#(composite-classes\|edge-kinds\|action-canonical\|fundamental-classes)-[0-9]+' docs/` at P-SEAL + verify all cross-refs reflect new anchor. Closes CH-26 Audit-B Side Observation 2. Both R3 + R4 surface in same P-SEAL checklist item 8a. **(Bundle C — CLAUDE.md / R5+R6)**: **(R5) Session-interrupt mid-audit kill detection + safe re-dispatch** — added to Audit-fix loop in both `/root/projects/phi/CLAUDE.md` + `/root/projects/phi/baby-phi/CLAUDE.md`. Re-dispatch with SAME prompt is safe (stateless sub-agent; atomic audit-log write). Re-dispatch NOT counted as iteration. CH-26 Audit-A precedent. **(R6) In-M5 carve-out vs M6-DEFERRED decision criteria** — added to gate-5 retrospective sub-section in both CLAUDE.md files. Decision matrix: in-M5 carve-out when load-bearing + ≤ 10 ed + user-explicit (CH-26 → CH-27 precedent); M6-DEFERRED-NN when NOT load-bearing OR > 10 ed OR M6+ feature surface (CH-25 F-D59.2/F-D59.3 precedent). **(Bundle D — settings.json / R7+R8 already-applied pre-cycle)**: `Bash(jq *)` line 57 + `Bash(tail *)` line 73 both extant; no new edits. CH-26 audit verdicts: A 24/24 PASS (re-dispatched once after session-interrupt, not counted as iteration); B 15/15 PASS + 2 orchestrator-Trivial patches at gate-3; C 9/9 PASS clean. Mid-cycle scope-revision (F1.b WIDE → advisory-only + NEW CH-27 carve-out per user routing 2026-05-16) ratified in ADR-0061 §D61.5 + §D61.7. M5.3 carve-out now 3-chunk {CH-25, CH-26, CH-27}; M6 plan-open waits on CH-27 close. Cross-cycle divergence 10-of-12 (~83%). Permissions-audit skill v4 2nd-cycle re-validation PASSED. |
| 2026-05-17 | `chunk-planner.md` (v16 → v17) + `chunk-implementer.md` (v10 → v11) + `per-chunk-planning-template.md` (§3 + §5 + §8 v17 additions) + `granular-bash-discipline-ab19399b.md` (§2.2 `until` example added) + `baby-phi/scripts/wait-for-pid.sh` (NEW helper) | omnibus 5-proposal update | CH-02b-i-phi retro `57b20bda` §4 Rows P1+P2+P3+P4+P5 | **CH-02b-i-phi standards-update batch (third i-phi cycle retrospective; 5 of 5 proposals applied — 3 planner/implementer/template MEDIUM-to-HIGH + 1 permissions/discipline MEDIUM + 1 informational ledger).** **(P1 — chunk-planner v16 → v17 + chunk-implementer v10 → v11 + per-chunk-planning-template §3, MEDIUM)**: per-fork pause-threshold re-derivation after gate-1 fork-locks. §3 cascade-fan-out thresholds (file count cap, per-file LOC cap, Cargo.lock churn cap) are derived at plan-draft BEFORE gate-1 locks the forks. When a fork-lock materially expands scope (e.g., F4.b broader-6 vs F4.a minimal-3), the original thresholds may no longer reflect locked scope. Plan §3 now MUST emit a per-fork pause-threshold table; orchestrator re-derives at gate-1 by summing locked-option deltas before handing implementer the active threshold set. Companion rule at chunk-implementer v11: on §3 cascade-threshold breach, implementer MUST emit AskUserQuestion AT the breach (not just log + push through). CH-02b precedent: `src/daemon/ipc/server.rs` shipped at 354 LOC vs plan-draft 250 LOC cap (1.5× predicted 150 LOC); F2.a transport-dual complexity was under-predicted; gate-1 didn't re-derive after F4.b lock. **(P2 — chunk-planner v17 + per-chunk-planning-template §5, HIGH; mid-cycle confirmed)**: explicit ADR top-level section enumeration in plan §5. Plan §5 MUST list all 7 canonical ADR sections (Forks / Context / Sub-decisions / Cross-references / Consequences-with-###-For-CH-NN-subsections / Revisit triggers / Verification). CH-02b precedent: Audit-B iter 1 caught BOTH gaps — `## Revisit triggers` section missing entirely + `### For CH-06` subsection missing from §Consequences. Orchestrator applied 2 Trivial-multi paperwork patches; Audit-B iter 2 PASS. Plan §5 enumeration would have surfaced both at plan-draft. Highest-value proposal in this cycle. **(P3 — chunk-planner v17 + per-chunk-planning-template §8, LOW)**: grep-verify carry-forward test names against actual repo state at plan-draft. Run `grep -hE "^fn (test_|smoke_)" <PROJECT_ROOT>/tests/*.rs` and use actual fn names verbatim; do NOT paraphrase from prior-cycle plan or retrospective text. CH-02b precedent: plan §8 listed CH-02a test names that didn't match actual fn names (wording-drift, no harm done; cheap insurance). **(P4 — granular-bash-discipline.md §2.2 + `baby-phi/scripts/wait-for-pid.sh`, MEDIUM)**: NEW `until + ;-chain` worked example documenting the polling-loop compound shape's PermissionRequest pattern (matcher splits at `;` between `until <check>` and `do ...; done` body; `until` not allow-listed). Resolutions: (a) prefer harness notification mechanism over polling; (b) wrap loop in script — ship `wait-for-pid.sh` as the canonical reference helper; (c) do NOT add `Bash(until *)` allow-rule. CH-02b permissions-audit surfaced 3 `Bash:until:!` PermissionRequest fires from background-polling pattern. **(P5 — informational ledger; this row body)**: CH-02b is the FIRST i-phi cycle to have multi-divergence (2 forks within one cycle: F4.b broader handlers + F-error.b thiserror IpcError). Cross-cycle divergence tracker advances: combined window 6-of-9 → **7-of-10** cycles (baby-phi 5-of-7 + i-phi 2-of-3); cumulative cross-cycle divergent forks now **10-of-12 (83%)**. F<X>.b expansion-divergence pattern is structurally durable across 8 cycles (baby-phi CH-15/17/18/20/24/25 + i-phi CH-02a F5.b + CH-02b F4.b + F-error.b). User-tightening pattern persists. CH-02a retro Row 4a `Bash(ps *)` rule firing cleanly at CH-02b (validation confirmed; previously 4 fires at CH-02a pre-edit). **Cycle context**: CH-02b is i-phi's third pipeline cycle. F0.A 3-way split's IPC slice. 9 forks user-locked at gate-1; 2 divergent (F4.b + F-error.b). 1-iter clean on Audit A; 1 re-spawn on Audit B (after 2 orchestrator-applied Trivial-multi paperwork patches on ADR-0003 sections). All MUST-RUN green (clippy 0 warnings; 11/11 tests; fmt clean). 2.4 GiB disk reclaimed at gate-4 cargo-clean. ADR-0003 with 13 sub-decisions D3.1-D3.13 (Divergent-cycle Forks-header). 2 drifts filed (FOLLOWUP-01 multi-attach-locking → CH-02c; FOLLOWUP-02 systemd-runtimedirectory → post-CH-08 ops chunk; both with explicit `Impl chunk`, NOT `TBD`). |
| 2026-05-17 | `phi/CLAUDE.md` (gate-2 widened-sweep rule extended) + `chunk-planner.md` (v15 → v16) + `i-phi/docs/v0/design/decisions/0002-...md` (Consequences forward-route appended) + `settings.json` (Bash(ps *) allow rule added) + `baby-phi/docs/specs/permissions/granular-bash-discipline-ab19399b.md` (§2.2 worked-examples block added) | omnibus 5-target update | CH-02a-i-phi retro `1bd3bdd1` §5 Rows 1+2+3+4+5 | **CH-02a-i-phi standards-update batch (second i-phi cycle retrospective; 5 of 5 proposals applied — 4 MEDIUM/LOW edits + 1 informational ledger).** **(Row 1 — phi/CLAUDE.md gate-2 widened-sweep ADR↔open-questions cross-check, MEDIUM)**: for every ADR sub-decision the chunk files that claims to resolve a concept-doc open-question (e.g. `### §D<N>.<M> — <resolution> (resolves F<X>)`), the orchestrator at gate-2 MUST verify the corresponding open-question in the cited concept doc carries an `[ANSWERED at CH-<NN>: <one-line resolution + ADR cite>]` annotation. CH-02a Trivial-1L gate-2 patch caught daemon.md's OS-integration open-question lacking the annotation despite ADR-0002 §D2.5 resolving F5.b; implementer had annotated SIGHUP + concurrency questions but missed OS integration. Applying the cross-check at gate-2 surfaces these as Trivial-1L (≤ 1-line annotation per missing question) before audit dispatch. **(Row 2 — chunk-planner v15 → v16, LOW)**: NEW procedure step 11a — P-SEAL cycle-index row Iterations + Status canonical lifecycle. When drafting §7 P-SEAL paperwork for cycle-index row updates, the canonical implementer behaviour is to leave `Iterations = pending` and `Status = in-flight` — orchestrator owns the transitions per `_cycle-index.md` row-lifecycle paragraph (gate-3 → ready-for-audit; gate-4 close → audited-pending-retro; Phase 6 / Phase 7 close → retro-complete + Iterations to final count). CH-02a plan §7 P3 deliverable 4 had prescribed `Iterations = 1` literally; implementer correctly followed the lifecycle paragraph and left `pending`. v16 codifies the deferral so future plans don't carry the same plan-text drift. **(Row 3 — ADR-0002 §Consequences, LOW)**: 1-line forward-routing note appended — *"For CH-06 (forward-routing note added 2026-05-17 per CH-02a retro Row 3): when FileSystemSessionStore session persistence ships, add ReadWritePaths=<state-dir> to contrib/i-phi.service [Service] section so the daemon can write its session files under ProtectSystem=strict."* Captures Audit-A additional-check E forward-routing observation so CH-06 implementer doesn't re-discover it. **(Row 4a — settings.json Bash(ps *), MEDIUM)**: 1-line addition to allow-list adjacent to benign-read cluster (`Bash(xargs *)` / `Bash(stat *)` / `Bash(cat *)` / `Bash(diff *)`). Closes 4-PermissionRequest hot cluster on `ps:1257447` from concurrent baby-phi CH-26 background-polling work. **(Row 4b — granular-bash-discipline.md §2.2, LOW)**: NEW "Worked examples" block at end of §2.2 documenting three compound shapes with their canonical break-up patterns: `cd /abs && cmd` → absolute-path form; `cmd1 ; cmd2` → separate Bash calls (with CH-02a-i-phi `ps -p $pid ; ls /proc/$pid/status` as the worked example); 4-stage pipelines → write to scripts/audit-tmp-*.sh. **(Row 5 — informational ledger; this row body)**: CH-02a is the FIRST cycle to empirically validate THREE CH-01-i-phi-retro standards updates in one cycle — (i) chunk-archive-plan v2 project-aware row-insertion (worked correctly; row appeared at line 35 of i-phi cycle-index at chunk-open with canonical `Iterations = pending` + `Status = in-flight`); (ii) chunk-initiate Phase 0 inline-draft offer (forward-scope `ch-02-daemon-core.md` drafted + reviewed + committed in-session, vs CH-01's out-of-band re-invocation pattern); (iii) `Bash(git -C /root/projects/phi/i-phi *)` rule firing cleanly (zero PermissionRequest fires; previously 6 fires at CH-01 pre-edit). The retro pipeline is healthy and self-correcting. **Cycle context**: CH-02a was the second i-phi cycle. F0.A 3-way split locked at gate-1 (CH-02a lifecycle this cycle; CH-02b IPC + CH-02c concurrency follow). F5 user-locked to F5.b (ship Linux systemd unit template `contrib/i-phi.service`) — diverges from planner-rec F5.default; first i-phi user-vs-planner divergence; combined cross-cycle window 6-of-9. 22/22 audit claims PASS, 1 Trivial-1L gate-2 orchestrator patch on daemon.md OS-integration annotation. Inaugural i-phi drift filed: `D-CH02A-FOLLOWUP-01-sighup-config-reload-deferred.md` (LOW; Impl chunk = post-CH-08 dedicated config-reload chunk; NOT `TBD`). Inaugural i-phi ADR-0002 with 8 sub-decisions D2.1–D2.8. |
| 2026-05-17 | `chunk-archive-plan` (skill, v1 → v2) + `chunk-initiate` (skill, Phase 0 sub-step added) + `i-phi/docs/v0/proposal/plan/_cycle-index.md` (column-semantics header paragraph added) + `chunk-auditor.md` (v9 → v10) + `chunk-retrospector.md` (v5 → v6) | omnibus 5-target update | CH-01-i-phi retro `95c96df7` §5 Rows 1+2+3+5+6 (Row 4 was informational, no file edit) | **CH-01-i-phi standards-update batch (first i-phi cycle retrospective; 6 of 6 proposals applied, 1 informational).** **(Row 1 — chunk-archive-plan v1 → v2, HIGH)**: skill is now project-aware via `PROJECT_ROOT`. Translates the cycle-index path (`<root>/docs/specs/plan/build/_cycle-index.md` for baby-phi vs `<root>/docs/v0/proposal/plan/_cycle-index.md` for i-phi), the cycle-folder path, and skips the `check-doc-links.sh` invocation gracefully when the project has no `scripts/` directory. Procedure step 6 now explicitly appends the cycle-index row at chunk-open (was silently skipped in v1 for i-phi; surfaced by CH-01-i-phi cycle-audit §6 deviation 1 when the orchestrator + implementer worked around the omission). Output format gains a `Project:` row + a `Cycle-index row:` status line. Mirrors the 2026-05-16 v15/v10/v9/v5 omnibus pattern that hit the four chunk-* agents (chunk-archive-plan was missed in that omnibus). **(Row 2 — chunk-initiate Phase 0 step 7, MEDIUM)**: on missing forward-scope, no longer abort outright — surface a 4-option inline-draft offer via AskUserQuestion (A: orchestrator drafts inline → commits as prerequisite → resumes; B: pause for user-authored draft + re-invoke; C: use chunk-graph row as stand-in with deviation logged; D: abort whole cycle). Improves UX for the 14+ remaining i-phi chunks. Forward-scope file path conventions documented per-project. **(Row 3 — i-phi `_cycle-index.md` header, MEDIUM)**: column-semantics paragraph added (`Hex` / `Slug` / `Phases` / `Auditors` / `Iterations` / `Status` / `Retro` definitions + canonical `in-flight → ready-for-audit → audited-pending-retro → retro-complete` Status transitions + row-lifecycle paragraph naming chunk-archive-plan / implementer-at-P-SEAL / orchestrator-at-gate-3/4 / retro-close as the canonical row-edit points). i-phi-only; baby-phi left untouched (its 25+ established cycles set the precedent without needing in-file codification). **(Row 4 — informational ledger entry; NO file edit)**: pattern documented in CH-01-i-phi retrospective §3.5 — "when onboarding a new submodule, anticipate `Bash(git -C /root/projects/phi/<submodule> *)` allow-rule will be needed; orchestrator's pre-cycle-zero checklist for a new project should verify the matching git-C rule exists". Inaugural-i-phi-project-rule-gap pattern; first data point from CH-01-i-phi's mid-cycle settings.json edit (2026-05-17T09:23:29Z adding the i-phi git-C rule after 6 PermissionRequest fires). **(Row 5 — chunk-auditor v9 → v10, LOW)**: bless `touch <PROJECT_ROOT>/src/lib.rs` as canonical cache-invalidation for clippy re-runs after implementer's preceding `cargo build` populated the incremental cache. Metadata-only (no file content changes; mtime bump only) — does NOT violate the read-only-on-source discipline. CH-01-i-phi Audit A used this once successfully on `src/lib.rs`; v10 codifies the pattern with usage-doc + a heavier alternative (`cargo clean`) for the case where the result is suspect AND time permits full re-compile. **(Row 6 — chunk-retrospector v5 → v6, LOW)**: starting CH-02-of-any-project, §3.5 MUST include a "Mid-cycle settings.json edits — cross-cycle trend" table (rows: cycle / date / rule added/edited / trigger pattern / validation status). Pre-seeded with two known data points: CH-08 baby-phi (bash-check rule widening 2026-05-08) + CH-01 i-phi (git-C i-phi rule add 2026-05-17). Codifies the "healthy responsive-tightening" pattern as a recurring artefact. Drop a cluster after 3 consecutive zero-fire cycles (mirrors §B regression-protection lifecycle). **Cycle context**: CH-01-i-phi was the cleanest first-cycle landing in pipeline history (15/15 audit claims PASS, 0 iterations, 0 patches, 0 implementation deviations, 0 planner-vs-user divergence). i-phi cross-cycle divergence baseline starts at 0-of-1; baby-phi remains 5-of-7 through CH-25; combined window 5-of-8. |
| 2026-05-17 | `.claude/skills/{chunk-initiate,chunk-archive-plan,chunk-template-fill,audit-envelope-size,phi-core-leverage-check,k8s-readiness-check,ci-guards-run,permissions-audit}` (layout migration — flat `.md` → `<name>/SKILL.md` folder form) | all 8 skills moved | out-of-band: skill-registration fix (planning archive `i-phi/docs/v0/proposal/plan/chunk-initiate-skill-ca633d30.md` — see 2026-05-17 status update note) | **Skill-folder layout migration.** All 8 skills under `.claude/skills/` originally landed as flat files (`<name>.md`); Claude Code's slash-command registry does NOT discover that form — it scans `<skill-name>/SKILL.md` only. As a result, none of the chunk-* skills (including the new `chunk-initiate`) were appearing as `/<skill-name>` in the harness; they were only reachable as plain markdown via `Read` (which is what the chunk-* agent prompts always did, so the chunk pipeline itself was unaffected). Migrated each skill via `git mv .claude/skills/<name>.md .claude/skills/<name>/SKILL.md`. Verified in-session: the post-migration `available-skills` list now includes all 8. Path references updated: (a) `chunk-initiate/SKILL.md` Critical files list (versions also bumped to v15/v10/v9/v5 matching the 2026-05-16 agent bumps; stale "Follow-up TODOs" for those four bumps removed); (b) `permissions-audit/SKILL.md` footer template (the "Generated by …" line emitted in §3.5 retro reports); (c) `.claude/hooks/README.md` "Consumed by" pointer. Historical references inside past retrospectives, plans, audits, and earlier rows of this changelog were **left untouched as frozen records** — they describe the file layout as it was at the time. Frontmatter `version: 1` field surfaces a VS Code-extension hint ("not a supported attribute"; supported set: argument-hint, compatibility, context, description, disable-model-invocation, license, metadata, name, user-invocable). Not blocking; can be moved into `metadata:` in a follow-up if desired. |
| 2026-05-16 | `chunk-planner.md` (v14 → v15) + `chunk-implementer.md` (v9 → v10) + `chunk-auditor.md` (v8 → v9) + `chunk-retrospector.md` (v4 → v5) | all four chunk-* agents bumped | out-of-band: follow-up to chunk-initiate v1 introduction (planning archive at `i-phi/docs/v0/proposal/plan/chunk-initiate-skill-ca633d30.md`); originally listed as deferred in that plan, landed in the same session per user request | **Project-aware path resolution across all four chunk-pipeline agents.** Each agent now reads a `PROJECT_ROOT` parameter from the orchestrator's runtime prompt and resolves paths relative to it. **Default behaviour unchanged**: if `PROJECT_ROOT` is unset or absent, every agent behaves exactly as its previous version (baby-phi paths, K8s readiness check, CI guards, canonical scripts). **i-phi overrides**: when `PROJECT_ROOT = /root/projects/phi/i-phi`, agents translate paths to i-phi conventions — cycle folder relocates from `baby-phi/docs/specs/plan/build/...` to `i-phi/docs/v0/proposal/plan/build/...`; K8s readiness check is skipped (no K8s posture); CI guards are skipped (no `scripts/check-*.sh` yet); cargo manifest uses `<PROJECT_ROOT>/Cargo.toml`; concept docs sit at `<PROJECT_ROOT>/docs/v0/{proposal,specs,design,user-guide}/...`. Each agent's "Project context" section is inserted between the intro paragraph and `## Inputs the orchestrator provides`, ~13–17 lines per agent. The per-chunk-template path stays at `baby-phi/docs/specs/v0/implementation/m5_1/process/per-chunk-planning-template.md` for both projects (template is shared cross-submodule). Skipped K8s / CI-guard claims surface as "paperwork-side observations, not code FAILs" in the audit log for i-phi runs. These four bumps unblock `/chunk-initiate project=i-phi` end-to-end execution — first exercise will be i-phi CH-01 (phi-core consumption foundation). Closes the deferred follow-up listed in the chunk-initiate-skill-ca633d30 plan archive (plan archive amended with a "Status update" note recording the landing). |
| 2026-05-16 | `.claude/skills/chunk-initiate.md` (NEW) | chunk-initiate v1 (initial release) | out-of-band: user-requested orchestrator-tier skill creation 2026-05-16 (planning archive at `i-phi/docs/v0/proposal/plan/chunk-initiate-skill-ca633d30.md`) | **NEW skill — first orchestrator-tier skill in the project.** `chunk-initiate` codifies the conversational orchestrator role into a user-invocable skill that runs end-to-end chunk cycles: plan → user-approval gate → implement → audit (parallel auditors per envelope) → mandatory final cycle re-audit (orchestrator gate-4) → cleanup (cargo clean) → optional retrospective. **Required inputs**: `chunk` (CH-NN), `project` (`baby-phi` \| `i-phi`), `approval` (`yes` \| `no`). **Optional inputs**: `resume_from_phase` (`plan` / `implement` / `audit` / `retro`), `skip_retrospective`, `audit_envelope` (`small` / `medium` / `large` / `auto`), `dry_run`. Skill spawns the four existing `chunk-*` agents in sequence with project-aware path context. Direct-approval criteria mirror CLAUDE.md; auto-approve fires only when `approval=no` AND all criteria hold, else falls back to user prompt via AskUserQuestion. Split-decision auto-detects via two-or-more triggers (scope > 1.5×, audit envelope = `large`, confidence < 9/10, deliverables > 5 surfaces, new architectural surface). Cargo-clean discipline at both placements (immediate-post-test + gate-5 final close). MUST-RUN list (clippy + 4 CI guards for baby-phi; clippy only for i-phi) runs authoritatively at Phase 4. Doc-sync widened sweep phrase set baked in. **Follow-up TODOs (deferred)**: `chunk-planner` v14 → v15 / `chunk-implementer` v9 → v10 / `chunk-auditor` v8 → v9 / `chunk-retrospector` v4 → v5 to honour a `$PROJECT_ROOT` parameter, defaulting to baby-phi for backward compatibility. These updates don't block the skill landing but **do** gate full i-phi support. v1 will be first exercised against i-phi CH-01 (phi-core consumption foundation). |
| 2026-05-16 | `chunk-planner.md` (v13 → v14) + `chunk-implementer.md` (v8 → v9) + `chunk-auditor.md` (v7 → v8) + `.claude/settings.json` (Row 6 allow rule) | chunk-planner v13 → v14; chunk-implementer v8 → v9; chunk-auditor v7 → v8 | CH-25 retro `ch-25-agent-as-creator-and-owner-1e01618e/retrospective.md` §4 Rows 1+3 (Bundle A) + 2+4 (Bundle B) + 6 | **Omnibus standards-update batch landing 5 rows + deferred Row 5 to CH-26.** **(Bundle A — chunk-planner v14)**: **(R1) Handler-gating verification at plan-draft** — when plan claims a scenario "exercises permission-check via handler Y" (in §7 phase deliverables OR §11 audit-prompt items OR ADR §"Acceptance scope"), planner MUST `grep -n check_permission` in `handlers/<Y>.rs` at plan-draft time. If the grep returns 0 hits, the handler is gated only by `AuthenticatedSession` (or equivalent identity gate); literal scenario CANNOT exercise the synth-grant. **Re-frame as engine-level test** (call `handler_support::check_permission` directly + assert Allow/Deny verdict) BEFORE plan-locking the ADR §"Acceptance scope". Document the verification grep at plan-draft in §3 cascade map + §7 deliverable text. Closes CH-25 Audit-C Claim 6 PARTIAL: CH-25 ADR-0060 §D60.4 described literal "A1 disables A2 via `disable_system_agent` handler" scenario; implementer discovered handler doesn't invoke check_permission; shipped load-bearing engine-level form; orchestrator Trivial-multi patch appended re-interpretation paragraph. v14 R1 complements v13 R1 (existence) by verifying behaviour (handler invocation) — together form 2-tier plan-time precision check. **(R3) Phi-core HEAD delta pre-flight at chunk-open** — at P0 run `git -C /root/projects/phi/phi-core log --oneline <last-cycle-close-SHA>..HEAD` to enumerate phi-core API surface changes since last cycle. Classify each commit as API-additive (new field/method) / API-breaking (removed/renamed) / API-internal. For API-additive + API-breaking, plan §7 P0 deliverable adds explicit carrier-fix task with file:line + construction site. Surfaces required carrier-fixes BEFORE plan-locking. **If phi-core HEAD has uncommitted feature work** (`git -C phi-core status --short` non-empty), pause via AskUserQuestion + escalate. NEW pattern surfaced by CH-25's `launch.rs:567 response_format: ResponseFormat::default()` carrier-fix — phi-core HEAD `d6f6998` 0.7.0 added `response_format` to `AgentLoopConfig` (structured-output feature); implementer initially framed as out-of-scope; orchestrator re-classified as routine cross-submodule API integration. **Operational note**: phi-core 0.7.0 is published to crates.io; baby-phi MAY migrate from git-submodule to `phi-core = "0.7.0"` to isolate from HEAD churn — separate architectural decision (NOT v14 standards-update); track as forward-routing candidate for M6 plan-open OR dedicated M5.3 carve-out chunk. **(Bundle B — chunk-implementer v9 + chunk-auditor v8)**: **(R2) P-SEAL paperwork checklist explicit cycle-index top verified-header prepend** — chunk-implementer v9 procedure step 8 refined: ALSO prepend a NEW verified-header line at TOP of `_cycle-index.md` describing this chunk-seal (above prior cycle's verified-header). Two-step verification at chunk-close: (a) `grep -n <cycle-hex>` returns ≥ 1 hit (the row); (b) `head -1 | grep -c <cycle-hex>` returns 1 (the verified-header prepend). Closes Audit-B Trivial-1L: CH-25 implementer's P-SEAL closeout appended row + Status cell but skipped verified-header prepend; orchestrator-applied Trivial-1L closed inline at gate-3. Trivial-1L recurrence delta: 3 cycles since CH-17 (CH-18/19/20/24 all passed; CH-25 surfaced again). v9 codifies the two-step verification. **(R4) Canonical-script-reuse mandate strengthening** — chunk-implementer v9 + chunk-auditor v8 mirror: BEFORE authoring any new `audit-tmp-*.sh`, FIRST run `ls scripts/audit-tmp-*.sh` to enumerate existing canonical scripts. If existing script covers need with minor parameterization, ADAPT THE INVOCATION instead of authoring new. Canonical script set: `scripts/audit-tmp-cargo-counts.sh` (full-workspace cardinality extraction). CH-25 implementer authored 2 ephemeral scripts (`audit-tmp-ch25-permissions.sh`, `audit-tmp-tally-counts.sh`); audit-A authored 1 (`audit-tmp-cardinality-detail.sh` for diagnostic specialization); orchestrator cleaned up at gate-5. Cumulative audit-tmp count trends upward across cycles; v9/v8 mandate slows future growth. **(Row 6 — settings.json)**: NEW allow rule `Bash(RUSTFLAGS="-Dwarnings" /root/rust-env/cargo/bin/cargo build *)` (line 20 after the existing clippy rule). Closes 7-prompt hot cluster from permissions-audit skill v4 first-cycle empirical report at CH-25 retro §3.5. Low-risk single-line addition; mirrors the existing clippy + cargo build allow rules. **(Row 5 DEFERRED to CH-26)**: concurrent-cargo-test target/ contention policy — user provided phi-core 0.7.0 breaking-change context + suggested baby-phi may migrate from git-submodule to `phi-core = "0.7.0"` from crates.io to isolate from HEAD churn; treats Row 5 as a phi-core stability issue rather than chunk-auditor coordination issue. Routing: forward to CH-26 retro for re-evaluation after the phi-core migration decision is taken. |
| 2026-05-11 | `chunk-planner.md` (v12 → v13) + `per-chunk-planning-template.md` (§3.E NEW + §4 non-terminal-drift rule) + `D-new-28.md` (R4-housekeeping 1-line) + `forward-scope/22035b2a-remaining-scope-post-m5-p7.md` (§2.5 CH-25 carry-forward R5 deferral) | chunk-planner v12 → v13 | CH-24 retro `ch-24-carryover-reverification-m5-seal-5778bb77/retrospective.md` §4 rows 1–6 (user chose Apply R1+R2+R3+R4+R6 + Defer R5 to CH-25 + R4-housekeeping inline) | **Omnibus v12→v13 with 5 sub-updates (Plan-time precision triad + Drift M*-DEFERRED-NN + Gate-2.5 lane + v9 re-evaluation)**. **(R1) Closed-set audit-prompt verification** — planner MUST verify each closed-set member (CLI subcommands / HTTP routes / trait methods / enum variants) against actually-shipping code via `git grep` before drafting audit-prompt items spanning that closed set. Closes CH-24 Audit-C #1 (`phi session tail` mis-claim — `tail` folded into Launch per CH-17 ADR-0055; 3-cycle pattern at audit-prompt tier). **(R2) Source-map PRODUCTION-vs-TEST-FIXTURE classification + enum-string verification** — §3 phi-core leverage maps + §7 phase deliverable cascade tables MUST classify every cited `file.rs:NNN` call-site as PRODUCTION or TEST-FIXTURE; every plan-text enum-string claim MUST be verified against canonical `as_str()` body. Closes CH-24 Audit-A #1 (`detail.rs:654` was test-fixture inside `#[cfg(test)] mod tests`, not production call-site) + #2 (plan §7 enumerated `"ended"` but actual enum string is `"completed"`; implementer correctly used `as_str()` rendering). 4-cycle plan-precision pattern (CH-14 cardinality + CH-15 doc-sync widened sweep + CH-17 closed-set + CH-24 source-map). **(R3) Struct-placement dependency-direction verification** — plan §7 struct-placement claims MUST verify dependency direction when type sits in trait-method-return signature. Trait-tier return types cannot reference server-tier or higher-tier structs (server → domain dep direction strict downward per `phi/CLAUDE.md`). For trait-tier returns in `domain::repository::Repository`, canonical home is `domain::model::composites_m<N>` alongside `SessionDetail` / `AgentCatalogEntry`. Closes CH-24 Audit-A #3 (plan §7 v4 indicated `RecentSessionEntry` lives at `server::platform::projects::detail`; architecturally infeasible; implementer correctly placed in `domain::model::composites_m5`). Extends CH-17 ADR-0055 dep-consciousness from subcommand-level to struct-level. **(R4) Drift M*-DEFERRED-NN allocation requirement** — non-terminal drifts (`Status: discovered`/`scoped`) MUST cite explicit `M*-DEFERRED-NN` allocation in `Impl chunk`/`Closing chunk` field, NOT `TBD`/`TBD — likely M6+`. At plan-draft time, planner inspects every drift file the plan touches; any `TBD` triggers a P-DOCS/P-SEAL deliverable promoting it to explicit allocation. For NEW drift files at mid-flight discovery, planner MUST populate explicit allocation at file-creation time. Closes CH-24 Audit-C #7 (12 non-terminal drifts at M5 close: 4 cite explicit `M*-DEFERRED-NN`; 7 cite `TBD`; 1 — `D-new-28` — cited stale `CH-19 (+ M6 review)`). **R4-housekeeping**: 1-line inline patch to `D-new-28` Impl chunk `CH-19 (+ M6 review)` → `M6-DEFERRED-01` (cross-referencing forward-scope §M6-DEFERRED-01 Memory contract). M6 plan-mode-open recommendation: ~0.5 ed mapping all current `TBD`-marked drifts to explicit allocations (11 carry-forward items). **(R6) Gate-2.5 mid-cycle scope-expansion lane formalization + v9 surfacing-not-suppressing re-evaluation** — formalizes mid-cycle architectural scope expansion as a viable workflow (CH-24 first-of-kind: P-NEW-TESTS authoring surfaced `recent_sessions: Vec::new()` hardcoded placeholder; user locked close-in-chunk at gate-2.5; new phase P-FLIP-RECENT-SESSIONS inserted; ADR-0059 ratified + drift remediated in-cycle). NEW optional planner-led template section `§3.E Anticipated gate-2.5 candidates` enumerates surfaces likely to be touched by P-NEW-TESTS/P-DOCS authoring (doc-comments referencing deferred-but-shipping behaviour; placeholder `Vec::new()` returns with inline "ships at M5+" comment; stale transitional struct shapes). Each candidate carries pre-loaded "if discovered at gate-2.5, route to <option-A close-in-chunk via P-FLIP-X phase> OR <option-B file follow-up drift>" recommendation. **v9 surfacing-not-suppressing re-evaluation**: 7-of-9 cumulative cross-cycle divergence (78%) suggests v9 has reached its asymptote; v13 introduces optional divergence-aware framing for forks where 5+ cycle pattern suggests user-preference: planner MAY frame as *"strict-reading recommendation: F<x>.a; but cross-cycle pattern suggests user-preferred F<x>.b — surface both with parity weighting at gate-1"*. Not blanket reversal of v9; selectively applied when cross-cycle pattern is overwhelming. Reverts to strict v9 if 3-cycle stretch flips back to planner-following. **R5 (telemetry-outage skill query)** **DEFERRED to CH-25** per user routing — log is healthy (1290 entries from 2026-05-11) but `permissions-audit` skill reports `Total tool calls observed: 0` for cycle-window query; likely skill query mechanism bug (jq window-predicate / file-path resolution / stdin-vs-file). Forward-scope §2.5 CH-25 row carries explicit carry-forward investigation note; CH-25 retrospector to sanity-check skill output against `grep -c "<cycle-date>" tool-use.log` before publishing §3.5 numbers. |
| 2026-05-10 | `chunk-planner.md` (v11 → v12) | v11 → v12 | CH-20 retro `ch-20-bucket-c-confirm-in-place-240616a4/retrospective.md` §5 row 1 | **Forward-scope drift-count pre-flight verification rule.** For ratification chunks (Bucket B / Bucket C / similar consolidated-doc chunks), the forward-scope row's drift-count parenthetical MUST be empirically verified against the actual drift list at plan-draft time. If off, ADD a META sub-decision in §5 ADR draft + plan a P3 chunk-seal step to amend the forward-scope row inline. CH-20 §D58.10 evidence: forward-scope row read `"(existing 14 items)"` but actual list had 16 IDs; planner caught + amended to `(16 items)`. First time a chunk amends its own forward-scope row at chunk-seal. v12 codifies the discipline. |
| 2026-05-10 | `chunk-planner.md` (v11 → v12) | v11 → v12 | CH-20 retro `ch-20-bucket-c-confirm-in-place-240616a4/retrospective.md` §5 row 2 | **Cross-cycle user-lock-divergence elevated from inline-prepend (v10) to prominent gate-1 callout (v12).** CH-20 retrospective confirmed the divergence pattern is now the modal outcome (4-of-6 cycles: CH-15 F5.B + CH-17 F5.B + CH-18 F3.B + CH-20 F1.B; CH-19 lone non-diverger). Inline-prepend was easy to skip past visually; 4-of-6 pattern justifies a more visible signal. v12 places the callout at the TOP of `## Forks for orchestrator` as a fenced admonition block: *"⚠️ CROSS-CYCLE DIVERGENCE PATTERN: planner-recommendation has diverged from user-lock in N of last M cycles. User systematically prefers tighter / more-fragmented / more-defensive options at gate-1. Treat divergence as the modal outcome, not the exception when reviewing forks below."* CH-19's "doc-only chunks reliably avoid divergence" hypothesis FALSIFIED by CH-20 F1.B; callout applies regardless of chunk shape. Cycle list maintenance: append new cycles as they close; drop earliest if window exceeds last 8; drop callout entirely if 3 consecutive cycles flip back to planner-following. |
| 2026-05-10 | `chunk-planner.md` (v10 → v11) + `per-chunk-planning-template.md` (§5 deliverable 3) | v10 → v11 | CH-19 retro `ch-19-bucket-b-ratification-2c520ba7/retrospective.md` §5 row 1 | **Pre-existing-behaviour preservation note formula relaxation.** CH-14 retro Row 10 codified the strict formula ("Shipped at M5/P<n> close (date YYYY-MM-DD); CH-NN does not change this") which fits sub-decisions ratifying single-date shipped runtime behaviours. CH-19 ADR-0057 surfaced 3 sub-decision shapes that don't fit: §D57.8/§D57.9 (deferred-scope — M6-DEFERRED), §D57.10 (multi-milestone-pattern emerged across M-tags), §D57.6 (never-shipped-yet absence). Audit B PASS-with-caveat noted spirit honored but letter doesn't fit. v11 codifies 3 documented variations: (a) Deferred-scope ("Pre-existing scaffold preserved..."); (b) Multi-milestone-pattern ("Pre-existing implementation preserved..."); (c) Never-shipped-yet ("Pre-existing absence preserved..."). Spirit-of-rule check unchanged: every note MUST identify (i) what was the case before, (ii) whether this chunk changes it, (iii) where historical evidence lives. The 3 variations accommodate sub-decision shapes lacking a single shipped-at date without loosening the rule's spirit. |
| 2026-05-10 | `chunk-implementer.md` (v7 → v8) + `chunk-auditor.md` (v6 → v7) + `chunk-retrospector.md` (v3 → v4) + `CLAUDE.md` (root + baby-phi mirror, §"Orchestrator's gates" gate-5) + `per-chunk-planning-template.md` (§"Chunk-close paperwork" cargo-clean subsection refined) | implementer v7 → v8; auditor v6 → v7; retrospector v3 → v4 | CH-18 retro `ch-18-authrequest-per-state-acl-enforcement-c77937bc/retrospective.md` §5 row 1 | **(USER DIRECTIVE 2026-05-10)** *"tests should be cleaned up immediately after the run as it may block future tests"*. Refines CH-17 retro Row 1 cargo-clean placement: cargo-clean discipline now operates at TWO placements — (1) **immediate-post-test cleanup (NEW per CH-18)**: AFTER each `cargo test --workspace` invocation across the cycle (sub-agent audits A + B per chunk-auditor v7; orchestrator gate-4 final test; retrospector permissions-audit script per chunk-retrospector v4), the invoker MUST run `cargo clean --manifest-path /root/projects/phi/baby-phi/Cargo.toml` BEFORE issuing the next cargo invocation; (2) **gate-5-close final cleanup** (CH-17 retro Row 1, USER REQUESTED 2026-05-09) remains in place. CH-17 retro Row 1's gate-5-close-only placement was insufficient because target/ can balloon DURING gate-4 if multiple test invocations run concurrently or sequentially without cleanup. CH-18 evidence: 2 duplicate cargo-test workspace background runs accumulated target/ to 146 GB → 100% disk → 1h24m hung process → user-directed kill + cargo clean reclaimed 151 GiB. Per-invocation cleanup ensures next invocation starts from clean target/; gate-5 final close ensures clean state at chunk release. Both placements are now mandatory. |
| 2026-05-10 | `chunk-planner.md` | v9 → v10 | CH-18 retro §5 row 2 | **Cross-cycle user-lock-divergence cross-reference rule.** When planner-recommendation differs from `tighter-scope` option AND prior 3 cycles show user-divergence pattern, planner MUST prepend a Cross-cycle pattern note: *"planner-recommendation has diverged from user-lock in 3 of last 4 cycles (CH-15 cycle hex `c3f46f17` / CH-17 cycle hex `40c4d759` / CH-18 cycle hex `c77937bc`). User systematically prefers tighter scope when audit envelope ≤ medium. User is free to lock either option without anchoring on planner-recommendation."* The recommendation field stays — planner continues to surface judgment per existing v9 surfacing-not-suppressing approach. The note explicitly de-anchors. Pattern resolves if user follows planner-rec for 3 consecutive cycles → drop the note. **(User chose Option B at retro batch surface: keep recommendation field + add cross-cycle divergence note.)** |
| 2026-05-10 | `chunk-planner.md` | v9 → v10 | CH-18 retro §5 row 3 | **Pre-flight grep precision for "definitionally redundant" claims.** When plan claims "X is definitionally true at every callsite of pattern Y", pre-flight grep MUST verify literal field assignment at EACH callsite — read AR-construction body (helper called immediately upstream, e.g., `build_<thing>_request`) + verify literal field assignment + cite verifying line in plan §3. If ANY callsite has different field source, flag as scope-narrowing risk in plan §"Forks for orchestrator" with sub-fork capturing structural-mismatch site. CH-18 P2b adopt.rs:96 admin-on-behalf-of-CEO mismatch surfaced at implementation time (`build_adoption_request` sets `requestor: ceo`, `input.actor` = platform-admin — distinct agent IDs); requires synthetic-Draft probe pattern + D-CH18-FOLLOWUP-02 filing. Per-callsite literal-field verification at plan-draft time would have surfaced earlier. |
| 2026-05-10 | `chunk-planner.md` | v9 → v10 | CH-18 retro §5 row 4 | **Cascade-grep extension to wire-mapping functions.** When predicting cascade for new variant on typed enum (e.g., `TemplateError::AccessDenied(...)`), cascade-grep MUST also check **wire-mapping functions** — `http_status_for(&err)`, `wire_code_for(&err)`, `Display::fmt`, `error_to_api_error`, etc. — NOT just typed-error-`match` blocks. Wire-mapping functions enumerate every variant explicitly for HTTP-status/display purposes; new variants ALWAYS require enumerative addition (no `_` catch-all). Cascade-vector enumeration: (1) typed-handler-side `match` blocks (existing v3 discipline), (2) wire-mapping functions (NEW v10 — predict cascade ≥ count of distinct wire-mapping functions), (3) `From` impls. CH-18 P2a evidence: plan predicted 0-cascade via `_.to_string()` catch-all; reality: 4 inline enumerative additions across `templates/mod.rs::http_status_for` + `templates/mod.rs::wire_code_for` + `projects/mod.rs::Display::fmt` + `handlers/projects.rs::error_to_api_error`. |
| 2026-05-10 | `chunk-auditor.md` | v6 → v7 | CH-18 retro §5 row 5 | **Auditor canonical-script-reuse mandate + immediate-post-test cargo-clean.** Prompt now enumerates canonical `scripts/audit-tmp-*.sh` scripts (`audit-tmp-cargo-counts.sh` for cardinality extraction). When auditor needs cardinality extraction, MUST use existing canonical script — don't author duplicates. If new script IS needed, MUST commit via Write tool with path matching `audit-tmp-*.sh` glob (settings.json line 45 covers invocation). Also adds immediate-post-test cargo-clean after Step 4 cargo test workspace run (Row 1 application). CH-18 evidence: Audit A authored orphan `scripts/audit-tmp-cardinality.sh` duplicating canonical `scripts/audit-tmp-cargo-counts.sh` (CH-14 retro Row 8 codified). Orphan deleted at gate-5 close; chunk-auditor v7 prevents recurrence. |


| Date | Target | Version | Source retro | Change |
|---|---|---|---|---|
| 2026-05-02 | (initial landing) | n/a → v1 | meta-plan `agentic-workflow/multi-agent-chunk-pipeline-0853574c.md` | First cut: 4 agents (chunk-planner / chunk-implementer / chunk-auditor / chunk-retrospector), 6 skills (phi-core-leverage-check / k8s-readiness-check / chunk-template-fill / ci-guards-run / chunk-archive-plan / audit-envelope-size) |
| 2026-05-03 | `chunk-auditor.md` | v1 → v2 | CH-11 retro `ch-11-per-session-consent-gating-d5428c43/retrospective.md` §5 row 1 | Added "Sandbox-blocked invocations" subsection. `RUSTFLAGS="-Dwarnings" cargo clippy ...` + `bash scripts/check-*.sh` are routinely sandbox-blocked from sub-agent shells; auditor must mark NOT-EXECUTED-IN-AUDIT + provide grep equivalents + defer to orchestrator's final cycle re-audit. Closes the gap from CH-11 Audit A iter 1 claims 15 + 16. |
| 2026-05-03 | `chunk-planner.md` | v1 → v2 | CH-11 retro §5 row 4 | Added "Cascade fan-out estimation" subsection. Planner MUST paste exact `git grep -n` invocation + raw matched-line count when predicting literal-struct fan-out; pause-discipline trigger expressed as `> 1.5× predicted` (percentage), not absolute count. Closes the cascade-undercount gap (CH-11 Grant: planned ~6 / actual ~28; Organization: planned ~10–15 / actual ~27). |
| 2026-05-03 | `CLAUDE.md` (×2: repo + baby-phi) | n/a (not versioned) | CH-11 retro §5 rows 2 + 5 | Two changes to "Multi-agent chunk pipeline" section: (a) gate 4 explicit MUST-RUN list (clippy + 4 CI guards) — sub-agent sandbox cannot run these; (b) Trivial FAIL split into Trivial-1L (orchestrator verifies in cycle-audit, no re-spawn) vs Trivial-multi (re-spawn auditor). Closes audit-fix-loop ambiguity from CH-11. |
| 2026-05-03 | `per-chunk-planning-template.md` | n/a (not versioned) | CH-11 retro §5 rows 3 + 6 + 7 | Three changes: (a) §10 close criteria: P4 paperwork checklist requires verified-header description matches body diff; (b) §8 tests summary: × 1.10–1.15 buffer factor for healthy implementer over-shoot, ±15% accept band at orchestrator per-phase gate; (c) §9 reading list: conditional bullet — engine-touching chunks must read launch.rs + preview.rs bodies. Closes 3 gaps from CH-11 (audit-B-claim-7 verified-header overpromise; tests-delta calibration; pre-existing M5 manifest-shape drift). |
| 2026-05-03 | `chunk-retrospector.md` | v1 → v2 | meta-plan `permissions/tool-use-logging-and-permissions-audit-skill-18564835.md` §4 | Frontmatter `skills: (none) → permissions-audit`. Added procedure step 5b that invokes the skill with cycle window + prior retros. Added new §3.5 "Permissions audit findings" section to the retrospective template (Hot candidates / Dead rules / Hook FP flags / Cross-cycle trend / Audit-driven standards updates). Added "Appendix — Permissions audit (full)" at end of template. Quality bar bumped to require all 7 sections + appendix. Length budget raised 1–4 KB → 4–10 KB. Output handoff now includes 5-line audit summary block. |
| 2026-05-03 | `permissions-audit` (skill, NEW) | n/a → v1 | meta-plan `permissions/tool-use-logging-and-permissions-audit-skill-18564835.md` §3 | New skill at `.claude/skills/permissions-audit.md`. Reads `.claude/tool-use.log` + settings.json, filters by cycle window, cross-references rules, classifies findings into 8-section markdown report (§A tool distribution, §B hot candidates, §C auto-approved, §D rule utilization, §E hook denials, §F high-frequency rejects, §G cross-cycle trends, §H standards updates). Invoked by retrospector v2 at procedure step 5b. |
| 2026-05-03 | `log-tool-use.sh` (hook, NEW) | n/a → v1 | meta-plan `permissions/tool-use-logging-and-permissions-audit-skill-18564835.md` §2 | New hook script at `.claude/hooks/log-tool-use.sh`. Wired three times in settings.json (PostToolUse / PostToolUseFailure / PermissionRequest), each registration passing the event name as `$1` for schema-version-independent differentiation. Emits JSONL to `.claude/tool-use.log` (gitignored). Features: env-var redaction (SECRET/TOKEN/PASSWORD/KEY/CREDENTIAL), 1000-char truncation, 10MB rotation (5 retained), flock-based concurrency safety with 1s timeout (skip-on-contention), self-skip on log-file references. Fail-safe: always exits 0 even on error. |
| 2026-05-04 | `chunk-planner.md` | v2 → v3 | CH-12 retro `ch-12-frozen-session-tag-immutability-6a748175/retrospective.md` §5 rows 2 + 4 + 5 + 7 | Four changes: (a) "Re-spawn re-verification on user-locked-divergent fork" subsection — when orchestrator re-spawns with a user-locked fork that diverges from planner-recommendation, planner MUST re-verify all auto-approval criteria on the locked path + state new verdict in iter-N banner + escalate via Forks-for-orchestrator section if any criterion now fails (closes CH-12 F5.B-divergence handling); (b) "Additive-enum cascade discipline" subsection — for additive enum variants, run `git grep -nE 'match.*\<X\>.*\\{' ...` + check `_ =>` / `other =>` catch-all pattern; predict 0 callsite edits if catch-all is dominant ≥ 80% (3-cycle pattern: CH-05 + CH-09 + CH-12 all required 0 edits; complements CH-11's struct-cascade discipline); (c) "Tag-write Repository contract reading-list conditional" — when chunk introduces tag-write Repository method (`update_*_tags` etc.), reading list MUST include `repository.rs` module-level docstring + close-criteria MUST include the validator+audit-event paired-precondition bullet (CH-12 forward-defensive contract from ADR-0049 §D49.5 + §D49.7); (d) "Citation freshness" — line citations MUST come from final pre-publish `grep -n` re-check (CH-12 audit A iter 1 found 3-line citation drift). |
| 2026-05-04 | `chunk-implementer.md` | v1 → v2 | CH-12 retro §5 row 6 | Added "Bash usage discipline" subsection. Per repo CLAUDE.md "avoid usage of cd": prefer absolute-path forms over `cd && cmd` compounds; use `cargo --manifest-path` instead of `cd && cargo`; use absolute paths for `bash scripts/check-*.sh`. CH-12 telemetry recorded 18 PermissionRequest prompts for `cd:/root/projects/phi/baby-phi` against 86 auto-approved invocations — reducing compound `cd` usage cuts cycle latency. |
| 2026-05-04 | `chunk-auditor.md` | v2 → v3 | CH-12 retro §5 row 6 | Mirror of chunk-implementer change: added "Bash usage discipline" subsection. Auditors are read-only; absolute-path Bash invocations are the natural fit + sub-agent shell working-directory persistence makes stray `cd` mid-audit risky for later relative paths. |
| 2026-05-04 | `per-chunk-planning-template.md` | n/a (not versioned) | CH-12 retro §5 rows 1 + 3 | Two changes: (a) §10 P4 paperwork checklist addendum — `_concept-audit-matrix.md` Status MUST be copy-pasted letter-for-letter from plan §2 target column; split-axis cases keep original Status with axis qualification in evidence cell (closes F-AUDB-1 from CH-12 audit B iter 1); (b) §8 tests-summary prose refined to asymmetric ×1.0–×1.20 accept band (deliverable-listed sum lower bound; ×1.20 upper bound) — confirms CH-11-codified ×1.10–1.15 buffer with CH-12's bull's-eye 1365 vs 1360–1366 prediction band. |
| 2026-05-04 | `.claude/settings.json` + `block-destructive-bash.sh` + `hooks/README.md` | n/a (config) | out-of-band: in-conversation user request after CH-12 close, grounded in CH-12 PermissionRequest telemetry (28 events; 26 explained by missing read-only allow rules) | (a) Added 10 Bash allow rules: `grep:*`, `ls:*`, `find:*`, `wc:*`, `head:*`, `tail:*`, `paste:*`, `bc:*`, `cargo --version:*`, `cargo -V:*`. (b) Extended `block-destructive-bash.sh` with `find -delete` deny pattern (closes the blanket `find:*` allow gap; defense-in-depth). (c) Updated `hooks/README.md` "Blocked patterns" table with the new `find -delete` row. Plan archive: `/root/.claude/plans/sharded-discovering-stearns.md`. |
| 2026-05-04 | `chunk-planner.md` | v3 → v4 | CH-13 retro `ch-13-audit-class-composition-strictest-wins-d4fe1b7c/retrospective.md` §5 row 2 | Strengthened struct-cascade prediction discipline (3rd refinement of the same standards rule). Cascade fan-out estimation now MUST paste THREE artifacts: (a) the `git grep -n` invocation, (b) the raw matched-line count, (c) **the per-file breakdown** of the grep output. Forces the planner to walk the full output rather than mental-count from a partial scan. Closes CH-11 + CH-13 struct-cascade undercount pattern (CH-11 Grant 4.7× under, CH-13 Grant ~10× under). If CH-14 still under-predicts a struct cascade, escalate to user for a different shape (planner saves grep output to plan archive; orchestrator double-checks during plan-approval). |
| 2026-05-04 | `chunk-implementer.md` | v2 → v3 | CH-13 retro §5 row 4 | Added "Edit-tool discipline for line-number citation refresh" subsection. When refreshing sequences of line-number citations: prefer surgical `Edit` calls with surrounding context over chained `replace_all` calls (sequential shifts double-shift if later patterns appear in earlier-edited context). When sequences are unavoidable, run in descending-shift order. CH-13 P3 hit this 3× during plan-archive citation refresh; all caught at review. |
| 2026-05-04 | `per-chunk-planning-template.md` | n/a (not versioned) | CH-13 retro §5 row 1 | Added "ADR-body checklist (v2026-05-04)" addendum at §5: every ADR drafted by a chunk MUST include (a) §"Forks" header with explicit user-lock outcome (direct-approval cycle states "user-locked at plan approval to F1.A / F2.A / F3.A"; divergent cycle states the divergence explicitly); (b) §"Cross-references" with all 4 categories (originating concept-doc + section + line range; closed drift(s); prior ADRs; **forward-scope row** that scoped this chunk). Closes CH-13 Audit B Trivial-1L gaps F-AUDB-1 (Forks header) + F-AUDB-2 (forward-scope cross-ref). |
| 2026-05-04 | `.claude/settings.json` | n/a (config) | CH-13 retro §5 row 6 | Added Bash allow rule `Bash(bash /root/projects/phi/baby-phi/scripts/check-*.sh:*)` (absolute-path wildcard). Closes the orchestrator gate-4 MUST-RUN friction surfaced by CH-13 telemetry (4 PermissionRequest prompts on `bash /root/projects/phi/baby-phi/scripts/check-*.sh` invocations). The pre-existing `Bash(bash scripts/check-*.sh)` rule was relative-path-scoped; this complements it with the absolute-path form preferred by CH-12 retro Row 6 cd-overuse discipline. |
| 2026-05-04 | `_changelog.md` (this file) | n/a (affirmation entry, not a version bump) | CH-13 retro §5 row 3 | Affirmation entry for the 3-cycle confirmation of the asymmetric ×1.0–×1.20 buffer band: CH-11 +22 over conservative target → buffer codified at CH-11 retro Row 6; CH-12 +1 over (bull's-eye dead-centre); CH-13 +0 (lower-edge bull's-eye). 3-cycle calibration confirmed. **No further test-count standards refinement needed.** Continue tracking across CH-14/15. |
| 2026-05-04 | `.claude/settings.json` + repo `CLAUDE.md` + `chunk-implementer.md` v3→v4 + `chunk-auditor.md` v3→v4 + NEW `permissions/granular-bash-discipline-ab19399b.md` | configs + agents v3→v4 | out-of-band: in-conversation user request after CH-13 close, grounded in CH-13 PermissionRequest telemetry (312 events; ~150 multi-line-script fragments + ~45 cd-compounds + ~7 git -C + ~98 other) | (a) Elevated **Granular Bash** to top-level discipline in repo `CLAUDE.md` + chunk-implementer/auditor v4 prompts: one logical operation per Bash invocation; no multi-line scripts; no `&&`/`;`/long-pipe chains; absolute paths only. (b) Converted 54 colon-star Bash rules to space-star (`Bash(prefix:*)` → `Bash(prefix *)`; Occam's razor — both forms equivalent at end-of-pattern per Claude Code docs). (c) Added 7 Bash allow rules: 3× `git -C /root/projects/phi{,/baby-phi,/phi-core} *` + `xargs *`, `stat *`, `cat *`, `diff *` (tee dropped per user feedback). (d) NEW design doc `granular-bash-discipline-ab19399b.md` captures the principle + matcher semantics + CH-13 prompt analysis + anti-pattern catalogue. Plan archive: `/root/.claude/plans/sharded-discovering-stearns.md`. |
| 2026-05-07 | `chunk-planner.md` | v4 → v5 | CH-07 retro `ch-07-multi-scope-cascade-contractor-model-cc912d07/retrospective.md` §5 row 1 | Added "Pre-archive line-number re-verification" step (procedure step 11). Planner re-runs every §3 grep against current git HEAD immediately before plan-archive + updates line citations if any drifted. Closes the line-number drift Audit A flagged at CH-07 gate 4 (`expansion.rs:55` cited in plan; actual `:56` due to a 1-line change above the struct between plan-draft and audit-execute). |
| 2026-05-07 | `per-chunk-planning-template.md` | n/a (not versioned) | CH-07 retro §5 rows 2 + 4 | Added §3 "cascade-artifact discipline" sub-section. Two caveats: (a) per-file edit-count predictions are approximate; aggregate band is the load-bearing prediction (CH-07 §3 Artifact D predicted 8–14 sites across ~6 files; actual 14 across 7 files — aggregate held, per-file precision did not); (b) when a concept-doc semantic could land at multiple pipeline steps, §3 MUST explicitly state which step + cite the rationale (CH-07 §3 Artifact B exemplar: concept 06 line 162 placed at `step_2a_ceiling` not `step_5_scope_resolution`; placement justified inline). |
| 2026-05-07 | `plan/forward-scope/22035b2a-remaining-scope-post-m5-p7.md` (CH-07 row, retroactive) | n/a (forward-scope) | CH-07 retro §5 row 3 | Added "(informal labeling for 5 distinct outcomes — concept-doc canonical phrasing is '2 main tiers + base_X tie-breakers + intersection fallback')" parenthetical to CH-07 row "Project → Org → base_project → base_org → intersection fallback" wording. Closes the F1-shape fork that consumed plan-approval AskUserQuestion bandwidth at CH-07. Convention going forward: when forward-scope row labeling diverges from concept-doc canonical phrasing, the row MUST adopt concept-doc phrasing OR carry an explicit informal-labeling parenthetical. |
| 2026-05-07 | `.claude/settings.json` | n/a (config) | CH-07 retro §5 row 5 | Added 2 paired Bash allow rules to capture the redirect+pipe combo that the existing `Bash(bash /root/projects/phi/baby-phi/scripts/check-*.sh *)` rule (added per CH-13 retro Row 6) failed to match: (a) `Bash(bash /root/projects/phi/baby-phi/scripts/check-*.sh:*)` colon form; (b) `Bash(bash /root/projects/phi/baby-phi/scripts/check-*.sh 2>&1*)` explicit redirect prefix. Closes the 3 PermissionRequest prompts CH-07 saw on `bash check-doc-links.sh 2>&1 | tail -30`-shape invocations — gets the cycle to PermissionRequest target < 5. CH-08+ retros track the bash-check signature count as the validation signal. |
| 2026-05-07 | `permissions/granular-bash-discipline-ab19399b.md` | n/a (design doc) | CH-07 retro §5 row 6 | Added §2.4 "Empirical observation from CH-07 (redirect+pipe combo defeats single-`*` glob)". Documents the third matcher quirk: `Bash(prefix *)` (space form) does NOT match `prefix arg 2>&1 \| tail -N` while `Bash(prefix:*)` (colon form) does. Workaround: prefer colon form for rules that may match commands with trailing `2>&1` redirects. Cross-refs CH-07 retro §5 row 5 + the paired settings.json rules. |
| 2026-05-07 | `.claude/skills/permissions-audit.md` | n/a (skill, v1 unchanged but procedure refined) | CH-07 retro §5 row 7 | Added "Regression-protection step" to §B (Hot allow-rule candidates). When prior-retro input lists a hot signature that had a standards update applied, verify in current cycle that PermissionRequest count for that signature is 0; if non-zero, escalate as `rule-pattern-failed-validation` with high priority + propose refined rule. Rationale: CH-07 caught CH-13's bash-check rule's continued failure post-rule-addition (3 prompts despite rule existing); this regression-protection step makes the failure surface in §B output instead of relying on orchestrator manual catch. |
| 2026-05-08 | `chunk-planner.md` | v5 → v6 | CH-08 retro `ch-08-allocate-transfer-cardinality-refinements-7cbe74a4/retrospective.md` §5 row 1 | Added milestone-prefixed-path requirement to ADR §5 cross-ref checklist quality bar bullet. When listing prior-ADRs cited in `(c)`, MUST cite milestone-prefixed paths (`m3/decisions/...`, `m4/decisions/...`) for any ADR not in the chunk's home milestone. Closes CH-08 P0 ADR-0052 broken-link bug — sibling-style relative paths to cross-milestone ADRs result in `check-doc-links.sh` 404s caught at P3 instead of P0. |
| 2026-05-08 | `per-chunk-planning-template.md` | n/a (not versioned) | CH-08 retro §5 rows 2 + 3 | Added §3 cascade-discipline caveats: (a) cascade-band citations MUST also name the canonical grep-pattern so all 4 lanes (planner / implementer / auditor / orchestrator) converge on one metric (CH-08 saw 27/30/33 across lanes — within tolerance but a measurement-drift signal); (b) before recommending `..Default::default()` cascade strategy, MUST verify all sub-field types derive `Default` — not just the top-level struct (CH-08 caught at P1: Grant could derive Default but PrincipalRef + ResourceRef don't, and adding Default to them would invent semantically-inappropriate placeholders). |
| 2026-05-08 | `chunk-auditor.md` | v4 → v5 | CH-08 retro §5 row 4 | Added "PASS-with-caveat" verdict guidance to "Sandbox-blocked invocations" subsection. When a normally-sandbox-blocked call SUCCEEDS in the audit shell (sandbox behaviour is non-deterministic across sub-agent invocations — CH-08 Audit B saw 4 CI guards execute cleanly while Audit A's same calls were blocked), record verdict as `PASS-with-caveat` (not `NOT-EXECUTED-IN-AUDIT` alone). Caveat reads: "observed PASS in audit shell; orchestrator MUST-RUN gate remains authoritative." Auditor must NOT treat in-shell success as license to skip the orchestrator gate. |
| 2026-05-08 | `chunk-retrospector.md` | v2 → v3 | CH-08 retro §5 row 8 | Added "settings.json mid-cycle edit capture" to procedure step 5b (after permissions-audit skill invocation). Capture `stat -c %y .claude/settings.json` mtime + `git diff HEAD .claude/settings.json` snippet at retro-write time. If mtime falls within cycle window, surface diff in §3.5 — signals out-of-band user-led permissions tuning during cycle (CH-08 user broadened bash-check rule mid-cycle at 07:36 UTC after retro-prep diagnostics). Post-edit settings.json state needs CH-NN+1 regression-validation; flag in §5. |
| 2026-05-08 | `.claude/settings.json` | n/a (config) | CH-08 retro §5 row 5 (user-led mid-cycle edit; codified at retro close) | Broadened bash-check rules: `Bash(bash /root/projects/phi/baby-phi/scripts/check-*.sh *)` → `Bash(bash /root/projects/phi/baby-phi/scripts/*.sh *)` (drop `check-` prefix; rule applies to ALL `.sh` scripts under that directory); also added `Bash(bash /root/projects/phi/baby-phi/scripts/*.sh 2>&1*)` paired redirect-form rule. User-led mid-cycle edit at 2026-05-08 07:36 UTC after retro-prep surfaced 18-prompt regression (15/18 on bash-check cluster; CH-13→CH-07→CH-08 3-cycle pattern). CH-NN+1 must validate PermissionRequest count is 0 for the cluster. If non-zero → `matcher-semantics-investigation` per CH-08 retro §5 row 7. |
| 2026-05-08 | `permissions/granular-bash-discipline-ab19399b.md` | n/a (design doc) | CH-08 retro §5 row 6 | Added §2.5 "Bash-check cluster: 3-cycle pattern (CH-13 → CH-07 → CH-08)". Documents 3-cycle history (CH-13 added rule, 4 prompts → CH-07 refined to colon-form + 2>&1, 3 prompts residual → CH-08 found 15-prompt regression despite both rules). New hypothesis: `*.sh` glob inside `Bash(prefix-expr *)` rule may be interpreted as a literal `*.sh` filename pattern, NOT as a wildcard binding. Workarounds: (a) literal script names per rule (verbose but matcher-deterministic); (b) drop `check-` prefix to `*.sh *` (CH-08 user adopted). Cross-refs CH-08 retro §5 row 7 escalation to matcher-semantics-investigation. |
| 2026-05-08 | `.claude/skills/permissions-audit.md` | n/a (skill v1 procedure refined) | CH-08 retro §5 row 7 | Added "Matcher-semantics-investigation escalation" to §B regression-protection step. When a hot-allow-rule signature persists across ≥ 2 cycles post-standards-update (i.e., rule added at retro N, regression at retro N+1, still regression at retro N+2), escalate from `rule-pattern-failed-validation` to `matcher-semantics-investigation` with **highest priority**. Next standards update MUST include empirical test-harness step: (a) propose candidate rule pattern; (b) run representative invocations against test settings.json; (c) capture post-edit telemetry to confirm 0 PermissionRequest fires; (d) only then commit. Closes CH-13→CH-07→CH-08 bash-check 3-cycle gap where rule-pattern fixes kept missing the actual matcher quirk. |
| 2026-05-08 | `chunk-implementer.md` | v4 → v5 | CH-14 retro `ch-14-system-genesis-authority-chain-revocation-cascade-5803bb94/retrospective.md` §5 row 1 | Added "Chunk-seal cross-check (ADR ↔ drift)" subsection to procedure. After flipping any ADR sub-decision Proposed→Accepted AND filing any new drift in the same chunk, grep both for the same claim (emission-cardinality, state-transition, migration-effect predicates) and confirm they agree. If contradiction detected, escalate to user via implementation report BEFORE chunk-seal commit. Closes CH-14 gate-2 contradiction (ADR-0053 §D53.7 said per-AR emission ships; D-CH14-FOLLOWUP-02 said deferred — orchestrator caught at gate 2 and re-spawned implementer; cross-check would have surfaced earlier). |
| 2026-05-08 | `chunk-implementer.md` | v5 (Quality bar bullet) | CH-14 retro §5 row 2 | Added "Scope-narrowing-decision-must-escalate" bullet to Quality bar. If during P0–P4 the implementer discovers plan scope cannot be fully delivered (e.g., audit-event emission needs deferring to a follow-up drift), MUST flag explicitly in implementation report's §"Forks taken" / §"Notes" with prefix `SCOPE-NARROWING vs plan §X.Y` AND escalate divergence to user before chunk-seal — do NOT silently file a follow-up drift and ship a narrowed deliverable. CH-14 caught this exact pattern: implementer narrowed plan §3.B A7 + ADR-0053 §D53.7 silently → orchestrator surfaced at gate 2 → user-locked Option B re-spawn. |
| 2026-05-08 | `CLAUDE.md` (root) + `baby-phi/CLAUDE.md` (mirror) | n/a (not versioned) | CH-14 retro §5 row 3 | Added "Doc-sync sweep after gate-2 inline corrections" sub-step to §"Orchestrator's gates" gate 2. When a gate-2 inline correction changes implementation behaviour (e.g., shipping per-AR audit-event emission that the chunk-seal-state had deferred), grep the user-facing docs (`docs/specs/v0/implementation/m*/architecture/*.md`, `m*/operations/*.md`, `m1/architecture/audit-events.md`) for stale references to the deferral (`FOLLOWUP-NN`, `deferred per`, `is NOT emitted`, `not emitted at CH-NN`) and patch any matches BEFORE dispatching auditors. CH-14 caught this at audit B iter 1; sweep at gate-2 would have avoided the iter-1 PARTIAL → iter-2 re-spawn. |
| 2026-05-08 | `CLAUDE.md` (root) | n/a (not versioned) | CH-14 retro §5 row 4 | Added "4-stage cardinality-extraction pipeline" antipattern bullet to §"Granular Bash discipline" Discouraged shapes. `cargo test ... 2>&1 \| grep ... \| sed ... \| awk ...` exceeds 2-stage cap. CH-14 gate-4 logged 14 PermissionRequests on a 4-stage `cargo test \| grep \| sed \| awk` pipeline (3.5× the 2-stage cap). Recommended fix: write extraction script to file (`scripts/audit-tmp-cargo-counts.sh`), then run `bash /abs/path/scripts/audit-tmp-cargo-counts.sh` as a single Bash call. Orchestrator gate-4 cargo-test invocation now refactored to this form. |
| 2026-05-08 | `per-chunk-planning-template.md` | n/a (not versioned) | CH-14 retro §5 rows 5 + 11 | Two §3 cascade-discipline caveats (chunk-planner v7 effective 2026-05-08): (a) Migration-number reservation — when plan adds new SQL migration, run `ls /root/projects/phi/baby-phi/modules/crates/store/migrations/` at draft-time and capture next-free slot; do NOT assume sequential numbering from chunk's hex order (CH-14 plan said `0011` was next-free, actual was `0014`). (b) Typed multi-value cascade-result precedent — when cascade method must return multiple semantically-distinct lists, prefer typed struct (`CascadeResult { revoked_grants, cascaded_ars }`) over raw `Vec<X>` or tuple returns; cite CH-14 `repository::CascadeResult` (`repository.rs:172-182`) as precedent. Rationale: handler-side iteration self-documenting; future cascade-result extensions don't break callsite signatures. |
| 2026-05-08 | `.claude/settings.json` | n/a (config) | CH-14 retro §5 row 6 (matcher-bug-confirmed escalation) | Replaced 2 broadened-glob bash-check rules (`Bash(bash /root/projects/phi/baby-phi/scripts/*.sh *)` + `*.sh 2>&1*`) with **5 literal-script-name rules** (`Bash(bash /abs/path/check-doc-links.sh*)`, `check-ops-doc-headers.sh*`, `check-phi-core-reuse.sh*`, `check-spec-drift.sh*`, `audit-tmp-*.sh*`). **PENDING empirical validation per matcher-bug-confirmed escalation protocol** (CH-14 retro §5 row 7): orchestrator must restart session, run each of the 4 CI guards' canonical invocation + 1 representative `audit-tmp-*` invocation, observe `tool-use.log`, verify 0 PermissionRequest fires for the cluster. If still failing → escalate to upstream Claude Code rule-matcher bug-report (do NOT iterate further). 4-cycle bash-check pattern (CH-13: 4 → CH-07: 3 → CH-08: 15 → CH-14: 43) is now empirically confirmed. |
| 2026-05-08 | `.claude/skills/permissions-audit.md` | v1 → v2 | CH-14 retro §5 row 7 | Added 3rd escalation tier `matcher-bug-confirmed` to §B regression-protection step. When hot-allow-rule signature persists across ≥ 3 cycles post-standards-update despite multiple rule-pattern revisions, escalate from `matcher-semantics-investigation` to `matcher-bug-confirmed` with **STOP-iterating-the-rule-pattern**. Retrospector MUST: (a) capture isolated reproducer (rule + invocation pair); (b) file upstream Claude Code rule-matcher bug-report; (c) document root cause + workaround in `granular-bash-discipline-ab19399b.md`; (d) propose behaviourally-equivalent literal-script-name workaround OR script-refactor. Cross-cycle trend-analysis fairness rule: once at `matcher-bug-confirmed`, next cycle's permissions-audit marks cluster as `external-bug-pending` and SUBTRACTS it from cross-cycle PermissionRequest count. Closes CH-13→CH-07→CH-08→CH-14 4-cycle bash-check pattern where rule-pattern iteration burned 4 cycle slots. |
| 2026-05-08 | `scripts/audit-tmp-cargo-counts.sh` (NEW) | n/a → v1 | CH-14 retro §5 row 8 | New script at `baby-phi/scripts/audit-tmp-cargo-counts.sh`. Replaces orchestrator gate-4's 4-stage `cargo test ... 2>&1 \| grep ... \| sed ... \| awk ...` cardinality-extraction pipeline with a single-Bash-invocation script-shaped form. Output: `passed=N failed=M ignored=K` to stdout. Matched by the new `Bash(bash /abs/path/audit-tmp-*.sh*)` literal allow rule (settings.json change). Eliminates the 14-prompt 4-stage friction observed at CH-14 gate-4 + restores 2-stage Granular Bash discipline. |
| 2026-05-08 | `permissions/granular-bash-discipline-ab19399b.md` | n/a (design doc) | CH-14 retro §5 row 9 | Added §2.6 "Bash-check cluster: 4-cycle pattern + matcher-bug-confirmed escalation (CH-14)". Documents 4-cycle data series (CH-13: 4 → CH-07: 3 → CH-08: 15 broadened → CH-14: 43 same broadened). Empirical conclusion: rule-pattern iteration inside `Bash(prefix-expr *)` does NOT close the bash-check cluster — matcher's `*.sh` glob does not bind to wildcard script name at runtime. Per CH-14 retro §5 row 7 escalation: STOP rule-pattern iteration; capture reproducer; file upstream bug-report; deploy literal-script-name workaround; empirical test-harness validation BEFORE commit. Cross-cycle trend-analysis fairness: once classified `matcher-bug-confirmed`, subtract cluster from cross-cycle PermissionRequest count for trend reporting. |
| 2026-05-08 | `per-chunk-planning-template.md` | n/a (§5 ADR-body checklist) | CH-14 retro §5 row 10 | Added "Pre-existing-behaviour preservation note" item 3 to ADR-body checklist (§5). If ADR sub-decision changes audit-event emission semantics, AR/Grant state-transition behaviour, or any prior runtime invariant, MUST document the pre-existing behaviour explicitly in the relevant sub-decision body. Format: "Pre-existing behaviour preserved: `<X>` (see `<file:line>` for the historical implementation; CH-NN does not change this)" or "Behaviour changed: `<X>` (was `<old>` per `<prior ADR §>`; CH-NN flips to `<new>` because `<reason>`)". CH-14 ADR-0053 §D53.7 needed this clarification mid-cycle (level-0 adoption AR's companion event from `revoke_ar` continues to be discarded — pre-CH-14 behaviour preserved verbatim). |
| 2026-05-08 | `chunk-planner.md` | v6 → v7 | CH-14 retro §5 row 12 | Added "Type-derive pre-checks for typed-equality forks" subsection. When a fork specifies typed equality comparison on existing type (e.g., F5.A's `ar.requestor == system_genesis_principal()`), grep type definition in advance to confirm it has required derive (`#[derive(PartialEq, Eq)]`). If missing: option (a) add derive in chunk's scope as small drift; option (b) document `matches!`-based fallback as known cost. CH-14 P3 hit this with `PrincipalRef` lacking `PartialEq` → `is_bootstrap_ar` falls back to `matches!` for requestor witness. Pre-checking at plan-draft time would have surfaced the (a) vs (b) choice explicitly. |
| 2026-05-09 | `chunk-implementer.md` (v7 chunk-close paperwork; cargo-clean RESPONSIBILITY DELEGATED to orchestrator gate-5) + `per-chunk-planning-template.md` (§"Chunk-close paperwork" — gate-5-close disk-reclamation step) + `CLAUDE.md` (root + baby-phi mirror, §"Orchestrator's gates" gate-5 final step) | v6 → v7 | CH-17 retro `ch-17-live-sse-tail-endpoint-40c4d759/retrospective.md` §5 row 1 | **(USER REQUESTED 2026-05-09)** Codified `cargo clean` as a permanent habit. **Placement corrected post-CH-17 close**: cargo-clean is the **orchestrator's last action at gate-5 close**, NOT the chunk-implementer's chunk-seal step. Why gate-5 close: chunk-implementer chunk-seal runs BEFORE gate-4 MUST-RUN (clippy + cargo test) AND BEFORE gate-5 retrospector's permissions-audit scripts — both rebuild target/, so any cargo-clean earlier in the pipeline is wasted. CH-17 ran cargo-clean at chunk-seal per the original mis-placed codification (reclaimed 107 GB), but gate-4 + retro rebuilt to 67 GB; user surfaced the issue at gate-5 close, orchestrator-applied final cargo-clean reclaimed another 68.7 GB to truly close at 154 GB free. Trigger: AFTER standards updates landed + cycle-index row flipped to `retro-complete`, run `/root/rust-env/cargo/bin/cargo clean --manifest-path /root/projects/phi/baby-phi/Cargo.toml` + log disk reclaimed in cycle-audit §7 metrics row. Rationale: CH-17 hit a mid-cycle Bash sandbox failure when compiled test binaries consumed >160 GB on root disk (every cargo/clippy/test/grep returned Exit 1 silently); cleanup at gate-5 close ensures next chunk opens with a clean slate. |
| 2026-05-09 | `chunk-planner.md` | v8 → v9 | CH-17 retro §5 row 2 | Strengthened §3.D `forward-scope-vs-concept-doc precedence` rule with MANDATORY pre-flight check: BEFORE writing any §3.D contradiction claim about closed action vocabulary / fundamental kinds / audit-event names / migration order, planner MUST run `git -C /root/projects/phi/baby-phi grep -nE '^\s*<verb>,$' modules/crates/domain/src/permissions/action.rs` AND verbatim-cite concept-doc 03 line 22 + line 44 (universal-applicability claim) BEFORE issuing the contradiction. CH-15 + CH-17 are the 2 precedent cycles requiring iter-2 re-spawn from incorrect closed-set claims. Pattern-watch: if CH-18+ continues to exhibit, escalate to `phi-core-leverage-check`-style hard-gate skill. |
| 2026-05-09 | `per-chunk-planning-template.md` | n/a (§3.C amendment rule) | CH-17 retro §5 row 3 | Added "Amend-don't-add precedence rule" to §3.C user-facing-doc impact map: when planner predicts a `m<N>/user-guide/` NEW file for a chunk-narrative walkthrough, the §3.C row MUST evaluate whether existing `m<prev>/user-guide/*` (the discoverable single-walkthrough surface for users) would be a more appropriate amendment target than fragmenting users across a 2-file walkthrough. Document evaluation rationale inline; if amendment chosen, cite target file + planned subsection title. CH-17 caught at audit B iter 1: plan §3.C row 4 predicted NEW `m5_2/user-guide/session-live-events-walkthrough.md`; orchestrator-applied Trivial-1L corrected to `m5/user-guide/first-session-walkthrough.md` "CH-17 amendment" subsection. |
| 2026-05-09 | `chunk-implementer.md` | v7 (chunk-close paperwork enumeration) | CH-17 retro §5 row 4 | Added explicit cycle-index row insertion as MANDATORY chunk-seal paperwork item (step 8) with verification command `grep -n <cycle-hex> /root/projects/phi/baby-phi/docs/specs/plan/build/_cycle-index.md`. NOT an implicit follow-on of plan §7 P-seal. Failure-mode CH-17 hit: implementer-spawn forked attention to mid-cycle scope expansion + dropped cycle-index row; orchestrator-applied Trivial-1L closed inline. |
| 2026-05-09 | `per-chunk-planning-template.md` | n/a (§8 buffer ceiling + mid-cycle scope re-stamp) | CH-17 retro §5 row 5 | Widened asymmetric buffer ceiling ×1.20 → **×1.30** when plan §8 lower-bound test count is contingent on Artifact C (assertion-edit fan-out) cascade. Mid-cycle scope-expansion rule: when user-driven scope expansion lands at gate-3 (e.g., integration tests added per user-locked re-dispatch), implementer MUST re-stamp plan §8 band immediately + log in implementation report; auditor-prompt-side band-widening is a band-aid, not primary fix. CH-17 ran [1484, 1488] plan band → [1488, 1495] audit-prompt band → 1491 actual; ×1.30 ceiling on Artifact-C cycles avoids future gate-discrepancy noise. |
| 2026-05-09 | `per-chunk-planning-template.md` + `chunk-implementer.md` Quality-bar | n/a (§8 enumeration split) + v7 (Quality-bar bullet) | CH-17 retro §5 row 6 | Split §8 test enumeration into MUST-SHIP (named test files that MUST exist as files-on-disk by chunk-seal — planner's binding contract) vs MAY-COVER (band-floor surrogate tests counting toward target but not MUST-SHIP). Chunk-implementer prompt v7+ MUST treat MUST-SHIP-absent-at-chunk-seal as a chunk-seal blocker, NOT silently substitute MAY-COVER coverage. CH-17 first implementer-spawn dropped MUST-SHIP `sse_live_stream_test.rs` per band-floor surrogate substitution; user-driven gate-3 re-dispatch closed gap with +4h scope. |
| 2026-05-09 | `chunk-planner.md` | v9 (§3 phi-core leverage map enumeration) | CH-17 retro §5 row 7 | When a phi-core type is shared across multiple sites (chunk's surface change touches ≥ 2 distinct files importing same `phi_core::*` type), §3 phi-core leverage map MUST anticipate Δ ≥ N rather than +1, with per-site enumeration column. CH-17 had `AgentEvent` shared across `state.rs:11` (registry value type for `broadcast::Sender<AgentEvent>`) + `events.rs:48` (SSE handler wire-serialisation); plan predicted +1, actual +2 — both canonical re-uses, neither duplicates a phi-core type. Not a violation, but predictability hardens auditor's gate-4 verification + closes a measurement-method drift class. |
| 2026-05-08 | `CLAUDE.md` (root) + `baby-phi/CLAUDE.md` (mirror) | n/a (not versioned) | CH-15 retro `ch-15-real-permission-check-gate-at-session-launch-c3f46f17/retrospective.md` §5 row 1 | Widened gate-2 doc-sync sweep (introduced at CH-14 retro Row 3) from plan §3.C-listed files only to ALL `docs/specs/v0/implementation/m*/architecture/*.md` + `m*/operations/*.md` + `m*/user-guide/*.md`. Added canonical stale-narrative phrase set: `FOLLOWUP-NN`, `deferred per`, `is NOT emitted`, `not emitted at CH-NN`, `advisory at M5`, `Step 0 only blocking`, `M6+ tightens the gate`, `at M5/P4`, `not blocking at M5`. 2-cycle pattern: CH-14 (3 docs stale post-gate-2) + CH-15 (1 doc stale outside §3.C map) both hit this. |
| 2026-05-08 | `per-chunk-planning-template.md` | n/a (not versioned) | CH-15 retro §5 rows 2 + 3 | Two §3 cascade-discipline caveats (chunk-planner v8 effective 2026-05-08): (a) Typed-multi-value cascade pause-threshold sizing — when cascade is typed-multi-value return-type change (e.g., `Grant → Vec<Grant>`, `T → CascadeResult { ... }`), size pause-threshold against deliberate-non-test-amendment edit count separately from test-amendment count (mechanical destructure ripple). CH-15 Artifact A actual 29 vs threshold 18 — 14 of those were test-amendments; sizing 15 vs 14 separately would have kept cycle within band. (b) Canonical phi-core baseline grep — codify `grep -rn "use phi_core"` (NO trailing `::`) as canonical measurement across all 4 audit lanes; CH-15 caught measurement-method drift between plan/auditor (49) and orchestrator (48 with `::`). |
| 2026-05-08 | `per-chunk-planning-template.md` | n/a (NEW §3.D) | CH-15 retro §5 row 4 | Added new §3.D — Forward-scope-vs-concept-doc precedence. **Binding rule**: when forward-scope row's literal text disagrees with closed concept-doc invariant (closed action vocabulary, fixed migration order, frozen schema, closed namespace tag set), concept doc wins; forward-scope wording becomes documented re-interpretation in relevant ADR sub-decision. Mechanical procedure: planner-side detection at chunk-open; ADR-side documentation in sub-decision body; user-lock outcome at gate-1. CH-15 ADR-0054 §D54.2 + §D54.8 is precedent (forward-scope said `session.start` / `session.tool_invoke` / `session.read_memory` as actions; concept-doc 03's closed 34-verb vocabulary precluded). |
| 2026-05-08 | `chunk-planner.md` | v7 → v8 | CH-15 retro §5 row 5 | Added "Forward-scope-vs-concept-doc precedence detection" subsection. When reading forward-scope row at chunk-open, grep concept-doc invariants for closed-set / fixed-order / frozen-schema language. If forward-scope literal terms NOT in canonical set, flag as **CRITICAL fork requiring user-lock**. Auto-approval blocker: forward-scope-vs-concept-doc contradiction always triggers user-escalation. Cross-ref per-chunk-planning-template §3.D + ADR-0054 §D54.2 + §D54.8 (CH-15 first instance). |
| 2026-05-08 | `permissions/granular-bash-discipline-ab19399b.md` | n/a (design doc) | CH-15 retro §5 row 6 | Added §2.7 "CH-15 5th-cycle validation: literal-script-name workaround empirically PASSES; cluster downgrades to `resolved-via-workaround`". Documents 5-cycle data series (CH-13: 4 → CH-07: 3 → CH-08: 15 → CH-14: 43 → **CH-15: 0**). The 5 literal-script-name rules at settings.json lines 41-44 close the bash-check cluster fully. **No upstream Claude Code rule-matcher bug-report needed.** Lifecycle conclusion: matcher-bug-confirmed → resolved-via-workaround. |
| 2026-05-08 | `chunk-implementer.md` + `chunk-auditor.md` | v5 → v6 (both) | CH-15 retro §5 row 7 | Mirrored CLAUDE.md §"Tool-specific absolute-path forms" verbatim into both agent prompts (closes 41/514 sub-agent `cd <abs> && <cmd>` drift observed in CH-15 telemetry — 8% of Bash signatures). Lists explicit replacements for `cd /root/projects/phi/baby-phi && git/cargo/bash/grep` patterns + cites the `Bash(bash /root/projects/phi/baby-phi/scripts/audit-tmp-*.sh*)` allow rule for multi-step audit/research scripts. |
| 2026-05-08 | `.claude/skills/permissions-audit.md` | v2 → v3 | CH-15 retro §5 row 8 | Added "Resolved-via-workaround terminal state" to §B regression-protection step. When `matcher-bug-confirmed` cluster's PermissionRequest count drops to 0 for ≥ 1 cycle post-workaround-commit, downgrade classification → `resolved-via-workaround`. Retrospector MUST: (a) record validation cycle's prompt count (0); (b) update granular-bash-discipline.md with new lifecycle state; (c) **drop cluster from cross-cycle trend tracking** (no longer noise). No upstream Claude Code rule-matcher bug-report needed at this terminal state. Regression handling: if future cycle finds prompts > 0 again, re-elevate to matcher-bug-confirmed and file upstream report. CH-15 first instance: bash-check cluster transitioned matcher-bug-confirmed → resolved-via-workaround after CH-14 close-time literal-script-name rules validated empirically (0 prompts / 514 events). |

## Versioning rules

- **Bump version on**: any meaningful change to an agent's quality bar / procedure / constraints / output format, OR a skill's procedure / output format.
- **Don't bump on**: typo fixes, frontmatter description tweaks that don't change semantics.
- **Source retro required**: every bump cites the retro hex that proposed the change. If no retro proposed it (rare — emergency fix), cite "out-of-band: <reason>".
- **Coordinated bumps**: when a retro proposes changes to multiple files, list each file's bump separately on its own row, but cite the same retro hex.

## Reading the index

Most-recent first. Old entries stay; the changelog is durable history.